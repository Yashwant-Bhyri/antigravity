# AGENTS.md — Shared AI Context File
> This file is the single source of truth for ALL AI assistants working on Antigravity.
> **Claude Code**, **Codex**, and **Antigravity (Gemini)** must all read this at the start of every session and update it after completing work.
> Yash edits this too when making decisions or giving direction.
> **⚠️ RULE: Always `git pull` before reading this file. Always `git push` after updating it.**

---

## 🤝 ONBOARDING — FOR ANY AI JOINING THIS PROJECT

**Welcome to Antigravity. Read this entire section before touching any code.**

### Who is on this team
| Agent | Role | Interface |
|-------|------|-----------|
| **Claude Code** | Primary backend + full-stack implementation, architecture decisions | Claude Code CLI (Yash's terminal) |
| **Codex** | Code generation, refactoring, implementation tasks | OpenAI Codex / ChatGPT |
| **Antigravity (Gemini)** | Full-stack, ideation, research, implementation | Google Gemini chat |
| **Yash** | Product owner, final decision maker | All of the above |

### Mandatory reading — go through EVERY LINE of these files before doing ANY work

**In `/Users/yash/antigravity/` (the project root):**
1. `AGENTS.md` ← you are here — read it entirely, every section
2. `PROJECT_STATE.md` ← **THE RELIGIOUS LOG BOOK.** Mandatory reading and mandatory update. Read the chronicle of the project's trajectory before doing any work.
3. `README.md` — project overview and API reference
4. `/Users/yash/Downloads/notes.md` — full PRD (Parts 1–5), the product vision, agent design, latency strategy, infra plan. This is the WHY behind every decision.

**Backend — read every file line by line:**
4. `backend/main.py` — FastAPI app entry, lifespan hooks, CORS
5. `backend/api/routes.py` — ALL endpoints: `/start_interview`, `/process_turn`, `/partial_transcript`, `/tts`, `/tts_filler`, `/end_interview`, `/state`, `/report`, `/deepgram_token`
6. `backend/services/orchestrator.py` — THE BRAIN. Parallel agent dispatch, attack strategy selection, sprint progression, prefetch logic. Most critical file in the backend.
7. `backend/services/tts_service.py` — Cartesia-first TTS, ElevenLabs fallback, filler cache warm-up
8. `backend/services/asr_service.py` — DEAD CODE. Do not use or modify. Frontend uses Deepgram SDK directly.
9. `backend/models/llm_router.py` — OpenRouter tiered model routing and usage/quality logging
10. `backend/state/session_manager.py` — Redis async session state, 1hr TTL
11. `backend/agents/concept_agent.py`
12. `backend/agents/weakness_agent.py` ← MOST IMPORTANT AGENT
13. `backend/agents/followup_agent.py`
14. `backend/agents/evaluation_agent.py`
15. `backend/agents/discrepancy_agent.py`
16. `backend/agents/resume_agent.py`
17. `backend/agents/reasoning_behavior_agent.py`

**Frontend — read every file line by line:**
18. `frontend/lib/audio.ts` — Deepgram browser SDK, utterance buffering, NER entity extraction, TTS utilities
19. `frontend/app/page.tsx` — Landing page (resume input)
20. `frontend/app/interview/[session_id]/page.tsx` — Main interview UI, filler-first response logic
21. `frontend/app/report/[session_id]/page.tsx` — Report display
22. `frontend/app/dashboard/page.tsx` — Recruiter dashboard (BLOCKED — needs `/sessions` backend endpoint)
23. `frontend/components/Waveform.tsx` — AIOrb, audio-reactive waveform
24. `frontend/app/layout.tsx`

**Config/Infra:**
25. `infra/docker-compose.yml`
26. `requirements.txt`
27. `frontend/package.json`
28. `.env.example` (never read `.env` directly — ask Yash for keys)

**Collaboration:**
29. `COLLAB.md` ← shared async discussion space between all AIs. Read it. Add to it when you have suggestions, questions, or critiques for the team.

### Rules you MUST follow
- Follow all conventions in the `## CONVENTIONS` section below exactly
- Never re-debate decisions in `## DECISIONS LOG` unless Yash explicitly reopens them
- Check `## IN PROGRESS` before starting work — don't duplicate
- After completing work: update `## COMPLETED`, `## DECISIONS LOG`, `## HANDOFF NOTES`
- **MANDATORY**: Update `PROJECT_STATE.md` with granular logs of **WHAT**, **WHY**, and **IMPACT** of every architectural shift or major code commit. This is non-negotiable.
- All cross-AI communication goes in `COLLAB.md`

---

## HOW TO USE THIS FILE

**Before starting any work:**
1. Read this entire file
2. Check `## In Progress` — don't duplicate work already being done
3. Check `## Decisions Log` — don't re-debate settled decisions
4. Check `## Conventions` — follow them exactly

**After completing work:**
1. Move your item from `## In Progress` to `## Completed`
2. Log what you did and any important decisions in `## Decisions Log`
3. Update `## Current State` if the architecture changed
4. **Update `PROJECT_STATE.md`** with granular logs (What, Why, Impact).
5. Flag anything the other AI needs to know in `## Handoff Notes`

---

## PROJECT IDENTITY

**Name:** Antigravity — AI Adversarial Interview Engine
**Location:** `/Users/yash/antigravity/`
**Goal:** A real-time voice-based cognitive interrogation system that probes the failure boundaries of a candidate's reasoning — not a chatbot, not a quiz engine.

**Core Loop:** User speaks → ASR streams → Parallel agents fire → Weakness detected → Follow-up generated → TTS responds instantly

**Philosophy:** Probe → Break → Analyze → Adapt. Never validate. Always attack the weakest point.

---

## CURRENT STATE

**Status:** Full end-to-end interview loop is live and running. All 7 agents wired. Frontend complete.

**Architecture note:** Audio flow is CLIENT-SIDE Deepgram SDK (not backend WebSocket relay). This was a deliberate change for latency. `asr_service.py` is preserved but unused — frontend uses `@deepgram/sdk` directly. The `/stream/{session_id}` WebSocket endpoint in the README is outdated.

**Shadow verification note:** `CompleteInterviewRunnerV1` is an isolated control harness only. It does not change or promote the live routes, UI, audio, Redis, Postgres, or provider path.

**What exists and is FULLY WIRED:**
```
backend/
  main.py                          ✅ FastAPI + lifespan (filler cache warm on startup)
  api/routes.py                    ✅ All endpoints: /start_interview, /process_turn,
                                      /partial_transcript, /tts, /tts_filler, /end_interview,
                                      /state, /report
  services/
    orchestrator.py                ✅ Full: parallel 4-agent dispatch, discrepancy priority,
                                      attack strategy selection, per-answer scoring (async),
                                      reasoning behavior wired, sprint progression
    tts_service.py                 ✅ Cartesia-first synthesis + filler pre-cache at startup
    asr_service.py                 🔲 Dead code — browser SDK used instead
  agents/
    concept_agent.py               ✅ Wired — partial + final transcript
    weakness_agent.py              ✅ Wired — context-aware (sprint, prior weaknesses)
    followup_agent.py              ✅ Wired — STRATEGY_MAP used in prompts,
                                      generate_discrepancy_challenge() method live
    evaluation_agent.py            ✅ Wired — Report V2, coverage gates, reasoning signals + per-answer scores
    discrepancy_agent.py           ✅ Wired — high severity triggers direct confrontation
    resume_agent.py                ✅ Wired — parses at session start, feeds all follow-ups
    reasoning_behavior_agent.py    ✅ Wired — runs in parallel, feeds final evaluation
  models/
    llm_router.py                  ✅ Tier-aware max_tokens: small=256, medium=768, large=2500
  state/
    session_manager.py             ✅ Redis async, 1hr TTL

frontend/
  app/page.tsx                     ✅ Landing page
  app/interview/[id]/page.tsx      ✅ True parallel filler-first: filler fires immediately,
                                      LLM + TTS prefetch run in parallel, zero gap on response
  app/report/[id]/page.tsx         ✅ Full report page
  app/dashboard/page.tsx           ⚠️ Frontend exists — blocked on /sessions backend endpoint
  lib/audio.ts                     ✅ playFiller(), prefetchAudio(), playAudioUrl()
  components/Waveform.tsx          ✅ AIOrb + audio-reactive waveform
```

**What does NOT exist yet:**
- `/sessions` list endpoint + Postgres persistence (dashboard blocked)
- RAG question bank (FAISS/Pinecone — not started)
- LangGraph StateGraph (raw asyncio works fine, low priority)
- Kafka/event bus (Redis Streams as fallback when needed)
- Tests and simulation harnesses exist for map, agenda, report, parser, scaffolding, model probes, and robust interview gates
- GitHub Actions CI

---

## TECH STACK

| Layer | Technology | Notes |
|---|---|---|
| Backend | FastAPI + Python 3.11 | async throughout |
| LLM | OpenRouter | Gemini, Claude Sonnet, DeepSeek by task policy; Opus is not a default live path |
| Orchestration | Raw asyncio + agenda controller | LangGraph remains low-priority/future |
| Session state | Redis (async) | running in Docker |
| Event bus | Kafka (planned) | Redis Streams as fallback |
| ASR | Deepgram WebSocket | streaming partial transcripts |
| TTS | Cartesia | **FINAL DECISION:** Cartesia-first forever. ElevenLabs fallback only. |
| Vector DB | FAISS (local dev) → Pinecone (prod) | RAG for question bank |
| Database | PostgreSQL | interview logs, scores |
| Deployment | Docker + Kubernetes | docker-compose for local |

---

## CONVENTIONS
> Follow these exactly. Do not deviate without updating this section.

1. **Agents never see raw transcripts.** They only receive JSON output from the preceding agent. Strict prompt chain isolation.
2. **All agent calls are async.** Use `asyncio.gather()` for parallel execution. Never block.
3. **LLM tier assignment:**
   - `small` → Gemini Flash Lite / Haiku-class structured extraction and cheap repair
   - `medium` → Sonnet/Gemini 3.5 Flash for follow-ups, map generation, critique, discrepancy
   - `large` → Sonnet or Gemini Pro for Report V2/final synthesis; Opus is not the default because of cost
4. **State lives in Redis only.** Never pass full state as function arguments between services.
5. **Agenda owns branching.** Weakness, discrepancy, fatigue, application transfer, coverage, and second-anchor signals feed the agenda selector; generic sprint prompts must not override map/application/coverage routing.
6. **TTS policy.** Cartesia is always first. Keep fillers context-free in this release; context-aware fillers are explicitly future-version scope.
7. **Python imports:** absolute imports only (`from backend.agents.x import X`), never relative.
8. **JSON outputs from all agents.** If LLM returns plain text, LLMRouter attempts to parse it. Agents must handle both `dict` and `str` returns gracefully.
9. **No sync Redis calls.** Use `redis.asyncio` everywhere.

---

## IN PROGRESS
> List who is working on what. Include AI name so we don't duplicate.

| Task | Owner | Notes |
|---|---|---|
| Frontend `turn_id` stale-response protection | Codex | `frontend/app/interview/[session_id]/page.tsx` + `frontend/lib/audio.ts`; depends on GM-1 |
| `/sessions` list endpoint (recruiter dashboard) | — | Dashboard page exists, needs backend endpoint + Postgres |
| LangGraph StateGraph wiring | — | Low priority — raw asyncio works fine for now |
| Postgres persistence (interview logs, session list) | — | Redis works for active sessions; need Postgres for history |
| Testing: unit tests for agents, simulation tests | — | No tests exist yet |


---

## COMPLETED

| Task | Done By | Date | Notes |
|---|---|---|---|
| CompleteInterviewRunnerV1 durable trace evidence repair | Codex | 2026-08-09 | Replaced the misleading redacted-record integrity flag with a separate exact canonical trace file (`0600`) and explicit `redacted_projection_only` artifact. Disk reconstruction now verifies InterviewTraceV1 integrity and all 15 spoken pairs; manifest records separate canonical/redacted hashes and canonical spoken-history hash. |
| CompleteInterviewRunnerV1 deterministic shadow checkpoint | Codex | 2026-08-09 | Added the bounded one-world runner, accepted InterviewTraceV1 evidence wiring, isolated storage/report/playback/actor seams, failure-injection contracts, causal checkpoint, and static manifest. The real production Orchestrator path completed 15/15 turns with 167 trace events and 16 quiescence boundaries. Run artifact is `/tmp/antigravity_complete_interview_runner_v1_control_20260809/`; shadow-only, candidate quality not assessed, no paid providers or live routes touched. |
| CandidateActorV1 deterministic actual-grant + behavioral provider gate | Codex | 2026-08-09 | Added 14 ledger-owned actual-grant fixture rows across five frozen worlds, the isolated exact 30-row matrix, and focused credential-loader tests. Repaired `.env.qwen.local` resolution through Git's primary worktree/common directory while preserving native environment precedence, restrictive permissions, redacted metadata, and no generic `.env` fallback. Deterministic checks passed (`21 pytest` rows; `8 unittest` rows). The OpenRouter run completed exactly 30/30 calls with 0 canonical responses: 15 schema/parse rejections and 15 deterministic safety rejections. Redacted packets are under `/tmp/candidate_actor_v1_provider_gate_20260809T083703Z`; durable hashes are in the gate manifest. No live orchestrator/UI/audio path changed, and this is not promotion evidence. |
| Replay voice QA harness with advanced capture | Codex | 2026-06-08 | Added `ReplayInterviewService` for `replay_*` sessions, replay API endpoints, `/interview-room/replay` launcher, passive layout QA capture from the locked live room, replay QA JSON/Markdown reports, mocked Playwright regression, opt-in real Deepgram/TTS voice QA spec, and WAV fixture helper. Replay sessions intercept state/partial/process/end/report before orchestration, so saved artifacts exercise the live room runtime with zero LLM calls. Verification: backend compile, `npm run build`, direct ASGI replay API smoke, scoped diff-check, and isolated `npm run test:replay-room` (`2 passed`). Real voice QA is implemented but intentionally opt-in via `REPLAY_VOICE_QA=1` + `REPLAY_FAKE_AUDIO_FILE`. |
| Live interview-room runtime smoke confirmation | Codex | 2026-06-05 | Confirmed `/interview-room/[session_id]` against a real backend session on local backend `8001` + production frontend `3010`. Fixed local CORS defaults for `localhost/127.0.0.1:3010` in `backend/main.py` and added `tests/interview-room-live-smoke.e2e.spec.ts`. Verification: build, backend compile, mocked room Playwright test, real-session smoke showing hydration from `/state`, `S. V. S. Apparao - Product Analyst`, media-gated start, `Mic ready`, and backend opening question. Caveat: smoke validates start/opening question path, not a real spoken answer commit. |
| V1 final silverline lock-in: best/messy/trap + report safety pass | Codex | 2026-06-05 | Ran a final three-case paid lock-in focused on report fairness, bad/noisy resume behavior, app-transfer floor, low-signal question prevention, and overclaim verdict gating. Latest clean paid artifacts: best product `/tmp/antigravity_v1_lock_best_product_fix3_20260605_full_gate.{json,md}` (`MAYBE 6.5`, structurally green; code now filters the old `ZERO_HONEST_ADMISSIONS` standalone risk if re-emitted), messy resume `/tmp/antigravity_v1_lock_messy_resume_fix_20260605_full_gate.{json,md}` (`MAYBE 7.0`, app transfer turn 5 after floor-recovery patch), trap overclaim `/tmp/antigravity_v1_lock_trap_overclaim_fix_20260605_full_gate.{json,md}` (`NO HIRE 3.2`, coverage gate passed, no banned file/tool/team recall patterns). Fixes: history-aware app-transfer served checks, app-transfer floor recovery from resume/focus fallback, Redis reconnect retry, report hydration/confidence-limit handling, removal of standalone honest-admission absence as risk, and deterministic/prompt bans for tool/team/file-format recall. Remaining V1 caveats: map prep is still slow (~97-102s), app-transfer/coverage questions can still be overlong, `legacy_agenda_backup` remains trace noise, and late-stage routing is safe but not yet senior-human optimal. |
| V1 three-case paid gate + hydration/routing fixes | Codex | 2026-06-05 | After commit `f36df20`, ran no-credit layer tests and paid gates one at a time. First `marketplace_growth` run exposed a second-anchor reselection recursion bug; fixed reselection to be iterative, duplicate-aware, and bounded by attempted surfaces. Marketplace retry exposed async hydration over-quarantining useful deferred surfaces for style/legacy compatibility notes; added deferred-only `_async_hydration_acceptance()` so structurally valid V2 tracks join the map with warnings while semantic/off-role/truncated ladder failures still quarantine. Final paid gates all passed: marketplace `/tmp/antigravity_v1_gate_marketplace_hydrationfix_full_20260605_full_gate.{json,md}` (`MAYBE 7.2`), best product `/tmp/antigravity_v1_gate_best_product_20260605_full_gate.{json,md}` (`MAYBE 6.8`), strong AI `/tmp/antigravity_v1_gate_strong_ai_20260605_full_gate.{json,md}` (`MAYBE 6.2`). Remaining V1 concerns: map prep latency/rescue variance, overlong coverage questions, telemetry noise from `legacy_agenda_backup`, and late second-anchor/same-surface warnings in best/strong. |
| Bounded third-surface probe routing | Codex | 2026-06-05 | Added a runtime `third_surface_probe` route for one high-signal deferred/third surface question after normal coverage/second-anchor work. It only uses accepted map material, skips pending/quarantined tracks, rejects bad-question families such as self-rating, low-signal recall, abstract framework prompts, and "what should we not claim" phrasing, and allows a second third-surface turn only when the first answer exposes concrete unresolved signal such as confounding, denominator, guardrail, ownership-boundary, refund/SLA, grain, or comparability risk. Verified compile, agenda contracts, ripper contracts, question-quality contracts, policy checker, and scaffolding audit `/tmp/antigravity_scaffolding_audit_20260605_022602.{json,md}`: 47 cases, 46 solved, 1 low hardcoding-risk/static review, no high-severity failures, green for paid confirmation. |
| App-transfer fallback, router retry, SurfacePlan deferred preservation, and marketplace confirmation | Codex | 2026-06-04 | Added fallback repair chain for overlong app-transfer questions, made JSON-format LLM calls retry after provider/client decode errors, improved robust harness failure diagnostics, and added SurfacePlanV2 deferred-focus preservation so high-signal planner surfaces are not erased when Gemini compresses the focus plan. Final marketplace full gate `/tmp/antigravity_surface_preserve_full_marketplace_growth_20260604_full_gate.{json,md}` passed: 15 turns, app transfer turn 5, coverage turns 6-8 with depth probe, second anchor turn 11, report complete, final `MAYBE 7.2`, quality gate passed. Caveat: dashboard/ops signal was mostly exercised through app-transfer/coverage; one deferred marketplace diagnostics track was quarantined for prosecutor/generic wording, so V1 stability is green but deeper hiring-signal orchestration remains future work. |
| Locked room UI live backend integration route | Codex | 2026-06-04 | Added `/interview-room/[session_id]` as the first live candidate-room route using the locked-room visual shell and the existing `lib/audio.ts` runtime (`InterviewSession`, Deepgram, barge-in, partial transcript, process-turn, TTS/filler, camera, end/report flow). Extracted `components/interview-room/InterviewRoomFloor.tsx` and added `tests/interview-room.e2e.spec.ts`. Locked prototype route was not mutated. Verified `npm run build` and focused Playwright mock-state route test. |
| V3 pre-integration scaffolding cleanup | Codex | 2026-06-03 | Fixed concrete scars from the latest V3 marketplace/best-product/strong-AI runs: overlong app-transfer questions are no longer retained after failed voice repair; async hydration is tracked so harnesses wait for the final map; full-gate artifacts include final `interview_trajectory_map` / hydration state; terminal warm/synthesis/close/complete packets are excluded from best/worst question scoring; grounded `legacy_agenda_backup` labels emit a low rename warning instead of high `late_generic_route` noise. Verified compile, parser contracts, policy checker, question quality, agenda, map contracts, map validation, saved-map replay, and scaffolding audit. |
| V3 best_product + strong_ai full gates | Codex | 2026-06-03 | Ran the next two V3 full gates one at a time. `best_product` passed: 15 turns, app transfer turn 5, coverage 6-7, second anchor 11, report complete, final `MAYBE 6.8`; artifact `/tmp/antigravity_v3_best_product_gate_20260603_full_gate.{json,md}`. `strong_ai` passed: 15 turns, app transfer turn 5, coverage 6-8, second anchor 11, report complete, final `MAYBE 5.5`; artifact `/tmp/antigravity_v3_strong_ai_gate_20260603_full_gate.{json,md}`. Remaining cleanup before broad silverline: app-transfer repair fallback kept an overlong strong-AI original after rejected rewrites, terminal close packets are scored as weak questions, and `legacy_agenda_backup` still appears in policy traces. |
| Launch-Ready Map Prep V3 + marketplace full gate | Codex | 2026-06-03 | Added LaunchTrackLite startup tracks, compact launch critic, V3 trace diagnostics, focus-plan rescue for underfilled primary plans, async hydration save-before-task ordering, and second-anchor spent-surface retirement/reselection. No-credit contracts and scaffolding audit are green; exact saved-map replay passed; paid `marketplace_growth` full gate passed with 15 questions, app transfer turn 6, coverage turns 7-8, second anchor turn 11, Report V2 complete, final `MAYBE 6.5`. Artifacts: `/tmp/antigravity_map_v3_marketplace_growth_full_anchorfix_20260603_full_gate.{json,md}` and `/tmp/antigravity_scaffolding_audit_20260603_005746.{json,md}`. |
| App-transfer floor replay + marketplace paid gate blocker | Codex | 2026-06-02 | Replayed the new app-transfer breadth/depth floor against historical full-run artifacts: 18 cases reviewed, 13 passed the new floor, 5 need attention or are old failed artifacts. Paid `marketplace_growth` was then run one at a time and intentionally stopped before `best_product`/`strong_ai` because the first case repeatedly failed before a valid full interview. Fixes applied: app-transfer validation now uses broader support context, hidden-internal repair diagnostics are explicit, supported BigQuery/dbt/schema context is allowed, map track parser normalizes object/list ladder text before strict schema validation, and focus planning no longer lets partial Pydantic recovery shrink raw focus areas before tolerant normalization. No-credit contracts are green, but paid confirmation remains blocked by launch-track generation/repair churn and timeout. Latest failed artifact: `/tmp/antigravity_appfloor_marketplace_growth_fix3_20260602_full_gate.{json,md}`. |
| Application-transfer breadth/depth floor + anchor recovery | Codex | 2026-06-02 | Tightened the app-transfer arc so two evaluated coverage dimensions are required when available, a light earned depth probe is scheduled when a depth-eligible surface has real signal, and vague early answers get one `application_anchor_recovery` question before resume-claim fallback. Policy checker now treats anchor recovery as app-transfer work in progress. Green: compile, ripper, agenda, policy checker, parser, scaffolding, final report, question quality, and diff check. |
| SurfacePlanV2 runtime wiring + deterministic question readiness | Codex | 2026-06-02 | Wired GPT-5.4 Mini-backed SurfacePlanV2 into `interview_map.generate_interview_map()` as a first-class pre-map recommendation layer. Gemini focus planning now receives compact focus/sub-focus/testable-surface recommendations, while `recommended_allocation_hint` is explicitly advisory and never direct question count logic. Added `backend/data/question_quality_guide.json`, `backend/services/question_quality.py`, packet-level `question_quality` metadata, and policy-checker warnings for bad-question families such as self-rating variants, low-signal SQL/event-property recall, generic framework prompts, unsupported internals, compound overload, late low-level probes, and guided lanes without escape hatches. Green no-credit verification includes compile, question-quality contracts, surface-plan contracts, policy checker, scaffolding, map, agenda, parser, final report, router JSON, usage audit, voice policy, ripper, saved-map replay, and scaffolding audit `/tmp/antigravity_scaffolding_audit_20260602_202155.{json,md}`. |
| Marketplace-growth silverline diagnostic case | Codex | 2026-06-02 | Added the `marketplace_growth` / Tanvi Menon Product Analytics Engineer fixture to `backend/test_robust_interview_simulation_suite.py` with question-aware LLM answer buckets for seller activation, KYC, first listing/order, support-call confounds, marketplace dashboard, OCR/CV demotion, honest correction, app transfer, coverage, and synthesis. Added a case-specific second-anchor expected-surface gate and a generic analytics focus-plan preservation repair in `interview_map.py` so concrete decision-use dashboard/reporting surfaces are not silently dropped. Paid map-only + replay are green after the repair (`/tmp/antigravity_marketplace_growth_planrepair_20260602_map_policy.{json,md}`, `/tmp/antigravity_marketplace_growth_planrepair_replay_20260602_20260602_172018.{json,md}`). Paid full run reached 15 visible turns with app transfer turn 5, coverage turns 6-8 including depth probe, but failed finalization due duplicate concurrent report generation; patched `orchestrator.end_session()` to wait on in-flight finalization instead of launching a second Sonnet report call. A fresh paid full confirmation is still needed before broad silverline batching. |
| Map-grounded reserve questions + app-transfer depth review | Codex | 2026-06-02 | Added a bounded reserve selector in `orchestrator.py` that can continue before synthesis using only already generated coverage dimensions, V2 ladder questions, or map-authored Q4 options; reserve packets preserve selected focus/sub-focus/coverage metadata and `reserve_map_question` is now map-backed in the policy checker. Depth review confirmed app-transfer depth probes already exist but only fire on partial/surface answers to depth-eligible coverage dimensions; recent Apparao runs had `depth_count=0` because answers were classified as missed, voluntary, or recovered-deep. Green: compile, ripper, policy checker, agenda, scaffolding, parser, final report, diff check. |
| Current Apparao map replay + visible-turn agenda repair | Codex | 2026-06-02 | Ran current paid Apparao map-only gate `/tmp/antigravity_current_map_gate_20260602_map_policy.{json,md}` and exact no-credit replay `/tmp/antigravity_current_map_gate_replay_20260602_20260602_151912.{json,md}`; both passed. Paid full gates exposed live runtime bugs: second-anchor/synthesis floors used ahead-of-history counters, close state was not globally sticky, and coverage-complete/no-secondary executor paths closed too early. Patched `orchestrator.py` to use visible answered history, make close sticky, count second-anchor by route kind, and continue with map-grounded questions before synthesis floor. Green no-credit checks and audit `/tmp/antigravity_scaffolding_audit_20260602_155052.{json,md}`; one fresh paid Apparao gate is still needed after the final executor patch before broader silverline runs. |
| No-credit interview ripper gate | Codex | 2026-06-02 | Added `backend/test_interview_ripper_contract.py` to inject early weak answers, repeated weak primary answers, missing application-transfer readiness, empty coverage maps, bad map-backed packets, late generic escape routes, two-anchor second-anchor/synthesis routing, and tunneled verdict overreach. Patched `orchestrator._coverage_route_allowed()` so zero-dimension coverage maps are not treated as valid coverage routes. Green: compile, ripper, agenda, policy checker, parser, scaffolding, map validation, saved-map replay, diff check. |
| Saved-map replay harness + raw-map artifact preservation | Codex | 2026-06-02 | Added `backend/test_saved_map_replay_suite.py` to replay saved full runtime maps without LLM calls, analyze historical full-run artifacts, and classify map-policy artifacts as raw replayable vs summary-only. Latest artifact `/tmp/antigravity_saved_map_replay_20260602_150328.{json,md}` showed 8 saved exports are obsolete pre-ladder maps and 24 recent `/tmp` map-policy artifacts are summary-only, so they cannot prove current startup replay. Patched `backend/test_robust_interview_simulation_suite.py` so future map-only artifacts preserve raw `interview_trajectory_map` for exact no-credit replay. |
| Application-transfer grounding arc + route ripple gate | Codex | 2026-06-02 | Added `application_grounding` before main app transfer for ambiguous depth anchors, persisted grounding/depth metadata on `AnswerCoverageMap`, excluded grounding from evidence/breadth/streak math, capped coverage depth by confirmed candidate layer, and made fast path promote coverage immediately after app transfer. Paid strong-AI gate `/tmp/antigravity_app_transfer_arc_strong_ai_20260602_full_gate.{json,md}` completed structurally with grounding turn 5/app transfer turn 6/report ready, but exposed coverage-delay/second-anchor/close issues that were patched. Follow-up paid rerun exposed independent map-prep timeout/schema fragility; parser now normalizes recovery object fields before validation. Latest no-credit audit `/tmp/antigravity_scaffolding_audit_20260602_145115.{json,md}`: 76 cases, 58 solved, 0 high-severity failures. |
| Incremental app-transfer depth + strong-AI close guard | Codex | 2026-06-02 | Application transfer is now breadth-first with 2-3 dimensions and explicit `surface_kind` / `depth_eligible`; coverage depth probes respect eligibility; hidden-internal guards block unsupported proprietary internals while allowing supported TinyML/SQL vocabulary; fast-track close flow no longer lets map/generic packets reopen after synthesis. Paid strong-AI gate `/tmp/antigravity_silverline_strong_ai_closeguard2_20260602_full_gate.{json,md}` passed structurally: 15 turns, app transfer turn 7, coverage turns 8-9, second anchor turn 10, report ready complete, final `MAYBE 5.8`. Remaining: slow map prep, occasional fail-closed app-transfer draft before repair succeeds, and `second_anchor_overused` quality warning. |
| Silverline Phase-1 production-readiness gate | Codex | 2026-06-02 | Implemented the low-risk Silverline pre-run fixes: application-transfer anchors are now `grounded transfer anchor`s instead of ownership/implementation-dominant anchors, overlong transfer question repair has deterministic checks plus a cheap verifier/retry, the robust harness has six cases including messy/noisy resume, early focus pivots are not mislabeled as second anchor, and premature synthesis is bounded. No-credit gates are green; latest audit `/tmp/antigravity_scaffolding_audit_20260602_043959.{json,md}` has 76 cases, no high-severity failures. Paid Phase 1 ran only `best_product` and `strong_ai`: best product structurally passed with app transfer turn 5/report ready/`MAYBE 6.2`; strong AI completed structurally but failed production quality due unsupported hidden technical-internals probes and rhythm warnings. Added a deterministic hidden-internals guard after the run; phases 2/3 intentionally paused pending paid confirmation. |
| Voice/rhythm and technical over-assumption fix pass | Codex | 2026-06-02 | Patched post-diagnostic issues from `/tmp/antigravity_v2_contract_diag_20260602_full_all.{json,md}`: second-anchor now caps at two turns and synthesis is terminal, late second-anchor no longer falls back to generic sprint questions, application-transfer anchors moved away from "most specific implementation detail" toward grounded work/decision/tradeoff evidence, app-transfer questions get cheap structured voice repair when overlong, coverage prompts are constrained to one spoken ask, map prompts establish ownership before hidden technical internals, duplicate `session_ended` telemetry is guarded, and Report V2 no longer calls broad same-parent surface coverage tunneling. Latest audit `/tmp/antigravity_scaffolding_audit_20260602_025618.{json,md}`: 75 cases, no high-severity failures, green for paid confirmation. |
| V2 contract paid 3-case diagnostic run | Codex | 2026-06-02 | Ran `best_product`, `strong_ai`, and `trap_overclaim` as full 15-turn LLM-answer simulations after V2 map contract alignment. All three passed structural gates with app transfer on turn 5, coverage after transfer, second anchor by turn 10/11, report ready, and complete finalization. Artifacts: `/tmp/antigravity_v2_contract_diag_20260602_full_all.{json,md}` plus token/quality audits. Remaining issues: long coverage/application questions, second-anchor streak warnings, repeated closing prompt, and startup latency. |
| V2 map contract consumer alignment audit | Codex | 2026-06-02 | Audited and aligned runtime/API/harness consumers around the unified map contract: `question_ladder` is authoritative, `assessment_dimensions` are evidence axes, and legacy fields are compatibility aliases only. `orchestrator.py`, `interview_map.py`, `routes.py`, and simulation harness previews now use V2-aware helper accessors. Latest no-credit audit `/tmp/antigravity_scaffolding_audit_20260602_020511.{json,md}` stayed green: 75 cases, 0 high-severity failures. |
| Interview Map V2 ladder-authority migration | Codex | 2026-06-02 | Made `question_ladder` the explicit map source of truth. V2 tracks now include `map_schema_version=v2_ladder`, `primary_question_contract=question_ladder`, and `legacy_fields_authority=compatibility_only`; old `opener` / `dimensions` / `recovery` / `candidate_q4_options` are derived into `legacy_compat` and exposed as aliases only for compatibility. Validation no longer requires legacy recovery completeness. Latest no-credit audit `/tmp/antigravity_scaffolding_audit_20260602_015125.{json,md}` stayed green: 75 cases, 0 high-severity failures. |
| Launch contract authority repair | Codex | 2026-06-02 | Fixed the deeper dual-contract issue after the three-case diagnostic run. Schema-only track rescue now rejects hollow but valid JSON; legacy `recovery.*` / `candidate_q4_options` no longer block launch when the runtime `question_ladder` is usable; track-level launch defects can replace the weak focus from the existing plan; close routing avoids repeated synthesis; robust gate uses surface streaks when sub-focus/coverage breadth exists. Latest no-credit scaffolding audit `/tmp/antigravity_scaffolding_audit_20260602_013940.{json,md}`: 75 cases, 57 solved, 17 old-artifact unknowns, 1 low hardcoding-risk warning, 0 high-severity failures, green for paid confirmation. |
| Three-case full diagnostic simulation | Codex | 2026-06-02 | Ran `best_product`, `strong_ai`, and `trap_overclaim` full simulation with quality capture. Artifacts `/tmp/antigravity_diag_3case_20260602_full_all.{json,md}`. Best product and strong AI failed at map prep with preserved diagnostics; trap completed 15 turns with app transfer turn 5, coverage turns 6-9, second anchor turns 10-12, synthesis from turn 13, final `INSUFFICIENT_DATA 3.5`. Findings: schema rescue is too permissive, legacy recovery fields still over-block launch despite ladder runtime source-of-truth, synthesis can repeat after second-anchor bound, and robust gate still has a parent-focus vs surface-aware streak mismatch. |
| Map readiness diagnostics + second-anchor bound | Codex | 2026-06-02 | Added `MapPreparationError` diagnostics so failed bounded-launch map prep preserves pass-one/repaired candidates, Sonnet reviews, repair targets, attempt errors, repair provenance, latency steps, and model policy. Track generation now tries a cheap Gemini Flash Lite schema-only normalizer before Sonnet rescue for useful-but-wrong-shape output. Robust simulation artifacts surface these diagnostics. Agenda now bounds `second_anchor` to max three turns total / two per focus, then forces synthesis close. Generic background sprint fallback is demoted to `legacy_agenda_backup`; startup seed route is `seed_first_followup`. |
| Policy checker multi-case sample | Codex | 2026-06-02 | Ran three selected robust full cases plus one replacement after trap map-prep failed. Best product and average partial passed 15-turn gates; strong AI completed but second anchor arrived one turn early; trap overclaim failed closed during launch-map readiness. Harness now surfaces policy checker events. Checker was extended to warn on premature synthesis and repeated second-anchor holding patterns. |
| Signal-only interview policy checker agent | Codex | 2026-06-01 | Added `backend/agents/policy_checker_agent.py` and wired it into `orchestrator.py` after next-packet staging. It emits warning-only telemetry/state for true same-surface tunneling, low-breadth parent-focus repetition, late generic routes, missing map focus, missing app transfer/coverage/second anchor, pressure streaks, and completion/report inconsistencies. It does not steer yet; use the next 2-3 runs to decide authority. |
| Stale scaffolding sweep after Apparao gate | Codex | 2026-06-01 | Cleaned stale runtime/reporting leakage after the paid full gate: coverage dimensions now propagate through packets/history and count as surfaces, map-promoted questions no longer retain stale `sprint_seed` labels, the opener route is `warm_open`, completion copy no longer says "three sprints", unreachable deterministic fallback bodies were removed from `interview_map.py`, and README/AGENTS stale Opus/static-fallback/full-map language was updated. Objective read: the Apparao run repeated the same parent focus but covered distinct surfaces; this was not same-weakness tunneling. |
| Paid confirmation + Apparao 15-turn gate | Codex | 2026-06-01 | Ran paid Gemini/Sonnet/Gemini-report confirmations, fixed an async sub-focus `NameError`, aligned map generation timeout to the 300s startup budget, added robust-sim progress logs, and regraded the Apparao full gate with the surface-aware anti-tunnel formula. Final regraded artifact `/tmp/antigravity_paid_confirmation_full_app_rerun3_20260601_full_gate_regraded.{json,md}` passes: 15 turns, app transfer turn 5, coverage turns 6-10, second anchor turn 11, report ready/finalization complete, verdict `MAYBE` 5.5. |
| Second-pass cross-component scaffolding stress audit | Codex | 2026-06-01 | Expanded `backend/test_llm_scaffolding_audit.py` with harsher handoff mutations: missing typed surface metadata, wrong typed surface metadata, ladder-only tracks with noisy weights, and strong local answer scores under narrow coverage. Added visible `heuristic_fallback_used` warnings in `interview_map.py` when boundary classification falls back to keyword heuristics, and kept typed `surface_kind` as the primary source of truth. Final second-pass artifact `/tmp/antigravity_scaffolding_audit_20260601_204637.{json,md}`: 75 cases, 57 solved, 17 old-artifact unknowns, 1 low hardcoding-risk warning, 0 still-broken, no high-severity failures. |
| Full deterministic LLM scaffolding audit harness | Codex | 2026-06-01 | Added `backend/test_llm_scaffolding_audit.py`, a no-credit audit runner covering LLM callsite inventory, historical artifact replay, mutation cases around prior failures, hardcoding-risk scan, and Markdown/JSON reporting. Fixed two audit-discovered scaffolding weaknesses: short malformed JSON now gets brace/bracket quality flags, and truncated critic JSON can no longer be locally repaired into fake `ready=true`. Removed Product/CV-specific application-anchor scoring from `orchestrator.py` in favor of map-authored role relevance and coverage weights. Final artifact `/tmp/antigravity_scaffolding_audit_20260601_203311.{json,md}`: 71 cases, 53 solved, 17 old-artifact unknowns, 1 low hardcoding-risk warning, no high-severity failures, green for targeted paid confirmation. |
| Latest Apparao map scaffolding repair gate | Codex | 2026-06-01 | Fixed ladder-first-to-legacy normalization, targeted repair preservation, DeepSeek pending-vs-timeout metadata, high-value ladder boundary repair targeting, leftover issue preservation after field verifier, and repair-critic skip safety. Final paid artifact `/tmp/antigravity_latest_code_app_map_fix4_20260601_map_policy.{json,md}` passed launch readiness with map score 9.1/no CV promotion, but latency remains high at ~234.8s due to Sonnet rescue/critic. |
| No-credit parser/schema/REST scaffolding stress gate | Codex | 2026-06-01 | Ran the deterministic no-credit gate before paid LLM-answer simulation: LLMRouter JSON, parser contracts, scaffolding contracts, map/agenda/final-report/token-audit tests, plus inline REST `/report` and `/state` shape smokes. Patched `backend/api/routes.py` to use `Field(default_factory=...)` for list/dict request defaults and to coerce malformed report/session shapes safely. Recommendation: proceed to latest-code paid Apparao map-only, then full 15-turn LLM-answer if sane. |
| Typed surface taxonomy + advisory ladder audit | Codex | 2026-06-01 | Focus-plan sub-focuses now carry `surface_kind`; boundary checks use typed surface metadata before fallback token heuristics; track prompts are assembled as named schema/voice + role sections; DeepSeek V4 Flash can run a non-blocking ladder-quality audit for expected-space, voice complexity, closed-choice, low-info, and prosecutor-streak signals. Verified py_compile and map/parser/validation contract tests. |
| Ladder follow-up importance gate | Codex | 2026-06-01 | Same-thread ladder follow-ups are now gated by structured map value rather than being available equally for every surface. High-value surfaces can use shallow/strong follow-ups; lower-value surfaces use plain recovery/main questions and should pivot sooner. Added contract coverage and verified compile/map/parser/validation tests. |
| Ladder/map brittleness audit pass | Codex | 2026-06-01 | Audited recent voice-first ladder/map changes for hardcoded and brittle behavior. Fixed stale `≥3 dimensions` prompt/critic wording, prevented repeated ladder postures from automatically choosing shallow follow-ups without a recovery signal, reordered taxonomy boundary classification ahead of conversion/subscription terms, and marked guided examples as style-only with an engineering example. Remaining follow-ups: expected-space coverage is not scored yet, low-info/voice-complexity reports rely on model self-labels, and product/CV word heuristics should become typed role-rubric rules. |
| Gemini track-generation rejection root cause probe | Codex | 2026-06-01 | Ran isolated Gemini-only Apparao retention/conversion track probes with Sonnet rescue disabled. Found our parser was rejecting otherwise useful Gemini output for stale compatibility reasons: missing legacy `recovery.short_answer` despite a usable `recover` ladder item, and exactly two dimensions despite a complete high-information ladder. `interview_map.py` now backfills legacy recovery from ladder items, accepts two dimensions only with a complete high-info ladder, logs raw output type/keys/preview on parse failures, and prompts for guided answer lanes like the 7-day-to-1-day trial example. After fixes Gemini produced a valid track in ~22.9s with no attempt errors. |
| Voice-first question ladder tracks | Codex | 2026-06-01 | Added `question_ladder` to focus tracks with frame/clarify/explore/pressure/synthesize/recover postures while preserving legacy map fields. Runtime now selects ladder questions before legacy pressure probes and carries posture/signal/expected-space metadata through packets, history, and robust reports. Surgical repair supports exact ladder paths and rejects truncated or over-prosecutorial replacements. Contract tests cover ladder parsing, repair safety, frame/clarify-before-pressure, and shallow-answer recovery. Paid Apparao map-only `/tmp/antigravity_robust_interview_map_policy.{json,md}` passed quality gates but exposed remaining startup latency: ~313s, with Gemini falling through to Sonnet generation and a launch replacement pass. |
| Five-resume bounded map-only suite + boundary false-positive repair | Codex | 2026-06-01 | Ran `/tmp/antigravity_bounded_launch_five_map_20260601_map_policy.{json,md}` after the bounded Apparao gate: 4/5 launch-ready in batch, with the trap inflated-claim case failing closed after bounded repair/replacement. Isolated trap rerun passed but exposed a deterministic boundary-check bug where retention/churn `campaign` language was misclassified as dashboard leakage. Patched `backend/services/interview_map.py` so conversion/retention boundary validation treats churn, checkout, pricing, funnel, holdout, seasonal, buyer/seller, success, and activation as valid surfaces and reserves leakage for actual dashboard/acquisition terms. Fixed trap artifact `/tmp/antigravity_bounded_launch_trap_fixed_20260601_map_policy.{json,md}` passed in ~74s. Final fixed full-suite artifact `/tmp/antigravity_bounded_launch_five_map_fixed_20260601_map_policy.{json,md}` passed 5/5 with all cases launch-ready, first two tracks ready, no quarantines, and third tracks deferred for async hydration. |
| Bounded launch-ready interview map prep + Apparao full gate | Codex | 2026-06-01 | Refactored map startup so turn 1 blocks only on two launch-critical tracks; later tracks defer into async hydration/quarantine; DeepSeek V4 Flash is compact advisory focus-plan audit only; Sonnet critiques launch tracks only; indexed critic paths now repair surgically. Paid Apparao map-only `/tmp/antigravity_bounded_launch_map4_20260601_map_policy.{json,md}` passed with two product analytics launch tracks and deferred marketing attribution. Paid 15-turn run `/tmp/antigravity_bounded_launch_full_gate_20260601_full_gate_rejudged.{json,md}` passed with 15 turns, app transfer turn 6, coverage turns 7-9, second anchor turn 10, report ready, verdict MAYBE 6.8. |
| Cheap LLM answerer Apparao gate + vulnerability findings | Codex | 2026-06-01 | Added a Gemini 3.1 Flash Lite question-aware candidate answerer to `backend/test_robust_interview_simulation_suite.py`, retaining the old answer bank as fallback and reporting answerer metadata. First run `/tmp/antigravity_llm_answerer_gate_20260601_072916_full_gate.{json,md}` completed with 0 answerer fallbacks and a saner `MAYBE` 6.2 report, but exposed duplicate application-transfer staging, WeaknessAgent enum crash, and history/question-count mismatch; patched `weakness_agent.py` and `orchestrator.py`. Second run `/tmp/antigravity_llm_answerer_gate2_20260601_074126_full_gate.{json,md}` failed before turn 1 because Sonnet correctly caught a plan-level map defect: a marketing/dashboard focus had CV-internship content, and full blocking plan repair timed out. Next target is bounded/nonblocking map plan repair for bad later tracks. |
| Apparao full 15-turn structural gate + answer-harness diagnosis | Codex | 2026-06-01 | Ran paid full-gate attempts with Report V2/token/quality audit. First attempt failed before turn 1 due to map repair/critic timeout, so `interview_map.py` now defers local startup repairs when pass-one is launch-ready, respects strong top-two critic readiness, uses compact repair critique, and avoids false readiness blocks from harmless opener/local repair notes; `orchestrator.py` now preserves empty exception types like `TimeoutError`. Latest run `/tmp/antigravity_full_gate_20260601_070050_full_gate.{json,md}` structurally passed: 15 turns, app transfer turn 5, coverage 6-10, second anchor 11, max focus streak 3, no generic flags, report complete. Token audit recorded 118 calls / 217,972 tokens / 0 retries; quality audit recorded 0 parse/provider failures. Substantive quality is not green yet because the synthetic Apparao answer selector still mismatched generated questions, producing a harsh but transcript-consistent `NO HIRE` 2.5. |
| Realtime interaction/action-deck contract | Codex | 2026-06-01 | Added `REALTIME_INTERACTION_ACTION_DECK_CONTRACT.md` to capture the live voice contract Yash established: hierarchical local action decks over the trajectory map, receipts before assessment moves, same-turn continuation for thin answers, non-counting rephrase/simplify when the candidate does not understand, balanced positive/neutral/risk answer signals, Realtime boundaries, counting vs non-counting move taxonomy, and implementation slices for `ActionDeck`, `InteractionMove`, `AnswerSignalPacket`, and spoken-question commits. |
| Model evaluation ledger README | Codex | 2026-06-01 | Added `MODEL_EVALUATION_README.md` as the append-only model-evaluation ledger for paid/isolated/model-policy runs. It records the Report V2 large-tier green gate, artifacts, token/cost audit, quality audit, model-policy interpretation, and the next planned full 15-turn Apparao gate. |
| Evidence-first final report V2 | Codex | 2026-06-01 | Rebuilt final evaluation around a `FinalEvidencePacket`, multi-lens report schema, pre-prose coverage/interviewer-quality gates, resume-hype calibration, alternate-fit preservation, confidence bands, and advisory review reconciliation. Per-answer scoring now feeds a local `turn_evidence_trail` plus `progression_summary` instead of only an average, so reports can reason about improvement/recovery/decline without treating local scores as verdict bricks. `/api/report` and persisted reports now expose V2 fields while keeping legacy report fields; the report UI now leads with assessment limits, role fit, strongest signal, tested strengths/risks, claim calibration, and follow-ups instead of raw weakness cards. Added `backend/test_final_report_quality_matrix.py` and ran the paid report-only large-tier gate across Sonnet 4.6, Gemini 3.1 Pro, and Gemini 3.5 Flash on five fixtures; final artifact `/tmp/antigravity_report_v2_quality_matrix_20260601_055244.{json,md}` passed 15/15 with zero retries/parse failures/provider errors. Token audit: `/tmp/antigravity_report_v2_token_audit_20260601_055244_1780274102.{json,md}` estimated $0.921536 for 30 calls. Quality audit: `/tmp/antigravity_report_v2_llm_quality_audit_20260601_055244_1780274102.{json,md}`. Verified py_compile, deterministic final-report contracts, paid report matrix, and `npm run build`. |
| Opt-in full-capture LLM quality audit | Codex | 2026-06-01 | Added `LLM_QUALITY_AUDIT=1` diagnostic logging beside the token audit. Quality logs capture per-attempt/full-call prompt/result data, parse flags, retry state, output shape, and full system/user/output text when `LLM_QUALITY_CAPTURE_TEXT` is enabled. Added `backend/llm_quality_audit_report.py` for agent/call-site/model summaries, repeated prompt detection, flagged malformed/verbose/meta outputs, and token join by `call_id`; verified py_compile, mocked quality/usage/privacy contracts, and report smoke. |
| DeepSeek latency micro-benchmark | Codex | 2026-06-01 | Added `backend/deepseek_latency_probe.py` and ran 3 repeats each for plain product question, strict JSON, and JSON repair on DeepSeek V4 Pro + V4 Flash via direct OpenRouter calls. Artifact: `/tmp/antigravity_deepseek_latency_probe_r3.{json,md}`. V4 Flash passed 9/9 with avg ~5.4s/median ~2.7s but two ~13s plain-text outliers. V4 Pro passed 5/9 with avg ~10.1s/median ~11.5s and several zero-visible-output calls despite reported completion tokens. |
| Direct DeepSeek/Gemini scaffolding probe | Codex | 2026-06-01 | Ran direct OpenRouter probes outside Antigravity agents/wrappers for DeepSeek V4 Pro, DeepSeek V4 Flash, Gemini 3.1 Flash Lite, and Gemini 3.5 Flash. DeepSeek passed direct reasoning, application-transfer JSON, JSON repair, and critic-shape checks, but OpenRouter latency stayed high (DeepSeek Pro avg ~20.5s on 3 direct cases; DeepSeek Flash avg ~16.8s; direct critic 27-42s). Gemini 3.1 Flash Lite passed critic/schema-repair direct checks in ~1.1-1.5s; Gemini 3.5 Flash passed in ~4.3-7.3s. Artifacts: `/tmp/antigravity_direct_deepseek_probe.{json,md}` and `/tmp/antigravity_direct_critic_repair_probe.{json,md}`. |
| Deterministic scaffolding contract audit | Codex | 2026-06-01 | Extended the parser sweep into model-adjacent scaffolding: question packet queues, map-backed focus attribution, coverage/verdict gate coercion, and token-audit privacy/counting. Added `backend/test_scaffolding_contracts.py` and `backend/SCAFFOLDING_AUDIT.md`; verified no-credit py_compile plus router/parser/scaffolding/token-usage/agenda/map contract tests. |
| Parser/data-boundary robustness sweep | Codex | 2026-06-01 | Audited assessment-critical parser paths after the Sonnet false-array finding. Hardened follow-up JSON question extraction, interview-map string parsing, critic payload coercion, concept/weakness/discrepancy/application agent output validation, coverage-map hydration, and candidate/agenda state hydration. Added `backend/test_parser_contracts.py` for malformed JSON repair, wrong-shape rejection, enum validation, no char-splitting from strings, and corrupted saved-state tolerance; verified py_compile plus parser, agenda, map contract, and validation tests. |
| LLMRouter JSON parser root-cause fix + local JSON repair | Codex | 2026-06-01 | Isolated the map-critic schema issue and found Sonnet was often returning an object-shaped response, but `LLMRouter._load_json_lenient()` could falsely parse a nested array such as `strengths` when the outer object was malformed/truncated. Patched the parser to respect the first JSON shape, added `json-repair>=0.59.10` as a local syntax-repair layer before any LLM schema rescue, and added `backend/test_llm_router_json.py`; verified py_compile plus router/map contract and validation scripts. |
| LLM token usage audit instrumentation | Codex | 2026-06-01 | `LLMRouter` now emits metadata-only per-attempt and aggregate usage records with provider tokens when available, estimated fallback tokens, retry overhead, call-site/session hints, latency, max-token utilization, and hashes rather than raw prompt/output text. Added `backend/services/llm_usage.py`, `backend/llm_token_audit_report.py`, and `backend/test_llm_usage_audit.py`; verified py_compile, router JSON contract, mocked usage/retry/privacy tests, and report-generator smoke. |
| Paid map critic schema-rescue verification | Codex | 2026-06-01 | After credits were refilled, ran focused map-only paid checks. `best_product` passed in ~161s with one verified surgical repair and no CV promotion. `strong_ai` reproduced the Sonnet top-level-array critic failure, so `interview_map.py` now has an explicit schema-rescue critic model (`OPENROUTER_MAP_CRITIC_SCHEMA_RESCUE_MODEL`, default Gemini 3.5 Flash) used only after Sonnet full/strict/compact critic attempts all fail object shape. Strong-AI then passed with `schema_rescue_used=true`, critic score 9.6, and map score 9.2; latency remained bad at ~358s. |
| Outcome-aware map boundary checker | Codex | 2026-05-31 | Tuned taxonomy/instrumentation boundary checks so Product/Analytics/Growth roles can ask outcome-led causal questions like conversion/retention lift without being flagged solely for outcome language. Vague outcome prompts are still flagged when they lack causality, measurement, attribution, schema, or ownership intent. Verified with py_compile plus map contract/validation scripts. |
| Map critic array-shape schema repair | Codex | 2026-05-31 | Fixed critic schema instability where Sonnet/OpenRouter could return JSON arrays and the validator would silently replace the critic with default `6.0` values. Critic parsing now recovers object arrays/fragments and retries pure string arrays once with a strict single-object contract; unrecoverable critic output now fails closed. Live strong-AI canary hit the old array failure and correctly retried, then stopped on OpenRouter 402 affordability instead of producing a fake review. |
| Five-resume map-only typed-repair impact run | Codex | 2026-05-31 | Ran the robust map-only suite across all five stress resumes after typed critic/field-verifier repair changes. Artifacts: `/tmp/antigravity_map_quality_repair_all_map_policy.{json,md}`. Result: 5/5 maps ready and first two tracks launch-ready; avg latency ~185s. Surgical verifier worked: accepted bounded field repairs, skipped second full critic when exactly 3 repairs passed, and rejected bad/no-op/unreadable swaps into full regeneration. Main remaining blocker before full interview fanout is critic-output schema instability: Sonnet sometimes returns list-shaped JSON, causing fallback 6.0 reviews despite high scorecards. |
| Typed map critic + field-verifier repair routing | Codex | 2026-05-31 | `backend/services/interview_map.py` now normalizes typed critic issues (`plan_level`, `track_level`, `field_level`, `readability_level`, `weighting_level`) and routes repairs by `issue_scope/action`. Added cheap structural checks for analytics focus-boundary leakage, voice readability, and sub-focus weight calibration; surgical repairs now store old/new provenance and can skip the second full Sonnet critic when 1-3 exact fields pass the verifier. Robust map reports now include quality scorecards, weight warnings, and repair summaries. Verified contract/validation/compile checks and a best-product map-only smoke at `/tmp/antigravity_map_quality_repair_map_policy.{json,md}`. |
| Exact-path map repair classification fix | Codex | 2026-05-31 | Confirmed exact repair targets already exist and patched `_critic_signals_plan_problem()` so local feedback like "focus area 2 opener" no longer triggers full plan regeneration when exact `repair_targets` exist. Added contract tests proving local opener repair stays surgical while genuine duplicate focus-plan defects still trigger plan repair. |
| Map latency breakdown instrumentation | Codex | 2026-05-31 | Added `latency_breakdown` to generated interview maps and the robust map-only report. A single Apparao/product profile saved `/tmp/antigravity_map_latency_breakdown_map_policy.{json,md}` and showed total 215.9s: pass-1 plan/track/critic about 114s, then an ultimately rejected repair pass about 101.9s. DeepSeek audit wait was not the blocker. Root finding: local question repair feedback can still be misclassified as a focus-plan defect, causing expensive full plan + all-track regeneration. |
| Role-relevance weighted sub-focus map surfaces | Codex | 2026-05-31 | `backend/services/interview_map.py` now asks the focus planner for structured sub-focus objects with `role_relevance_weight`, `profile_importance_weight`, `evidence_strength`, `claim_risk`, and `coverage_value`, while keeping legacy string sub-focus compatibility. Focus plans/runtime maps are ordered by coverage value, agenda secondary pivots prefer higher-value role-relevant surfaces over flashy off-role claims, and final coverage accounting reports weighted/high-value surface coverage. Added regression coverage for choosing Apparao-style dashboard/product surfaces over off-role CV benchmarking. A single Apparao map-only smoke saved `/tmp/antigravity_weighted_subfocus_check_v2_map_policy.{json,md}`; it selected product surfaces over CV and produced rubric-derived weights, but startup was still slow at ~190.6s. |
| First-class sub-focus surface coverage model | Codex | 2026-05-31 | Promoted the intended `experience area -> sub-focus surface -> question/dimension coverage` model into live state and gates. History now stores `sub_focus_key`/`sub_focus_label`, agenda tracks `turns_by_surface`, assessment coverage reports `distinct_surfaces` and `surfaces_by_focus`, hard verdict gates accept broad sub-focus coverage inside one experience, and simulation gates use surface breadth instead of blindly requiring multiple focus keys. Also fixed the live background pipeline so answered-turn focus is not overwritten by the next selected focus after a pivot, and application-transfer packets inherit a mapped focus instead of inventing a focus key from prose. |
| Repo sweep for formula/focus-planning divergence points | Codex | 2026-05-31 | Audited live interview orchestration, map planning, focus attribution, simulation harnesses, and completion/verdict gates for the same failure family as the Apparao CV jump. Patched the remaining pre-agenda hard-coded focus-ratio guard to use focus-evidence turns and the primary-focus floor, removed global consecutive-weakness count as a focus-rotation trigger, made map-backed packets fail if they lose focus attribution, softened critic plan-regeneration detection so local repairs stay surgical, removed the stale "60% primary focus" planning instruction, expanded focus inference/snippet recovery to include sub-focuses and dimension text, and patched the older live simulation harness to measure answered focus rather than next staged focus. |
| Floor-ownership LiveKit room prototype | Codex | 2026-05-31 | Added new `/visualizer-livekit-room-floor` route, preserving `/visualizer-livekit-room` as baseline. The new version keeps the official LiveKit Aura, adds a right-rail candidate camera presence tile, makes AI-vs-candidate floor ownership explicit through a center rail and zone highlights, keeps the question card pitch black, separates live transcription from committed turn history, and verifies the 10-turn history stack/collapse behavior. |
| Role-aware agenda anti-tunnel fix | Codex | 2026-05-31 | Patched the live repo path, not only the simulation harness: anti-tunnel dominant-focus ratio now has a minimum evidence-turn floor, the primary focus gets a minimum depth floor before ratio/rotation can move away, coverage respects the streak cap after minimum coverage, Product Analyst application transfer is target-role-domain gated, map focus planning now allows multiple role-relevant surfaces from the same real job before unrelated internships, and the robust simulation transcript now separates answered focus from next-question focus. Added agenda contract tests for the early-ratio failure. |
| Surgical interview-map repair restored | Codex | 2026-05-31 | Re-read the prior map-repair docs/history and patched `backend/services/interview_map.py` so critic `opener_issue` is preserved, critics can emit exact `repair_targets`, and repair now tries a small question-level patch before regenerating a whole focus track. Localized map flaws can now swap `opener`, `dimensions[n].surface/mechanism/boundary`, recovery probes, or Q4 options; full track/plan regeneration remains reserved for broad defects. Verified with py_compile, diff check, local surgical patch probe, and existing map validation/contract scripts with `PYTHONPATH=.` |
| Five-resume robust simulation harness + first gate run | Codex | 2026-05-31 | Added `backend/test_robust_interview_simulation_suite.py` with `map_only`, `full_gate`, `full_all`, and `both` modes, five stress resumes/profiles, map policy tracing, question-aware answers, coverage/application/second-anchor checks, best/worst question scoring, and JSON/Markdown artifacts. Map-only run: 5/5 maps ready, Sonnet rescue used 4/5, DeepSeek audit present 5/5, avg map latency ~184s. Full best-case gate completed 15 turns with report ready and `INSUFFICIENT_DATA`, but failed anti-tunneling gate (`max_same_focus_streak=6`); remaining full cases were not run per gate rule. Artifacts: `/tmp/antigravity_robust_interview_map_policy.{json,md}` and `/tmp/antigravity_robust_interview_full_gate.{json,md}` |
| Map model policy: Gemini primary, Sonnet rescue, DeepSeek audit | Codex | 2026-05-31 | Updated `backend/services/interview_map.py` so map/focus generation defaults to Gemini 3.5 Flash primary, Sonnet 4.6 direct rescue/checker, and DeepSeek V4 Flash advisory audit. Sonnet rescue no longer depends on DeepSeek completing first; DeepSeek audit can run in parallel and is attached as advisory `audit_review` when ready. Runtime maps now include `model_policy` and per-track `track_model` provenance; verified with `python3 -m py_compile backend/services/interview_map.py` |
| Medium-tier prompt repair + cost-aware rerun | Codex | 2026-05-31 | Added model-specific guardrails for follow-up wording, application-transfer dimensions, and map-track JSON shape, then reran the medium-quality harness on Gemini 3.1 Pro Preview, Gemini 3.5 Flash, DeepSeek V4 Flash, and Sonnet 4.6. Gemini 3.5 Flash improved from 53/60 avg 92.8 to 60/60 avg 99.2 with 7.9s avg latency; Sonnet on repaired prompts scored 60/60 avg 98.7 with 11.7s avg latency; DeepSeek V4 Flash improved from 55/60 avg 94.3 to 59/60 avg 98.3 but with 13.9s latency and one weakness-quality miss; Gemini 3.1 Pro improved to 58/60 avg 96.0 but had a 95s map-critic timeout. Artifacts: `/tmp/antigravity_medium_model_quality_deepdive_prompt_repair*.{json,md}` |
| Stencil-light correction for LiveKit room | Codex | 2026-05-31 | Reworked `/visualizer-livekit-room` after Yash clarified the desired reference was the boxed light-leak behavior, not animated neural graphics. Removed the SVG neural field, path/comet animation, scatter overlay, and ripple/event graphics; replaced them with a restrained central light field, dark stencil panels, edge glow around the stage/card surfaces, and a transparent question plate that lets light show through gaps. Official LiveKit Aura and anchored transcript behavior preserved; verified `npm run build` and browser smoke |
| Live room transcript layout cleanup | Codex | 2026-05-31 | Removed unnecessary center-stage diagnostic chips like `You said`, `monotonic fencing token`, and `Testing / delayed replica behavior` from `/visualizer-livekit-room`; center stage is now only the anchored question card plus a smaller bounded answer transcript drawer with a `Show live transcription` toggle and roughly five-line open height. Side rail renamed to `Turn history` with `Full transcript` affordance; verified `npm run build` and browser smoke |
| Live room question/transcript geometry fix | Codex | 2026-05-31 | Stabilized `/visualizer-livekit-room` center-stage geometry so the transcript drawer cannot eat into the question card. The stage body is now a three-row grid: question gets remaining height, transcript has bounded compact/open heights, review stays separate. Added length-based question typography classes (`short`/`medium`/`compact`/`dense`) so longer questions shrink instead of clipping; verified `npm run build` and browser geometry smoke that question text fits inside its card and transcript starts below it |
| Live room collapsible history focus mode | Codex | 2026-05-31 | Made `/visualizer-livekit-room` turn history collapsible so the central question/live-transcription block can take more width in focus mode; collapsed state becomes a slim `Turn history` handle on desktop and a compact handle on narrow screens. Increased question/stage light emphasis around the question card only, avoiding whole-page disco effects; verified `npm run build` and browser smoke for collapse state/classes |
| Live room transcript state separation + card ratio | Codex | 2026-05-31 | Split `/visualizer-livekit-room` live transcription from full transcript: live transcription is the volatile current-answer drawer below the anchored question, while full transcript is committed turn history in the side rail. The two toggles now use independent state, and question-card height is density-based instead of filling all available stage space; verified `npm run build` and browser smoke for independent toggles and no overlap |
| Live room 10-turn simulation + question focus pulse | Codex | 2026-05-31 | Expanded `/visualizer-livekit-room` from 3 sample turns to a 10-turn product-analytics interview arc, updated progress/status to reflect all 10 turns, and added a restrained asking-phase question focus pulse with slight non-question dimming; verified `npm run build` and browser smoke for 10 progress marks plus `focus-question` styling during Asking |
| Live room density + turn history rail fix | Codex | 2026-05-31 | Tightened `/visualizer-livekit-room` after screenshot review: stage now aligns to content instead of stretching a large empty middle, question cards use smaller density heights, live transcription is more compact, and the turn-history rail is now a proper header/list/action layout with the list owning scroll and the full-transcript button pinned to the bottom. Full transcript uses latest-first committed turns with real turn numbers; verified `npm run build` and browser smoke |
| Live room stacking history + question activation pass | Codex | 2026-05-31 | Fixed `/visualizer-livekit-room` history accumulation by changing timer-driven `review()` to a functional committed-state update; compact history now stacks the latest four committed turns. Collapsing turn history visibly increases question type, active question cards get non-ready phase `sync-glow`, and question text now has a translucent line-by-line ink plate for sharper white text over neural light; verified `npm run build` and browser smoke |
| Live room pitch-black prompt plate + outer stage glow | Codex | 2026-05-31 | Changed `/visualizer-livekit-room` so neural light no longer creeps inside the question card: the prompt plate is pitch black with no internal gradient/pseudo-layer and no per-line ink highlight, question-card min-heights were removed for closer content fit, and the breathing neural light moved to the central stage border via `lk-stage-edge-sync`; verified `npm run build` and browser smoke |
| Live room calmer Aura + floor ownership model | Codex | 2026-05-31 | Removed the Aura container's extra radiating neural glow while keeping the LiveKit Aura ring color. Added `data-floor`, top-chip floor copy, central floorline, and candidate-turn highlighting on the live-answer strip so AI turn vs candidate turn is explicit and woven into the existing AI/candidate zones; verified `npm run build` and browser smoke |
| LiveKit Aura live-room Next route | Codex | 2026-05-31 | Added `/visualizer-livekit-room`, a Next.js iteration of the live-room prototype using the official `AgentAudioVisualizerAura` instead of the generic standalone orb; tightened the central question typography, added a semi-transparent neural-fluid question card, kept the question anchored while the transcript toggles below, and preserved memory rail/candidate controls/closing overlay; verified `npm run build` and browser smoke with Aura canvas, transcript toggle, anchored question, and no console warnings/errors |
| Standalone live interview room prototype | Codex | 2026-05-31 | Added `antigravity-live-room-preview.html`, a new standalone iteration that applies the latest UI research direction: live-room architecture, peripheral AI presence rail, current-turn stage, compact memory rail, transcript drawer, candidate controls, simulated barge-in, answer commit/review transition, and calm report-prep closing; verified via local static server with state simulation, transcript drawer, closing overlay, and no console warnings/errors |
| Live interview UI research assignment brief | Codex | 2026-05-30 | Rewrote `ANTIGRAVITY_UI_RESEARCH_PRD.md` and `antigravity-ui-research-brief.html` as a researcher-facing context dossier rather than a prescriptive design plan; it asks the UI researcher to independently decide page segmentation, transcript behavior, turn-taking, AI presence, visibility boundaries, controls, error recovery, and ending based on product/system context and external research; verified HTML render through local static server with no console warnings/errors |
| Medium-tier model contract + quality deep dive | Codex | 2026-05-30 | Added `backend/test_medium_model_quality_deepdive.py` plus `/tmp/antigravity_llm_callsite_inventory.md` to inventory API call sites and test medium surfaces including weakness, discrepancy, reasoning behavior, application transfer, follow-up/challenge/sprint questions, prefetch, map focus planning, map track generation, and map critique. Five-case run: Sonnet 59/60 avg 96.2; Gemini 3.1 Pro 57/60 avg 93.5; Gemini 3.5 Flash 53/60 avg 92.8; DeepSeek V4 Pro 53/60 avg 90.1 with severe latency/timeouts; DeepSeek V4 Flash 55/60 avg 94.3 but one focus-plan failure/noise leak. Artifacts: `/tmp/antigravity_medium_model_quality_deepdive_combined.{json,md}` |
| Quality deep-dive for Haiku vs Gemini 3.1 Flash Lite small tier | Codex | 2026-05-30 | Added `backend/test_small_model_quality_deepdive.py`, a stricter qualitative harness across 6 resumes/cases and 8 small-tier surfaces: resume parse, concept extract, seed question, clarification, adapted follow-up, speculative follow-up, implementation-anchor extraction, and application-coverage classification. Two-repeat quality run: Gemini 93/96 passes, avg score 96.8, avg latency 1572ms; Haiku 86/96 passes, avg score 93.6, avg latency 2148ms. Resume parsing was 12/12 for both; Gemini was cleaner on anchors, seed questions, and application coverage. Artifacts: `/tmp/antigravity_small_model_quality_deepdive.{json,md}` |
| Haiku 4.5 vs Gemini 3.1 Flash Lite small-tier replacement evaluation | Codex | 2026-05-30 | Added `backend/test_small_model_replacement.py` and ran Haiku vs Gemini Flash Lite across 9 small-tier task types and 4 clean/noisy/weak/honest-gap cases. Two-repeat run: Haiku 69/72, Gemini 72/72; Gemini avg score 98.3 vs Haiku 94.4 and avg latency 1648ms vs 2397ms. Added anchor-extraction guard after discovering Haiku could return `""` plus explanatory text that the old extractor treated as an implementation anchor; post-guard rerun remained Gemini 36/36 vs Haiku 35/36. Artifacts: `/tmp/antigravity_small_model_replacement*.{json,md}` |
| Candidate deep-ocean diagonal caustics | Codex | 2026-05-30 | Added a restrained underwater-light caustic layer to `/visualizer-livekit-candidate`, with subtle diagonal bands concentrated from bottom-left and top-right behind the UI; opacity is tied lightly to mic energy and reduced-motion disables the drift; verified build and browser smoke |
| Candidate Aura stronger colorShift bands | Codex | 2026-05-30 | Increased `/visualizer-livekit-candidate` state-owned Aura `colorShift` values across all modes so Idle, Listening, Reviewing/Thinking, Question/Speaking, and Closing read more distinctly while staying deterministic; verified build and browser smoke across all phase buttons |
| Candidate Aura state-owned colorShift + reddish Question state | Codex | 2026-05-30 | Updated `/visualizer-livekit-candidate` so each phase has its own Aura `colorShift` plus a small mic-energy boost; Question/Speaking now uses a refined reddish ruby/rose palette instead of violet-blue; verified build and browser smoke |
| Candidate Aura deterministic phase palettes | Codex | 2026-05-30 | Updated `/visualizer-livekit-candidate` so Aura/background colors change by interview state instead of random cycling: muted blue idle, cyan/green listening, amber/violet thinking, violet/blue/rose speaking, green/blue closing; verified build and browser smoke |
| Candidate Aura color rollback | Codex | 2026-05-30 | Rolled `/visualizer-livekit-candidate` back from randomized multi-color cycling after Yash flagged it as too disco-club; now uses one restrained Gemini-like cyan/blue Aura with subtle violet/green background undertones, calmer field strength, and slightly stronger panels; verified build and browser smoke |
| Candidate-facing lightweight LiveKit Aura preview | Codex | 2026-05-30 | Added `/visualizer-livekit-candidate`, a separate leaner candidate-facing preview using the official LiveKit Aura with candidate metadata header, current question, response panel, mic test, lightweight hue ripples, compact notes, and Claim/Gap/Next Probe/Run Conversation simulation controls; removed internal/operator copy such as neural/color-control language and agent-floor diagnostic text; verified build and browser smoke |
| Isolated DeepSeek/Gemini prompt-contract probe + app-transfer/evaluator repair | Codex | 2026-05-30 | Added `backend/test_model_contract_probe.py` to call OpenRouter directly with production prompt families for app transfer, resume-focus fallback transfer, focus planning, and final evaluation. Probe passed 9/10: Gemini 3.1 Pro passed all cases; DeepSeek V4 Pro passed app transfer/eval/Apparao focus plan but failed messy-resume focus-plan JSON parsing. Added LLM-authored application-transfer fallback after primary anchor failure, using deterministic map/resume anchor selection only; repaired final evaluator rubric and added sanity calibration for impossible zero-score `NO HIRE` outputs |
| DeepSeek V4 Pro + Gemini 3.1 Pro 15-turn simulation rerun | Codex | 2026-05-30 | Re-ran the live interview simulation suite without Opus. DeepSeek V4 Pro as medium/large failed the first Apparao gate during trajectory-map prep with a timeout. Gemini 3.1 Pro as medium/large passed the Apparao gate and 2 of the 4 follow-on cases, but failed the vague-overclaim case by missing the application-transfer deadline and failed the messy-resume case during map prep; artifacts saved under `/tmp/antigravity_live_interview_sim_suite_{deepseek_v4_pro,gemini_31_pro}.{json,md}` |
| Gemini-style turn ripple effects for LiveKit Aura preview | Codex | 2026-05-29 | Enhanced `/visualizer-livekit-neural` so phase/turn changes go through `transitionPhase()` and spawn short-lived hue ripples in the background; ripples trigger from phase buttons, voice commands, mic-start transition, and conversation simulation, with phase-specific palettes and reduced-motion support; verified build and browser smoke |
| Full LiveKit Aura neural interview preview | Codex | 2026-05-29 | Added `/visualizer-livekit-neural`, a fuller interview-preview route mirroring the standalone neural canvas while embedding official `AgentAudioVisualizerAura` instead of the orb; includes phase controls, transcript/probe panels, palette controls, guided conversation simulation, local mic capture, Web Audio analysis, and browser speech-recognition commands that can change phase/color; mic energy drives Aura audio input plus surrounding Gemini-style glow/scatter/color CSS variables; verified build and in-app browser smoke |
| Official LiveKit Agents UI Aura test route | Codex | 2026-05-29 | Installed the real `@agents-ui/agent-audio-visualizer-aura` shadcn registry component plus dependencies/config and added `/visualizer-livekit`, an isolated Next route rendering `AgentAudioVisualizerAura` with phase controls, Gemini-like color controls, and a user-clicked local mic track for audio-reactive testing; verified `npm run build` and in-app browser route load with no console warnings/errors |
| Standalone neural visualizer test preview | Codex | 2026-05-29 | Added `antigravity-neural-visualizer-preview.html` as a clone of the standalone neural interview preview that replaces the orb with a canvas audio visualizer; supports Aura, Wave, Radial, Grid, and Bar modes, preserves voice-energy controls, phase transitions, guided conversation simulation, barge-in, closing, and finale; original preview untouched; verified script syntax plus in-app browser load/control smoke with no console warnings/errors |
| Gemini-inspired neural interview UI pass | Codex | 2026-05-29 | `app/interview/[session_id]/page.tsx`, `components/Waveform.tsx`, and `app/globals.css` now render the voice interview as a phase-driven neural conversation field: full-screen aurora/path aura, active-probe spotlight, active-turn glow/border beam, and richer idle/listening/thinking/speaking orb states; added `antigravity-neural-interview-preview.html` as a standalone backend-free interactive demo with phase controls, voice-energy simulation, evidence ripple, barge-in shockwave, closing mode, finale overlay, and guided conversation simulation; audio/API behavior untouched; verified with `npm run build`, browser render check, and script syntax validation |
| Agenda-governed 15-turn interview orchestration | Codex | 2026-05-29 | Added `backend/state/interview_agenda.py`, wired agenda-owned application-transfer/coverage/second-anchor routing, hard completion/coverage gates, fail-closed app-transfer surfacing, and question-aware 15-turn simulation gates; verified compile + agenda contract tests + one real Apparao 15-turn quality-gate pass, with full five-case rerun blocked by OpenRouter 402 budget depletion |
| Live interview simulation harness + Apparao gate run | Codex | 2026-05-29 | Added `backend/test_live_interview_simulation_suite.py` for real orchestrator/session simulations and ran the user-provided Apparao resume as the first gate. Fixed an application-transfer state race by awaiting `_generate_application_transfer()` inside the background pipeline. Apparao now completes E2E with app transfer and final report, but quality did not pass: slow map prep, retention/conversion tunneling, no pivot to dashboard/CV focus areas, and an overly harsh `NO HIRE` 2.5 verdict; the next four simulations were not fanned out yet |
| Gemini 3.5 Flash scaffolding probe + JSON contract repair | Codex | 2026-05-29 | Quality-first raw-output probe showed Gemini 3.5 Flash can produce strong resume/application/focus-planning content but was being hurt by scaffolding: incomplete/trailing-brace JSON, missing JSON mode on structured agents, and too-small output budgets; `LLMRouter` now has lenient model-authored JSON parsing plus one structured retry, JSON-critical agents request JSON mode, and map repair keeps launch-ready pass-1 LLM maps when later repair fails |
| Tier-aware interview model matrix | Codex | 2026-05-29 | Added `backend/test_tiered_model_matrix.py` to test small/medium/large candidates only on their intended interview call sites plus E2E tier combinations; current evidence favors Gemini 3.1 Flash Lite for small structured work, Sonnet 4.6 as the general medium workhorse, Opus 4.8 Fast for evaluation, and flags map preparation/application-transfer as architecture/prompt bottlenecks rather than solved by a simple model swap |
| Model bake-off + E2E fail-closed verification | Codex | 2026-05-28 | Added `backend/test_model_bakeoff.py`, verified OpenRouter IDs for Claude Haiku 4.5, Gemini 3.5 Flash, Gemini 3.1 Flash Lite, DeepSeek V4 Pro, and DeepSeek V4 Flash, ran structured task coverage across resume parse/seed question/weakness/discrepancy/reasoning/application-transfer/map prep, and completed a real API E2E with Gemini 3.1 Flash Lite; Gemini 3.1 Flash Lite was the only 100% success model in this run |
| Interview LLM/map fallback removal | Codex | 2026-05-28 | Removed heuristic/deterministic resume/map/question fallback behavior from the live interview path: resume parsing, seed question generation, trajectory map generation/critique/track hydration, background agent failures, speculative/clarification/follow-up generation, and no-prepped-question fast paths now fail closed instead of inventing generic questions; OpenRouter defaults updated to valid Claude model IDs; fallback contract tests inverted to assert disabled behavior |
| Sprint/agenda separation in live interview routing | Codex | 2026-05-28 | `backend/services/orchestrator.py` now treats sprints as persona/pressure stages only: sprint advancement no longer injects generic opener questions or clears staged map/application/coverage packets; Q0 and non-evidence routes no longer burn sprint evidence budget; application-transfer prep/promotion now keys off evidence-bearing turns and coverage routing waits for application transfer |
| Simulation conversation recap README | Codex | 2026-05-26 | Added `SIMULATION_CONVERSATION_README.md` as a re-onboarding artifact for the full simulation workstream: origin, philosophy shift, implementation history, current files/routes, QA contract, demo flows, known gaps, and next sprint direction |
| Simulation demo reliability sprint: summary-index admin + stable report assertions | Codex | 2026-05-16 | Added lightweight Redis-backed `sim_summary:*` / `invsim_summary:*` indexes so `/api/simulation/sessions` no longer hangs on full report blobs and survives backend restart; added explicit Reasoning Quality / Authorship Signal blocks to inline/share reports; hardened Playwright assertions around evidence truth and admin persistence; verified backend tests, build, 6-scenario e2e, and restart smoke |
| Simulation Evidence Ledger v1 + adaptive next-challenge contract | Codex | 2026-05-16 | Payment and inventory reports now include deterministic evidence ledgers mapping runtime tests, candidate artifacts, production gaps, authorship alignment, coverage score, and next adaptive challenge; regression tests assert strong/shallow ledger behavior; verified with py_compile, payment/inventory backend tests, build, and 5-scenario Playwright e2e |
| Simulation platform functionality hardening: persisted sessions + inventory scoring parity | Codex | 2026-05-16 | Payment and inventory simulation services now mirror state locally while saving to Redis, mark completed simulations for longer TTL, list persisted Redis-backed sessions in admin, expose `simulation_family`, and inventory now has payment-grade reasoning-quality scoring/caps/report evidence; added inventory backend tests and an inventory Playwright completion/report flow; verified with py_compile, payment tests, inventory tests, build, and 5-scenario e2e |
| Payment simulation QA hardening: core cockpit + first-pass validation semantics | Codex | 2026-05-16 | Removed visible voice/Gemini/audio controls from `/simulation`, collapsed duplicate raw gate errors into one clean assessment note, fixed validation scoring so a first-pass correct candidate with strong evidence can score Strong Hire, removed the artificial 82 cap for non-shallow coherent work, softened gate language toward senior-interviewer feedback, preserved caps for keyword-salad/correct-code candidates, and added e2e assertions for the de-voiced cockpit; verified with backend tests, build, Playwright e2e, and in-app browser smoke |
| Payment simulation reasoning-quality scoring contract | Codex | 2026-05-16 | Added deterministic artifact-quality scoring for causal reasoning, specificity, repetition, keyword-list shape, and production-twist handling; final reports now cap keyword-salad/correct-code candidates below Strong Hire and surface weak reasoning as unproved evidence; verified with backend tests, build, and 4-scenario Playwright e2e |
| Payment simulation evidence-contract hardening | Codex | 2026-05-16 | `/simulation` now uses backend-owned stage gates, starter-code baseline separation, code-diff validation, no premature score display, deterministic final scoring by default, validation/reflection finalization requirements, and Playwright coverage for empty click-through, starter-code abuse, and strong-candidate completion; verified with backend tests, `npm run build`, e2e, and live browser smoke |
| Standalone engineering simulation V1 + Realtime voice path | Codex | 2026-05-16 | Added `/simulation` payment idempotency case, `/api/simulation/*` session/test/finalize routes, temp-only Node test runner, scoring/reporting, Playwright e2e harness, and OpenAI Realtime WebRTC offer endpoint with frontend microphone controls; verified with backend tests, build, mocked Realtime contract, and simulation e2e |
| Interview UI static left rail + separate camera panel | Codex | 2026-05-08 | `app/interview/[session_id]/page.tsx` now constrains the desktop interview workspace to viewport height, keeps the left interrogator rail stable while the transcript scrolls internally, and renders the camera stream as its own panel instead of as an overlay under the AI orb; verified with `npm run build` and a route `curl -I` check |
| Final product-decision hardening: Cartesia-first, default fillers, LLM-final verdicts | Codex | 2026-05-07 | Hard-wired Cartesia as the permanent primary TTS provider, removed live context-aware filler selection for this release, fixed early communication-mode detection, preserved app-transfer/coverage as the elite redesign direction, changed coverage verdict logic from override to advisory evidence for the LLM evaluator, and updated regression/UI handling around dimension maps/verdict enums. |
| Deployed API-origin fallback fix + ProvenHire service compatibility check | Codex | 2026-04-23 | `lib/api.ts` now uses same-origin `/api` on deployed hosts when `NEXT_PUBLIC_API_URL` is unset instead of incorrectly calling `localhost:8000`; verified with `npm run build`. Also re-checked the service contract: ProvenHire still calls `ANTIGRAVITY_API_URL/api/start_interview` directly via `server/src/routes/aiInterviewAdapter.ts`, and `backend/services/orchestrator.py` remains backward-compatible because `start_session()` now runs strict `prepare_session_map()` before `start_prepared_session()`. |
| Frontend visual system overhaul + design-team UI port | Codex | 2026-04-22 | Ported the design-team Antigravity UI language into the real Next.js app: new shared design system in `components/design-system.tsx`, new visual tokens/animations in `app/globals.css`, upgraded `components/Waveform.tsx`, and redesigned `app/page.tsx`, `app/interview/[session_id]/page.tsx`, `app/report/[session_id]/page.tsx`, and `app/dashboard/page.tsx` while preserving live interview logic; `npm run build` passed |
| Repo audit docs extended through memory-highway + `lib/audio.ts` pass | Codex | 2026-04-17 | Documentation-only pass. Added/updated `repo_optimization_journal.md`, `bug audit.md`, and `PROJECT_STATE.md` with new systems-level findings around mixed memory layers, data-transfer hot paths, API/LLM call budgets, and `lib/audio.ts` control-plane behavior. No runtime code changed in this pass. |
| Verified-startup interview map + compact focus keys | Codex | 2026-04-15 | `backend/services/orchestrator.py` now awaits `_seed_first_question()` and `_build_interview_map()` before `start_session()` returns, verifies non-empty `interview_trajectory_map`, and traces a compact map preview; `backend/api/routes.py` now returns `trajectory_focus_areas`; `backend/services/interview_map.py` now compacts fallback/LLM focus keys so they don’t balloon into raw-resume sentences; `frontend/app/interview/[session_id]/page.tsx` no longer waits on a background map and instead expects the startup state to already contain it; verified with `python3 -m py_compile` and `npm run build` |
| Raw-resume focus packs + trajectory-map retrieval hardening | Codex | 2026-04-15 | `backend/services/interview_map.py` now builds focus areas from raw-resume seeds plus exact supporting snippets, treats those snippets as source-of-truth in the map prompt, and no longer falls back to an arbitrary first focus area when overlap is weak; `backend/services/orchestrator.py` now passes focus-context/snippets into speculative, clarification, discrepancy, attack, bank-followup, sprint-question, and sprint-opener generation; `backend/agents/followup_agent.py` now lets speculative refine/keep contend with a relevant map candidate and requires clearer in-question pivot phrasing; `frontend/lib/audio.ts` normalized to `endpointing=1500` / `utterance_end_ms=2800`; verified with `python3 -m py_compile` and `npm run build` |
| Short-answer rescue hardening + generic-packet demotion | Codex | 2026-04-15 | `backend/services/orchestrator.py` no longer lets synthetic/generic packets outrank rescue/speculative candidates; rescue now covers 1–18 word answers, generic staged fallback can be overridden by speculative/rescue, and explicit rescue trace events were added; verified with `python3 -m py_compile` and a live terse-answer simulation against the local backend |
| Short-answer rescue + refine-or-keep speculative loop | Codex | 2026-04-15 | `backend/services/orchestrator.py` now tries one bounded `short_answer_rescue` Haiku call before generic fallback when a tiny answer arrives with nothing staged; `backend/agents/followup_agent.py` speculative generation now supports structured `keep` vs `replace` decisions using the current cached speculative question instead of blindly overwriting it on every new partial snapshot; verified with `python3 -m py_compile` and helper sanity checks |
| STT timing retune to `endpointing=1500` / `utterance_end_ms=2800` | Codex | 2026-04-15 | After analyzing live run `061852df-d640-4a05-a962-4c1ce7fbc739`, adjusted Deepgram timing in `lib/audio.ts` to trim perceived dead air without undoing the calmer turn-commit architecture: slightly faster utterance-end handoff, slightly calmer `is_final` chunking; intended follow-up work remains short-answer rescue + refine-or-keep speculative generation |
| Calmer STT boundaries + throttled interim partial snapshots | Codex | 2026-04-15 | Rolled `lib/audio.ts` back toward Deepgram-led turn boundaries (`utterance_end_ms=3000`, long safety timeout only), started sending throttled interim `/partial_transcript` snapshots with `is_final` + `snapshot_seq`, updated `backend/api/routes.py` + `backend/services/orchestrator.py` for stale-snapshot-safe speculative handling, and adjusted `app/interview/[session_id]/page.tsx` so normal UtteranceEnd commits bypass the old defensive hold; verified with `python3 -m py_compile`, `npm run build`, and a live local smoke test that recorded `api.partial_transcript` events in telemetry JSONL |
| Full interview telemetry + per-session trace capture | Codex | 2026-04-15 | Added `backend/services/interview_telemetry.py` plus `/api/telemetry` and `/api/telemetry/{session_id}`; instrumented backend routes, orchestrator fast/slow paths, TTS pre-generation/cache, and frontend audio/interview events so live runs now capture API calls, latencies, route decisions, flush reasons, playback timing, stale/revision behavior, and bottleneck/error events into `backend/runtime/interview_traces/{session_id}.jsonl`; smoke-tested endpoint readback and verified with `python3 -m py_compile` and `npm run build` |
| Historical ElevenLabs live backend path restored | Codex | 2026-04-15 | Historical provider-debug note only. Superseded by 2026-05-07 final decision: Cartesia is primary forever; ElevenLabs is fallback only. |
| Packetized follow-up scheduling + immediate turn memory | Codex | 2026-04-15 | `backend/services/orchestrator.py` now uses `active_question_packet` / `prepped_next_packet` so current-thread follow-ups are deterministic and no longer starved by the next staged topic; committed answers now create immediate `history` skeletons and `_apply_staged_analysis()` enriches them in place by `turn_id`; seed staging now includes packet follow-ups; added `/api/tts_health` plus TTS runtime provider/config visibility; verified with `python3 -m py_compile` and a live sanity check where Turn 1 and Turn 2 both served `bank_followup_fast` |
| Follow-up continuity + anti-tunneling + STT calming pass | Codex | 2026-04-14 | `backend/services/orchestrator.py` now lets queued bank follow-ups beat generic staged pivots, preserves up to two follow-ups, and pivots sooner on repeated same-focus high-severity probing; `backend/agents/followup_agent.py` now seeds sprint questions from transition memory/topic anchors and discourages generic Sprint 3 prompts; `frontend/lib/audio.ts` and `app/interview/[session_id]/page.tsx` now gate early commit more conservatively and merge clustered finals with a 700ms settle window; old TTS note superseded by Cartesia-first policy; verified with `python3 -m py_compile` and `npm run build` |
| API base normalization + follow-up output hardening | Codex | 2026-04-14 | Added shared `lib/api.ts` so landing/interview/report/dashboard/audio all normalize `NEXT_PUBLIC_API_URL` consistently, restored landing-page `target_role` / `years_experience` validation, and hardened `followup_agent.py` so malformed LLM output is validated and replaced with route-specific fallback questions instead of leaking labels/JSON into the interview; verified with `python3 -m py_compile` and `npm run build` |
| Same-turn revision versioning + historical TTS default pass | Codex | 2026-04-14 | `backend/services/orchestrator.py` now assigns backend-managed `answer_version`s to same-turn revisions, drops superseded staged analyses, and prevents older background runs from overwriting newer revisions; old TTS default note superseded by Cartesia-first policy; verified with `python3 -m py_compile` and `npm run build` |
| Config precedence hardening + trajectory-map prompt hardening | Codex | 2026-04-15 | Added `backend/config/env_runtime.py` so TTS/model aliases resolve uniformly, changed `backend/main.py` so repo-root `.env` loads as base and `.env.local` overrides it; historical ElevenLabs override note superseded by 2026-05-07 Cartesia-first policy; tightened `backend/services/interview_map.py`; verified with `python3 -m py_compile` and map checks |
| Sprint continuity + anti-tunnel follow-up routing | Codex | 2026-04-14 | Added deterministic resume fallback parsing in `resume_agent.py`, focus-family tracking + finite contradiction/deflection budgets in `orchestrator.py`, and richer sprint handoff briefs / avoid-topic guidance in `followup_agent.py`; also fixed sprint handoff using the answered question instead of the next follow-up; verified with `python3 -m py_compile` and `npm run build` |
| Split-answer merge + backend staging hardening | Codex | 2026-04-14 | `app/interview/[session_id]/page.tsx` now merges late STT chunks into one candidate turn and supports same-turn revision with the same `turn_id`; `backend/services/orchestrator.py` now uses an ordered `prepped_turn_queue` instead of a single fragile staging slot; `lib/audio.ts` waits for audio readiness before playback; `EvaluationAgent` JSON example now includes `INSUFFICIENT_DATA`; verified with `python3 -m py_compile` and `npm run build` |
| Role/YOE-calibrated interview flow + report contract | Codex | 2026-04-14 | Added `target_role` + `years_experience` input on landing page, threaded calibration through `ResumeAgent` / `WeaknessAgent` / `EvaluationAgent`, added discrepancy levels (`none/suspected/confirmed`), report `untested_dimensions`, and a light breadth guard in `orchestrator.py`; verified with `python3 -m py_compile` and `npm run build` |
| Frontend turn-commit hotfix for mid-thought cutoff | Codex | 2026-04-05 | `frontend/lib/audio.ts` no longer lets CV directly flush utterances into the LLM path; `frontend/app/interview/[session_id]/page.tsx` now invalidates AI_THINKING turns as soon as the user resumes partial speech; verified with `npm run build` |
| Frontend `turn_id` stale-response protection | Codex | 2026-04-04 | `frontend/app/interview/[session_id]/page.tsx` + `frontend/lib/audio.ts`; sends `turn_id`, drops stale replies, invalidates on end/unmount, verified with `npm run build` |
| GM-2: Multimodal Turn Prediction (MediaPipe CV) | Antigravity | 2026-04-04 | `frontend/lib/vision.ts` + `audio.ts` fusion; Lip closure + Gaze prediction |
| GM-1: FloorManager + Transcript Accumulator + Barge-in | Antigravity | 2026-04-04 | `frontend/lib/audio.ts` refactored with interruption/abort support |
| Full project scaffold (all agents, router, session manager, API, Docker) | Claude Code | 2026-03-30 | See `/backend/` |
| Initial question bank seeded (3 questions) | Claude Code | 2026-03-30 | `data/question_bank/ml_questions.json` |
| Git initialized + first commit | Claude Code | 2026-03-30 | ✅ Done |
| GitHub private repo created + pushed | Antigravity | 2026-03-30 | ✅ Live at github.com/Yashwant-Bhyri/antigravity |
| AGENTS.md created (shared AI coordination protocol) | Claude Code | 2026-03-30 | ✅ Committed and pushed |
| Deepgram ASR fully wired (nova-3, streaming, partial+final callbacks) | Claude Code | 2026-03-30 | `backend/services/asr_service.py` |
| Orchestrator predictive prefetch on partial transcripts | Claude Code | 2026-03-30 | `on_partial_transcript()` in orchestrator |
| WebSocket endpoint `/stream/{session_id}` for audio streaming | Claude Code | 2026-03-30 | `backend/api/routes.py` |
| Historical ElevenLabs TTS implementation | Claude Code | 2026-03-30 | Historical initial implementation. Current provider policy is Cartesia-first, ElevenLabs fallback only. |
| Sprint progression (5Q per sprint, auto-advance 1→2→3) | Claude Code | 2026-03-30 | `orchestrator.py` |
| Persona switching (curious_lead → socratic_mentor → senior_peer) | Claude Code | 2026-03-30 | `orchestrator.py` + `followup_agent.py` |
| Interview termination (30 min or sprint 3 exhausted) | Claude Code | 2026-03-30 | `orchestrator._is_complete()` |
| Full interview evaluation at session end (Opus model) | Claude Code | 2026-03-30 | `evaluation_agent.score_full_interview()` |
| FollowUpAgent rewrite: 3 persona prompts, no RAG, resume-grounded | Claude Code | 2026-03-30 | `backend/agents/followup_agent.py` |
| POST /end_interview endpoint | Claude Code | 2026-03-30 | `backend/api/routes.py` |
| Frontend: sprint dividers, persona badge, progress bar, auto-redirect | Claude Code | 2026-03-30 | `frontend/app/interview/` |
| Report page: dimension scores, failure surface, strengths, risk flags | Claude Code | 2026-03-30 | `frontend/app/report/` |
| Switched LLM provider to OpenRouter (OpenAI-compatible, all models) | Antigravity | 2026-03-31 | `backend/models/llm_router.py` — uses `OPENROUTER_API_KEY` |
| Fixed TTS streaming API call (`convert` not `convert_as_stream`) | Antigravity | 2026-03-31 | `backend/services/tts_service.py` |
| Added CORS middleware + `load_dotenv()` to main.py | Antigravity | 2026-03-31 | `backend/main.py` |
| Fixed docker-compose paths (relative to infra/ dir) | Antigravity | 2026-03-31 | `infra/docker-compose.yml` |
| Project README written | Claude Code | 2026-03-31 | `README.md` at repo root |
| /tts POST endpoint (MP3 streaming response) | Claude Code | 2026-03-30 | `backend/api/routes.py` |
| /report GET endpoint (scores, weakness summary, hire rec) | Claude Code | 2026-03-30 | `backend/api/routes.py` |
| Next.js frontend — full scaffold (4 pages) | Claude Code | 2026-03-30 | `frontend/` |
| Landing page (resume input, start interview) | Claude Code | 2026-03-30 | `frontend/app/page.tsx` |
| Interview UI (live voice, transcript, waveform, mic pulse) | Claude Code | 2026-03-30 | `frontend/app/interview/[session_id]/page.tsx` |
| Report page (failure surface, weakness log, HIRE verdict) | Claude Code | 2026-03-30 | `frontend/app/report/[session_id]/page.tsx` |
| Recruiter dashboard (all sessions, ranked by verdict) | Claude Code | 2026-03-30 | `frontend/app/dashboard/page.tsx` |
| MicStreamer + speakText audio utilities | Claude Code | 2026-03-30 | `frontend/lib/audio.ts` |
| Waveform + MicPulse components | Claude Code | 2026-03-30 | `frontend/components/Waveform.tsx` |
| Prompt injection attempt detected + removed from frontend/ | Claude Code | 2026-03-30 | Fake AGENTS.md + CLAUDE.md placed by unknown source |

---

## DECISIONS LOG
> Settled decisions. Do not re-debate these unless Yash explicitly reopens them.

| Decision | Rationale | Date |
|---|---|---|
| Canonical trace bytes and redacted projection bytes are separate evidence products | `InterviewTraceV1` provenance source-reference order must survive serialization, so the exact unsorted `export_records()` JSON is persisted owner-only and independently verified. Any sorted/redacted projection is explicitly non-authoritative and cannot claim reconstruction integrity. | 2026-08-09 |
| CandidateActorV1 behavioral quality requires deterministic safety plus independent subjective review | The real-provider gate is capped at 30 calls and separates fact-ID scope, ownership, temporal, protected, correction, schema/transport, and lifecycle checks from naturalness, answer-class quality, and semantic human review. No model self-judge may be the sole oracle; missing dedicated credentials must produce blocked evidence rather than fixture claims. | 2026-08-09 |
| Dedicated experiment credentials resolve through Git primary-worktree metadata only | Isolated worktrees may not contain the owner-only `.env.qwen.local`; the loader may resolve Git's shared common directory or an explicit override whose basename is exactly `.env.qwen.local`, while never reading generic `.env`, logging values, or weakening native environment precedence and mode checks. | 2026-08-09 |
| Third-surface engagement is bounded and evidence-seeking, not a new long arc | V1 should expose at least one high-value deferred surface when the hydrated map has one, but it must not reopen endless routing or serve generic filler. Default budget is one `third_surface_probe`; a second is allowed only if the candidate's answer creates unresolved signal worth pursuing. Bad/abstract question families are skipped rather than repaired into production at runtime. | 2026-06-05 |
| Application transfer needs breadth first and one earned light depth probe | A single coverage question is too thin when the coverage map contains multiple useful dimensions. Require two evaluated app-transfer dimensions when available; ask one light depth probe only when a depth-eligible dimension has real signal. Bad/vague early answers get one anchor-recovery question before resume fallback, not an immediate resume-claim transfer. | 2026-06-02 |
| Silverline paid batches stop on semantic quality blockers, not only structural failures | A case can complete 15 turns with report-ready state and still be unsafe for production if it asks unsupported hidden internals, misroutes synthesis/second-anchor, or violates the role/fairness philosophy. Do not run the middle/adversarial Silverline batches until the strong-AI hidden-internals guard and routing rhythm are paid-confirmed. | 2026-06-02 |
| Technical depth must be earned from candidate answers, not inferred from resume hype | Resume claims decide where questioning starts, but the interviewer must establish ownership/scope before probing hidden implementation internals. Frame/clarify/explore should precede pressure on a new focus; implementation-depth probes are valid only after the candidate has actually opened that door or the snippets explicitly support it. | 2026-06-02 |
| Surface taxonomy should be map-authored, not inferred forever from role keywords | New focus plans should declare `surface_kind` for each sub-focus. Deterministic keyword checks remain compatibility safety nets only; validators and routing should increasingly consume typed map metadata. | 2026-06-01 |
| DeepSeek can audit question ladders but cannot block startup | DeepSeek V4 Flash is cheap enough to flag expected-space, voice, closed-choice, low-information, and prosecutor-streak risks. Its output is a signal for later Sonnet verification/repair, not a live gate. | 2026-06-01 |
| Same-question ladder follow-ups are a scarce high-value-surface tool | Immediate shallow/strong follow-ups should not be generated equally for every focus. The map's structured coverage/importance weights decide whether a surface deserves extra same-thread depth; low-value surfaces should recover briefly or pivot. | 2026-06-01 |
| Role/taxonomy keyword heuristics are safety nets, not the target architecture | Current product/CV/taxonomy word checks can prevent obvious drift, but they are brittle. The target should be typed role/surface metadata in the focus plan, with cheap advisory audits and Sonnet escalation only when deterministic signals and LLM review disagree. | 2026-06-01 |
| Realtime interaction moves are not assessment turns | Receipts, repeat, rephrase, simplify, continuation prompts, pause/resume, pacing, and bad-audio repair preserve conversational fairness and must not increment `question_count` or consume trajectory/action-deck moves. Counting assessment moves remain backend-approved technical probes such as mechanism, boundary, ownership, challenge, coverage, pivot, and synthesis. | 2026-06-01 |
| Action decks must include positive and neutral moves, not only attacks | Yash clarified that a state model made only of gaps, contradictions, weakness, stuck, and skip would recreate the adversarial drift the redesign is trying to modulate. Future answer signals and action decks must include validation, strength exploration, narrative invitation, continuation, and fair simplification alongside risk/challenge moves. | 2026-06-01 |
| Cartesia is the permanent primary TTS provider | Final Yash decision. Do not revert to ElevenLabs-first. ElevenLabs remains fallback only. | 2026-05-07 |
| Context-aware TTS fillers are future-version scope | Keep release fillers context-free/default-only until Yash explicitly reopens this. | 2026-05-07 |
| Application-transfer + coverage map is the core redesign direction | This must stay and be implemented at elite robustness, not treated as optional experiment. | 2026-05-07 |
| Coverage map informs final evaluation but does not override the LLM verdict | Yash is not comfortable making deterministic coverage the final hiring authority; LLM keeps final-context authority while coverage remains strong evidence/advisory. | 2026-05-07 |
| RAG/question-bank removal is approved | The redesign should continue with trajectory map/application transfer rather than the old RAG bank. | 2026-05-07 |
| Disengagement/save-face routing is approved | Keep skip/deflection handling and candidate-protective pivots. | 2026-05-07 |
| Sprints are persona/pressure stages, not agenda owners | The trajectory map, application-transfer, coverage, discrepancy, and weakness routes own question selection; sprint advancement must not inject generic opener questions over a staged agenda packet. | 2026-05-28 |
| Interview LLM/map failures must fail closed | Yash explicitly rejected generic heuristic/deterministic fallbacks. If resume parsing, map generation, map critique/hydration, or follow-up generation violates the LLM contract, surface the failure instead of continuing with invented fallback questions. | 2026-05-28 |
| Current bake-off winner for structured live-interview subtasks is Gemini 3.1 Flash Lite | In the 2026-05-28 bake-off, `google/gemini-3.1-flash-lite` was the only model with 100% success across the tested structured contracts and completed a real ready-map API E2E; keep other providers behind explicit task-specific testing before making them defaults. | 2026-05-28 |
| Model evaluation must be tier-aware | Yash clarified that small/medium/large candidates should only be tested on the interview subtasks they would realistically serve; do not use all-models-on-all-tasks bake-offs as the final selection evidence. | 2026-05-29 |
| Use Anthropic SDK for LLM calls (not LangChain) | Lower latency, direct control, fewer abstraction layers | 2026-03-30 |
| Tiered model routing (Haiku/Sonnet/Opus) | Cost + latency optimization — don't use Opus for simple tasks | 2026-03-30 |
| Redis for session state (not in-memory) | Stateless services, horizontal scaling, session survives restarts | 2026-03-30 |
| Strict prompt chain isolation (JSON-only between agents) | Prevents hallucination propagation and context bleed | 2026-03-30 |
| Multi-pass scoring (3 evaluations averaged) | LLMs are inconsistent; averaging reduces variance | 2026-03-30 |
| Start with FAISS for RAG, migrate to Pinecone for prod | Avoid cloud dependency in dev; easy swap via interface | 2026-03-30 |
| TTS provider: Cartesia first, always | Yash final-boss decision: Cartesia is the holy-grail primary provider everywhere; ElevenLabs is fallback only. | 2026-05-07 |
| LLM via OpenRouter (not direct Anthropic SDK) | One key for all models: Claude, DeepSeek, Gemini. Antigravity switched this. | 2026-03-31 |
| Single OpenRouter key is sufficient | Parallel calls to multiple models work with one key — stateless routing. Two keys only needed for separate billing. | 2026-03-31 |
| Frontend: Next.js 14 App Router + TypeScript + Tailwind | Better for recruiter dashboard (SSR) + interview page (real-time) in one framework | 2026-03-30 |
| V1 scope: full product (candidate + recruiter dashboard + reports) | Yash confirmed — build everything, deploy later | 2026-03-30 |
| Audio flow: client-side Deepgram SDK (NOT backend WS relay) | Lower latency, simpler architecture. Key exposed via /deepgram_token — acceptable for internal tool. Original decision reversed. | 2026-04-01 |
| Interview calibration should be role/level-relative, not a universal senior bar | Prevents modest ownership claims from being over-probed and lets reports mark under-tested dimensions as inconclusive rather than low | 2026-04-14 |
| Interim STT snapshots are speculative-only; canonical turns still require a real utterance boundary | Lets the backend prepare better follow-ups from live speech without allowing unstable interim text to fragment history or trigger immature interruptions | 2026-04-15 |
| With interim snapshots in place, `endpointing` should be tuned for chunk quality, not ultra-fast commit | We now rely on `utterance_end_ms` for actual turn commit; a slightly higher `endpointing` can reduce speculative churn without reintroducing the old interruption bug | 2026-04-15 |
| Short answers deserve one bounded LLM rescue attempt, then fail closed | Supersedes the old generic-fallback policy. Very short answers may get a fast LLM-authored rescue, but must not drop into deterministic sprint fallback questions. | 2026-05-28 |
| LLM token auditing is metadata-only by default | Token/cost observability must not store raw resumes, prompts, candidate answers, or model outputs. Use provider usage when available, estimates when missing, and hashes/char counts for debugging. | 2026-06-01 |
| Full prompt/result LLM quality capture is explicit opt-in only | Use `LLM_QUALITY_AUDIT=1` for controlled debugging runs. It may store raw resumes, candidate answers, prompts, and model outputs, so do not enable it casually in production. | 2026-06-01 |

---

## HANDOFF NOTES

- **→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-08-09**
  - Durable-evidence blocker fixed. Canonical trace: `/tmp/antigravity_complete_interview_runner_v1_control_20260809/complete_interview_runner_v1_canonical_trace.json` (SHA-256 `41a05c382f79e0492e91b5a78dd300a74b3efae29692d3e3714448c747cbb13e`, mode `0600`); it reloads and passes `InterviewTraceV1.verify_integrity()` with all 15 spoken pairs.
  - Redacted projection artifact: `/tmp/antigravity_complete_interview_runner_v1_control_20260809/complete_interview_runner_v1_shadow_artifact.json` (SHA-256 `2ab6ea51a815832bf86e5bf3a413b2fce1af498fcbc5b82b9ed1bffc690690c3`); run manifest SHA-256 `50fd979d99f5d0525fd5cde85b95c52ea79d2eef0eaf27b4f5393df9042b11f0`. The projection is explicitly non-reconstructive.

- CandidateActorV1 behavioral gate files are in `backend/test_candidate_actor_v1_experiment.py`, `backend/test_local_experiment_credentials.py`, `backend/services/local_experiment_credentials.py`, and `backend/candidate_actor_v1_provider_gate.py`. The repaired real-run command was `python3 -u backend/candidate_actor_v1_provider_gate.py --timeout-seconds 45 --rate-sleep-seconds 0`; it completed exactly 30 provider calls through OpenRouter (`anthropic/claude-haiku-4.5`). All 30 rows were rejected (15 schema/parse, 15 deterministic safety), so there is no behavioral acceptance or promotion claim. Redacted packets are under `/tmp/candidate_actor_v1_provider_gate_20260809T083703Z`; durable checkpoint/manifest hashes are `f8a0490293c1f01c106805a2c67f08baaea5344b087baa5561c3307e317bc698` and `40f68a474f47f8a58d5df19d69054e30800475430c85363cf26fb4bdaff611e5`. Tail truncation remains externally sealed and unproven.
> Time-sensitive notes from one AI to the other. Clear these once acknowledged.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-08**
- Replay voice QA is live at `/interview-room/replay`. It loads saved full-gate artifacts from `/tmp/antigravity_*full_gate.json` plus `backend/data/session_exports/*.json`; current local scan finds 29 replay cases. Starting a case creates a `replay_*` session and the normal locked live room can open it through `/interview-room/[session_id]`.
- Hard guard: `replay_*` sessions intercept `/state`, `/partial_transcript`, `/process_turn`, `/end_interview`, and `/report` before the orchestrator, so they do not call map generation, LLM routing, scoring, or report generation. QA reports are available at `/api/replay/qa_report/{session_id}` and write `/tmp/antigravity_replay_qa_<session_id>.{json,md}`.
- Verification completed: `python3 -m py_compile backend/api/routes.py backend/services/replay_interview_service.py`, `npm run build`, direct FastAPI ASGI smoke, scoped `git diff --check`, and `PLAYWRIGHT_NEXT_START=1 PLAYWRIGHT_BACKEND_PORT=8015 PLAYWRIGHT_FRONTEND_PORT=3015 npm run test:replay-room` (`2 passed`). Real Deepgram/TTS automated voice QA remains opt-in: generate a WAV with `npm run replay:wav -- /tmp/antigravity_replay_answer.wav`, then run `REPLAY_FAKE_AUDIO_FILE=/tmp/antigravity_replay_answer.wav npm run test:replay-voice`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-05**
- Live room integration smoke is green for the start/opening-question path. Real session `75a160a8-ce0f-4321-aa7d-424bb10e579f` was tested through `/interview-room/[session_id]` using backend `127.0.0.1:8001` and production frontend `127.0.0.1:3010`; the room hydrated from `/state`, showed `S. V. S. Apparao - Product Analyst`, started with fake media permissions, reached `Mic ready`, and rendered the backend opening question.
- Two local testing traps were fixed/documented: restart/rebuild the production preview if Next serves missing client chunks, and keep `3010` in backend CORS defaults for local production-preview smoke. `backend/main.py` now allows `http://localhost:3010` and `http://127.0.0.1:3010`.
- Added `tests/interview-room-live-smoke.e2e.spec.ts`. This is intentionally a smoke, not a full voice-e2e: it validates state hydration, media-gated start, and opening question/TTS path. It does not yet prove a real microphone answer travels through Deepgram, `/partial_transcript`, `/process_turn`, and turn-history commit.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-05**
- Final lock-in after the mini-suite: best product, messy resume, and trap overclaim are now the latest paid V1 readiness artifacts. Best product `/tmp/antigravity_v1_lock_best_product_fix3_20260605_full_gate.{json,md}` reached 15 turns, app transfer turn 5, coverage 6-7, second anchor 11, final `MAYBE 6.5`; current code now filters the standalone `ZERO_HONEST_ADMISSIONS` report risk if the evaluator emits it again. Messy resume `/tmp/antigravity_v1_lock_messy_resume_fix_20260605_full_gate.{json,md}` passed after app-transfer floor recovery, final `MAYBE 7.0`. Trap overclaim `/tmp/antigravity_v1_lock_trap_overclaim_fix_20260605_full_gate.{json,md}` passed after low-signal file/tool/team recall bans, final `NO HIRE 3.2`.
- Fixes made in this pass: history-aware app-transfer-served checks to prevent duplicate transfer, synchronous floor recovery using resume/focus fallback when transfer prep is late, Redis reconnect retry for closed local connections, report map-hydration confidence-limit metadata, deterministic filtering of standalone "zero honest admissions" risk, and question-quality/prompt bans for tool/team/file-format recall.
- Remaining V1 caveats: map prep is still slow (~97-102s in the latest messy/trap runs), app-transfer/coverage questions can still be overlong, `legacy_agenda_backup` remains trace noise though not served as generic fallback, and late-stage route cleanup after second anchor is safe but not yet senior-human optimal.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-05**
- V1 mini-suite status is now structurally green after two live fixes. `marketplace_growth`, `best_product`, and `strong_ai` all reached 15 turns, `history_len=15`, report ready, and complete finalization. Artifacts: `/tmp/antigravity_v1_gate_marketplace_hydrationfix_full_20260605_full_gate.{json,md}`, `/tmp/antigravity_v1_gate_best_product_20260605_full_gate.{json,md}`, `/tmp/antigravity_v1_gate_strong_ai_20260605_full_gate.{json,md}`.
- Two code fixes made during the gate: second-anchor reselection is now iterative/visited-set bounded to prevent recursion; async hydration now accepts structurally valid deferred V2 tracks with local style/legacy compatibility warnings instead of quarantining high-signal surfaces too aggressively.
- Do not interpret the green gate as perfect interview quality. Remaining issues are: map prep still slow and sometimes rescue/replacement-heavy; coverage questions can be long; `legacy_agenda_backup` route labels still appear in warning telemetry; best-product and strong-AI still show late second-anchor/same-surface warning patterns; local Postgres was unavailable during finalization, so DB-backed dashboard/persistence remains unverified.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-05**
- Implemented bounded `third_surface_probe` routing in `backend/services/orchestrator.py`. It is intentionally conservative: accepted map material only, no pending/quarantined tracks, no off-map generic questions, one turn by default, second turn only on depth-trigger evidence. This should help dashboard/ops/alternate surfaces get one sharp exposure without turning the interview into another holding pattern.
- Added deterministic agenda contracts for accepted deferred surface selection, self-rating rejection, quarantined-surface skipping, and second-turn trigger gating. Also aligned `PolicyCheckerAgent` and `question_quality` so `third_surface_probe` is treated as map-backed and expected-space aware.
- Verification green: `py_compile`, `backend.test_interview_agenda_contract`, `backend.test_interview_ripper_contract`, `backend.test_question_quality_contract`, `backend.test_policy_checker_agent`, and scaffolding audit `/tmp/antigravity_scaffolding_audit_20260605_022602.{json,md}`. No paid run was performed in this pass.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-02**
- Current paid gate status: do not run `best_product` / `strong_ai` yet. The requested one-at-a-time sequence was started with `marketplace_growth`, but it is still blocked before turn 1 by launch-map prep. Three attempts exposed and fixed separate scaffolding bugs (`unsupported/unspeakable` app-transfer validation, ladder object/list text shape drift, focus-plan schema recovery shrink), but the latest attempt still timed out during launch-track generation/repair after planning 4 focus areas.
- Latest artifacts: no-credit replay `/tmp/antigravity_app_transfer_floor_replay_20260602_224156.{json,md}`; paid failures `/tmp/antigravity_appfloor_marketplace_growth_20260602_full_gate.{json,md}`, `/tmp/antigravity_appfloor_marketplace_growth_fix1_20260602_full_gate.{json,md}`, `/tmp/antigravity_appfloor_marketplace_growth_fix2_20260602_full_gate.{json,md}`, `/tmp/antigravity_appfloor_marketplace_growth_fix3_20260602_full_gate.{json,md}`.
- Local verification after fixes is green: parser, map contract, map validation, scaffolding, ripper, agenda, policy checker, final report, question quality, and diff check. Remaining blocker is not app-transfer runtime now; it is launch-track generation/repair quality/latency under the V2 track contract.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-02**
- App-transfer routing now has a stricter breadth/depth floor. If two or more coverage dimensions exist, final coverage gates expect at least two evaluated dimensions. If a depth-eligible dimension is evaluated with real signal and no depth probe has been served, completion/report gating now flags `application_transfer_depth_probe_not_served`.
- New route `application_anchor_recovery` asks one concrete decision/metric/tradeoff question when early answers are too vague to form a grounded transfer anchor. It gets one chance; after that the existing resume-focus fallback/fail-closed path applies.
- Depth probe wording was softened from "candidate couldn't explain the mechanism" to "light mechanism/tradeoff/boundary follow-up at the level they showed." Verify the next paid marketplace/strong-AI gate for app-transfer coverage turns plus exactly whether an earned `coverage_depth_probe` appears naturally.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-02**
- SurfacePlanV2 is now wired into live map preparation. Default model is `openai/gpt-5.4-mini` through `OPENROUTER_SURFACE_PLANNER_MODEL`; `openai/gpt-chat-latest` is not default and should remain rescue/baseline only. The surface plan is first-class context for Gemini but allocation hints are soft only; do not turn `recommended_allocation_hint` into question counts or fixed budgets.
- Packet construction now adds deterministic `question_quality` metadata using `backend/services/question_quality.py`. `PolicyCheckerAgent` reports blocking bad-question families as `bad_question_readiness`, but does not steer yet. This catches variants like "Which component are you most confident with?", late "specific SQL script" recall, unsupported internals, and overpacked questions.
- Runtime stale scan still finds `legacy_agenda_backup` / `sprint_seed` labels in compatibility/safety code. Treat them as high-risk compatibility labels, not desired interview routes. A future cleanup can rename/retire them after confirming no UI/harness consumer depends on the exact route names.
- Latest no-credit artifacts: scaffolding audit `/tmp/antigravity_scaffolding_audit_20260602_202155.{json,md}` and saved-map replay `/tmp/antigravity_saved_map_replay_20260602_202231.{json,md}`. Both are green for paid confirmation; recent raw runtime map-policy artifacts replayed 4/4 through startup.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-02**
- Latest marketplace-growth diagnostic is the current head of the paid-simulation story. Do not run the 6-case silverline suite yet. The new case fixture and map-only/replay path are working, but the latest full run failed because `end_session()` launched duplicate concurrent final-report calls: one Sonnet report succeeded, a second Sonnet report failed with empty/malformed JSON. I patched `end_session()` to wait on `finalization_status=running` / `_finalization_inflight` instead of starting another report. Next useful paid step is exactly one fresh `marketplace_growth` full gate to confirm report finalization and then inspect whether the visible second-anchor question actually hits the dashboard/reporting surface.
- The generic focus-plan repair hook in `interview_map.py` is intentional and not Apparao/Tanvi hardcoding: for analytics roles, if a resume clearly has a concrete dashboard/reporting decision-use surface and the focus plan omits any `dashboard_reporting`/dashboard surface, it regenerates the focus plan once with a typed preservation hint. Map-only after this hook produced launch focus `marketplace_health_modeling_dashboards` with `stakeholder_reporting_platforms` as `dashboard_reporting`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-02**
- Silverline Phase 1 ran only `best_product` and `strong_ai` as requested. Do not continue to phases 2/3 yet. `best_product` structurally passed, but `strong_ai` exposed a real production-quality blocker: unsupported hidden technical-internals probes and premature synthesis/second-anchor rhythm warnings despite structural completion.
- I added a deterministic hidden-internals guard in `backend/services/interview_map.py` after the paid run. The next useful paid confirmation is a small rerun of `strong_ai` first, or `best_product,strong_ai` if budget allows. Proceed to `average_partial`/`honest_gap` only after strong AI no longer asks unsupported internals and no longer trips `synthesis_before_second_anchor`.
- Latest audit artifact: `/tmp/antigravity_scaffolding_audit_20260602_043959.{json,md}`. It reports 76 cases, 58 solved, 17 historical old-artifact unknowns, 1 low hardcoding-risk warning, no high-severity failures, and `green_for_paid_confirmation=true`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-01**
- Latest no-credit deterministic gate is green after REST hardening. Before spending on the full 15-turn suite, run one paid latest-code Apparao map-only check to verify `surface_kind`, `ladder_quality_audit_review`, Gemini primary behavior, and latency. If sane, proceed to the full 15-turn LLM-answer gate.
- REST hardening changed request model defaults and report/session shape coercion in `backend/api/routes.py`; keep this in place because it prevents malformed internal state from crashing report APIs.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-01**
- New maps may include typed `surface_kind` on sub-focuses. Boundary validators now consume that before falling back to old token rules. Please do not add more broad Product/CV keyword patches unless it is a temporary compatibility fix.
- `ladder_quality_audit_review` is advisory DeepSeek output only. It can inform Sonnet repair/escalation later, but must not block launch or become a required chain step.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-01**
- Same-thread `question_ladder` follow-ups now have an importance gate in `backend/services/interview_map.py`. Do not remove this when tuning voice rhythm; the product decision is that only high-value surfaces deserve extra immediate follow-up budget.
- Taxonomy classification remains deterministic and token-based for now. Treat it as a safety net. The intended next design is a typed role/surface taxonomy emitted by focus planning and used by validators instead of adding more role keyword lists.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-01**
- New architecture contract: `REALTIME_INTERACTION_ACTION_DECK_CONTRACT.md`. Read this before implementing action decks, Realtime voice tools, recovery service, or spoken-question logging.
- Yash's key clarification: the action deck must not become only a negative/adversarial state machine. The move set must distinguish non-counting interaction moves (`receipt`, `rephrase`, `simplify`, `continue`, `pause`, bad-audio repair) from counting assessment moves (mechanism, boundary, ownership, challenge, coverage, pivot, synthesis).
- Same-turn continuation is now a product contract: short/incomplete answers can receive "I hear you... is there one concrete thing you would add?" without incrementing `question_count`; confusion can receive a simpler rephrase without consuming a trajectory branch. Future state should store answer parts + interaction events and assess the merged answer.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-01**
- New quality-debug audit path: enable `LLM_QUALITY_AUDIT=1` for full prompt/result diagnostics. It writes to `backend/runtime/llm_quality/YYYY-MM-DD.jsonl` by default, or `LLM_QUALITY_DIR` if set. `LLM_QUALITY_CAPTURE_TEXT=1` is the default once quality audit is explicitly enabled; set it to `0` for shape/flag-only logs.
- Generate the quality report with `PYTHONPATH=. python3 backend/llm_quality_audit_report.py --out-prefix /tmp/antigravity_llm_quality_audit`. It joins token records by `call_id`, groups by agent/call-site/model, detects repeated prompt hashes, and flags malformed JSON, formatting noise, likely truncation, model meta/refusal language, verbose question outputs, and multi-question follow-up outputs.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-06-01**
- New LLM token audit path: all `LLMRouter.call()` calls now log metadata-only usage records under `backend/runtime/llm_usage/YYYY-MM-DD.jsonl` by default, or `LLM_USAGE_DIR` when set. Disable with `LLM_USAGE_AUDIT=0`.
- Generate a report with `PYTHONPATH=. python3 backend/llm_token_audit_report.py --out-prefix /tmp/antigravity_llm_token_audit`; add `--fetch-pricing` only when you want live OpenRouter price lookup. The report counts aggregate `llm_call` records to avoid double-counting attempts.
- The audit deliberately does not change prompts, models, token caps, or fail-closed behavior. Use it to identify candidates like per-answer triple scoring, repeated full-resume sends, map critic retries, and low max-token utilization before optimizing.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-31**
- New comparison route: `/visualizer-livekit-room-floor`. Do not collapse it back into `/visualizer-livekit-room`; Yash explicitly wanted multiple iterations preserved.
- This variant tests the floor-ownership philosophy: official LiveKit Aura on the left, candidate camera presence tile at top-right, committed turn history below it, pitch-black anchored question card in the center, live transcription as current utterance only, and full transcript as committed history only.
- Verification: `npm run build` passed; in-app browser smoke verified route load, run-session progression, stacked committed turns, and collapsed-history class state. Chrome-channel screenshots at 1440x900 and 1920x1080 were also captured after the center stage was adjusted to avoid lazy lower dead space.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-29**
- Yash explicitly asked to treat the live interview UI as an open canvas and push toward the closest Gemini-style expressive AI aura. Current implementation is frontend-only: phase-driven neural field, SVG path energy, active-probe spotlight, active-turn glow, and upgraded orb in `app/interview/[session_id]/page.tsx`, `components/Waveform.tsx`, and `app/globals.css`.
- Do not “fix” this by shrinking it back to the old restrained rail/transcript feel unless Yash asks. The intended direction is more cinematic and stateful. The audio floor manager, backend routes, TTS, and transcript processing were not changed.
- For visualizer exploration, `antigravity-neural-visualizer-preview.html` is a separate standalone clone of `antigravity-neural-interview-preview.html`; it swaps only the orb area for a canvas visualizer with Aura/Wave/Radial/Grid/Bar modes. Keep both files around while Yash compares directions.
- Yash rejected the hand-rolled visualizer as too distorted and explicitly asked for the real LiveKit UI component. `/visualizer-livekit` is now the correct benchmark route: it uses LiveKit's official `AgentAudioVisualizerAura` shader component and optional local mic capture. Prefer this direction for any production visualizer port.
- `/visualizer-livekit-neural` is the fuller version to compare against `antigravity-neural-interview-preview.html`: same neural-interview intent, official LiveKit Aura instead of orb, mic energy wired to both Aura audio track and Gemini-style background/glow variables, optional browser speech commands for phase/palette changes, and per-turn hue ripples triggered from every phase transition path.
- `/visualizer-livekit-candidate` is the leaner candidate-facing direction: candidate metadata at top, internal/operator wording removed, simpler effect stack, official Aura, mic test, lightweight hue ripples, compact notes, and only the simulator controls Yash wanted to preserve. It now also has a restrained deep-ocean diagonal caustic overlay behind the panels, concentrated bottom-left and top-right, so keep future background changes subtle and deterministic.
- Verification: `npm run build` passed; in-app browser rendered `/interview/visual-test-session` on the existing Next dev server with no console errors. Separate Playwright mobile screenshot pass could not run because the local Playwright Chromium binary is missing.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-29**
- New live-interview simulation harness: `backend/test_live_interview_simulation_suite.py`. It runs the real map prep -> prepared session -> `handle_transcript()` turns -> background wait -> `end_session()` path and writes `/tmp/antigravity_live_interview_sim_suite.json` plus `.md`.
- Apparao first gate now technically completes after fixing an application-transfer state race in `backend/services/orchestrator.py`: `_generate_application_transfer()` is awaited inside `_run_background_pipeline()` so coverage state is not overwritten by a later whole-state save.
- Do not treat the gate as product-quality passed. It asked 8 questions, served application transfer, and finalized, but still tunneled on retention/conversion, never exercised the dashboard or CV focus areas, and gave a harsh `NO HIRE` 2.5 verdict. I intentionally did not fan out the next four simulations because Yash asked to do that only if the first one went through well.
- Artifacts: `/tmp/antigravity_live_sim_first_after_appfix.log`, `/tmp/antigravity_live_interview_sim_suite.md`, `/tmp/antigravity_live_interview_sim_suite.json`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-29**
- Gemini 3.5 Flash should not be dismissed from the earlier failure table. Raw-output checks show high-quality application-transfer and focus-planning content when output budgets are large enough. Failures were partly scaffold-induced.
- Scaffolding changes made locally: lenient JSON-value parser + one malformed JSON retry in `backend/models/llm_router.py`; JSON mode on structured agents; larger output budgets for resume/application/map planning; map repair now keeps an already launch-ready pass-1 LLM-authored map if a later repair pass fails.
- Gemini 3.5 full map-track generation is still not reliable: one run preserved two high-score launch tracks then failed a lower-priority repair track; another failed first focus-track generation. Treat Gemini 3.5 as promising for planner/application-transfer experiments, not as the full map generator yet.
- Detailed report: `/tmp/antigravity_gemini35_scaffolding_report.md`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-29**
- Yash clarified the model experiment should be tier-aware, not every model on every call site. New harness: `backend/test_tiered_model_matrix.py`.
- Combined report is at `/tmp/antigravity_tiered_model_matrix_combined_report.md`; raw logs are `/tmp/antigravity_tiered_model_matrix.log` and `/tmp/antigravity_tiered_model_matrix_e2e.log`. The generated JSON/report at `/tmp/antigravity_tiered_model_matrix_results.json` and `/tmp/antigravity_tiered_model_matrix_report.md` reflect the E2E-only rerun after a harness bug fix.
- Cost correction from Yash: do **not** default to `anthropic/claude-opus-4.8-fast`; it is too expensive for this project loop. Current local default large tier is `deepseek/deepseek-v4-pro`, but keep the large token cap unchanged for now. The next model pass should explicitly test whether DeepSeek V4 and Gemini 3.1 Pro can replace some Sonnet 4.6 / large-tier work.
- The temporary OpenRouter key Yash pasted was not written to disk or logs by this work; the harness loads app env through `backend.main` and avoids printing secrets.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-28**
- Model bake-off artifacts are at `/tmp/antigravity_model_bakeoff_report.md`, `/tmp/antigravity_model_bakeoff_results.json`, `/tmp/antigravity_model_bakeoff.log`, and the E2E run summary is at `/tmp/antigravity_e2e_gemini31_session.json`.
- Recommendation from the current evidence: use `google/gemini-3.1-flash-lite` for fast structured live-interview subtasks and map prep experiments; keep `anthropic/claude-haiku-4.5` as a possible resume-parse quality specialist only if the extra latency is acceptable. `google/gemini-3.5-flash`, `deepseek/deepseek-v4-pro`, and `deepseek/deepseek-v4-flash` should not become defaults until their strict JSON/map-contract failure modes are fixed or isolated.
- `backend/main.py` now preserves inherited `OPENROUTER_*`, `ANTIGRAVITY_*`, `TTS_*`, `CARTESIA_*`, and `ELEVENLABS_*` process env vars after dotenv loading. This matters: before this fix, `.env.local` could silently override model bake-off/deploy process overrides.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-28**
- Yash explicitly killed the fallback philosophy for the interview system. Runtime interview code now refuses heuristic/deterministic fallback paths: resume parse output must be structured LLM JSON, trajectory-map planning/critique/hydration must succeed, agent failures are re-raised, and the fast path raises if no LLM/map-authored next question exists.
- `backend/models/llm_router.py` defaults are valid OpenRouter IDs and intentionally avoid Claude Opus 4.8 for cost. Keep future Gemini/DeepSeek changes evidence-driven, with DeepSeek V4 especially worth retesting as a Sonnet-cost reducer.
- Legacy deterministic map helper functions remain importable only to raise disabled errors; old fallback tests were inverted to assert that behavior.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-05-28**
- Yash identified a real overlap between generic sprint openers and the map/application-transfer agenda. `backend/services/orchestrator.py` now advances sprint/persona metadata without generating or overwriting the next question. Q0/non-evidence route kinds do not increment the evidence budget, application transfer is prepared after enough evidence-bearing turns, and coverage routing is tied to application-transfer serving rather than a bare turn number.
- Remaining product opportunity: introduce an explicit `agenda_phase` / `agenda_step_index` state machine so focus-area 1 → transfer → coverage → focus-area 2 → close is auditable instead of inferred from route priority.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-23**
- ProvenHire production flow is confirmed to call `ANTIGRAVITY_API_URL/api/start_interview` directly from `server/src/routes/aiInterviewAdapter.ts`; it does **not** depend on the Antigravity frontend’s browser-side two-step launch flow.
- The strict map-first backend work is still compatible because `backend/services/orchestrator.py:start_session()` now performs `prepare_session_map()` and only then `start_prepared_session()`.
- Separate deployment bug fixed: `lib/api.ts` now defaults to same-origin `/api` on deployed hosts instead of `localhost:8000`, which was the most likely cause of broken standalone Vercel behavior after a correct GitHub push.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-22**
- The Antigravity frontend now has a real shared visual system and a full design-language port across the home, interview, report, and dashboard surfaces.
- New shared layer:
  - `components/design-system.tsx`
  - `app/globals.css`
  - refreshed `components/Waveform.tsx`
- Important implementation detail: this was a UI-shell rewrite, not a behavioral rewrite. The live interview turn logic, stale-turn protection, barge-in handling, snapshot boot/resume flow, and report/dashboard data wiring were preserved.
- I also fixed one functional gap while porting: `startFreshInterview()` in `app/interview/[session_id]/page.tsx` now forwards `target_role` and `years_experience` from the loaded session snapshot instead of dropping calibration on fresh reruns.
- Validation: `npm run build` passed after the port.
- I have **not** committed or pushed these changes.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-17**
- The latest work is documentation-only and is being pushed: `repo_optimization_journal.md`, `bug audit.md`, `PROJECT_STATE.md`, plus this `AGENTS.md` note.
- New high-signal audit finding: the runtime no longer truly matches the “Redis-only state” story. Load-bearing live state still lives in orchestrator/TTS process-local sidecars (`_per_answer_scores`, `_partial_entities`, `_partial_snapshot_meta`, `_pipeline_inflight`, `_turn_pipeline_running`, `_speculative_locks`, `_prepped_audio`), so multi-worker or non-sticky execution is a real correctness risk.
- New `lib/audio.ts` findings logged: telemetry is now part of the hot path and playback telemetry is not consistently session-scoped; the current lexical echo-suppression cooldown can plausibly clip legitimate answer openings when the candidate repeats the just-asked project/technology terms.
- No code fixes were applied in this pass; this was an audit/logging checkpoint only.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- The latest raw-resume/focus-pack hardening is local only. Nothing was pushed.
- `backend/services/interview_map.py` now uses exact raw-resume snippets as the canonical grounding for each focus area and no longer returns an arbitrary first focus area when retrieval is uncertain. This was a real prompt-pollution risk in the earlier selector.
- `backend/services/orchestrator.py` now passes focus-context + exact snippets into speculative generation, discrepancy/clarification/attack probes, adapted bank follow-ups, sprint questions, and sprint openers. Speculative generation can now compare the rolling partial transcript against both `current_best_question` and a relevant interview-map candidate.
- Important mismatch to keep in mind: the live code still does **not** block `start_session()` on `_build_interview_map()`. It still uses `asyncio.create_task(...)` for `_seed_first_question()` and `_build_interview_map()`. Some recent `COLLAB.md` prose claimed `await asyncio.gather(...)`; that is not true in the current code.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-05**
- The root-cause hotfix for the "mid-thought fragment goes to LLM" bug is now in the frontend.
- `frontend/lib/audio.ts` no longer lets the CV score directly call `_flushUtterance()`. Vision now predicts only; meaning commit is still gated by Deepgram `UtteranceEnd` / safety flush.
- `frontend/app/interview/[session_id]/page.tsx` now invalidates the active turn immediately if new partial speech arrives while the app is still in `AI_THINKING`.
- This should stop the most visible failure mode: a prematurely committed fragment reaching TTS while the candidate has already resumed speaking.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-04**
- Frontend stale-response protection is now wired in `frontend/app/interview/[session_id]/page.tsx` and `frontend/lib/audio.ts`.
- Each committed utterance generates a `turn_id`, sends it through `/process_turn`, and silently drops late responses whose echoed `turn_id` no longer matches `currentTurnIdRef`.
- `endInterview()` and component unmount now invalidate the active `turn_id`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-14**
- Role/YOE calibration is now live in code.
- Landing page collects `target_role` + `years_experience`; `/start_interview` stores them in session state.
- `ResumeAgent` now emits richer ownership/claim context; `WeaknessAgent` and `EvaluationAgent` use that plus role/YOE to calibrate pressure.
- `DiscrepancyAgent` now distinguishes `suspected` from `confirmed`.
- `EvaluationAgent` can return `INSUFFICIENT_DATA` and `untested_dimensions`; report page renders those safely.
- `orchestrator.py` now has a light breadth guard to avoid repeatedly tunneling on the same weakness family without a confirmed contradiction.
- When `onBargeIn` lands in Gemini's floor manager, the intended invalidation hook is: `currentTurnIdRef.current = crypto.randomUUID()`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-14**
- The broken session `f905995d-6897-45c1-a491-0bf6f9ee8003` matched a real split-answer bug, not just user error.
- `app/interview/[session_id]/page.tsx` no longer turns a late `onFinal` fragment into a fresh answer turn; it now merges chunks into an `AnswerDraft`, waits a short settle window, and if another chunk arrives mid-processing it resubmits as a same-turn revision with the same `turn_id`.
- `backend/services/orchestrator.py` now treats same `turn_id` submissions as same-turn revisions and no longer relies on a single `prepped_turn_analysis` slot; queued analyses are ordered and consumed later, so `question_count` and `history` should stop drifting apart under fast successive turns.
- The exact UI regression path was already present in the `6c7fea5` interview-page changes (`processingRef` + `pendingFinalRef` replay path). `7e9b63e` made turn boundaries more likely to surface under load, but it was not the sole cause.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-14**
- The dummy-candidate session `6132c99e-34f4-4c05-a462-66bf1248d50b` exposed two real behavior issues:
  - sprint transitions still felt cold
  - contradiction pressure on the Wondershare internship was allowed to dominate too long
- Fixes now in code:
  - `resume_agent.py` backfills parsed resume structure heuristically when the LLM parse is sparse, so follow-ups and sprint openers have stronger grounding even when the small-model parse underperforms
  - `orchestrator.py` now tags each turn with a focus family and applies finite budgets for repeated deflection / contradiction on the same focus before forcing broader exploration
  - `_maybe_advance_sprint()` now uses the question the candidate actually answered when building sprint handoff memory; previously it could accidentally hand off the next follow-up question instead
  - `followup_agent.py` now gets continuity briefs + over-probed topics to avoid so sprint openers/questions feel more connected and less cold

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-14**
- I took the remaining backend robustness fixes directly.
- `backend/services/orchestrator.py` now carries a backend-managed `answer_version` for each committed same-turn revision, stores `latest_turn_versions` in Redis session state, and discards superseded staged analyses before they can mutate canonical history. This removes the old timing assumption where an older background pipeline could still win by finishing later.
- `_pipeline_inflight` is now keyed by `(session_id, turn_id, answer_version)`, so only exact duplicate work is suppressed; same-turn revisions are still allowed to run.
- Superseded by 2026-05-07 final decision: Cartesia is now the permanent primary TTS provider; ElevenLabs is fallback only.
- Verified with `python3 -m py_compile` and `npm run build`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-14**
- I fixed the remaining integrated-review issues directly.
- Added shared `lib/api.ts` so frontend fetches now normalize `NEXT_PUBLIC_API_URL` consistently to include `/api`; this is now used by the landing page, interview page, report page, dashboard, and `lib/audio.ts`.
- `backend/agents/followup_agent.py` now validates cleaned LLM question output before returning it. Serialized JSON strings with a `question` field are extracted, label-only / blob-like outputs are rejected, and each generation path now has a route-specific fallback question instead of trusting `_clean_question_output()` blindly.
- `app/page.tsx` once again requires `target_role` and `years_experience` before interview start, restoring the role/YOE calibration contract.
- Verified with `python3 -m py_compile` and `npm run build`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-14**
- I took a direct pass on the persistent live-interaction complaints: missing follow-ups, same-concept tunneling, vague Sprint 3 starts, over-eager STT commit, and TTS provider drift.
- `backend/services/orchestrator.py` now prioritizes queued bank follow-ups ahead of generic staged sprint pivots, preserves up to two follow-up templates, and keeps remaining follow-ups alive after a `bank_followup_fast` turn instead of overwriting them immediately. I also tightened repeated same-focus pivoting for high-severity non-recovering turns.
- `backend/agents/followup_agent.py` now queries sprint seeds using transition memory + latest topic anchor + weakness hints, and the sprint prompts explicitly ban generic stock design questions unless they match candidate context.
- `frontend/lib/audio.ts` no longer treats speculative early-commit like a forced flush: early-commit now requires a substantive utterance, Deepgram end-of-utterance timing is looser, and the hard cap is longer. `app/interview/[session_id]/page.tsx` now uses `ANSWER_SETTLE_MS = 700` so clustered final chunks merge before `processTurn`.
- Superseded by 2026-05-07 final decision: Cartesia is now enforced as project policy; ElevenLabs is emergency fallback only.
- Verified with `python3 -m py_compile` and `npm run build`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- I implemented the next architectural fix directly in `backend/services/orchestrator.py`: the interview loop now distinguishes the **current thread** from the **next parked topic** via `active_question_packet` and `prepped_next_packet`.
- The important behavioral change is that `handle_transcript()` no longer eagerly consumes `prepped_next_question` if the current question still has deterministic follow-ups left. That next topic stays parked until the current thread is exhausted or pivoted away from.
- Fast-path memory is now immediate: committed answers create a `history` skeleton with `analysis_status=\"pending\"`, and `_apply_staged_analysis()` enriches the same turn in place by `turn_id` instead of returning early on duplicates.
- I also added `TTSService.status_snapshot()` and `/api/tts_health` so we can see the live provider/config state during runtime debugging instead of guessing.
- Verified with `python3 -m py_compile`, `curl /api/tts_health`, and a live sanity check where Turn 1 and Turn 2 both served `bank_followup_fast` rather than generic sprint fallback.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- Historical note: this fixed a former ElevenLabs mismatch before Yash's 2026-05-07 provider reversal.
- Current source of truth: Cartesia is primary regardless of former ElevenLabs-debug context.
- `backend/main.py` now resolves `.env` and `.env.local` from the project root, loads `.env` first with override, then loads `.env.local` only to fill missing values.
- As of 2026-05-07, expected healthy runtime is Cartesia-first; `/api/tts_health` should report `provider=cartesia` when Cartesia credentials are present.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- I added full interview telemetry capture for the next live tests.
- New trace sink: `backend/services/interview_telemetry.py`, storing JSONL under `backend/runtime/interview_traces/{session_id}.jsonl`.
- New endpoints:
  - `POST /api/telemetry` for client-side/browser events
  - `GET /api/telemetry/{session_id}` for summary + recent events
- Instrumented:
  - backend API request timings
  - orchestrator fast-track / background-pipeline decisions and latencies
  - TTS pre-generation + cache hits
  - frontend floor transitions, flush reasons, processTurn/prefetch timings, hold/revoke behavior, and playback events
- Verified with a smoke event round-trip, `python3 -m py_compile`, and `npm run build`.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- I implemented Yash’s “send the real-time transcript too, but don’t let it prematurely act” idea directly.
- `lib/audio.ts` now sends throttled interim `/partial_transcript` snapshots with `turn_id`, `is_final`, and `snapshot_seq`; the backend still treats them as speculative-only.
- I rolled the frontend back toward the calmer STT behavior Yash preferred: Deepgram owns turn boundaries again (`utterance_end_ms=3000`), the custom early-commit/hard-cap commit path is gone, and only a long safety timeout remains as a defensive fallback.
- `backend/services/orchestrator.py` now rejects stale partial snapshots by sequence and uses the richer stream only for speculative prep / entity accumulation. Canonical turn history still only mutates in `handle_transcript()`.
- `app/interview/[session_id]/page.tsx` now skips the old defensive TTS hold on normal UtteranceEnd-backed finals; only safety-timeout commits remain defensive.
- Smoke-tested live against the running backend: both interim and final partial snapshots returned `200`, and the per-session JSONL trace recorded the new fields correctly.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- After reviewing live run `061852df-d640-4a05-a962-4c1ce7fbc739`, I retuned `lib/audio.ts` again:
  - `utterance_end_ms: 3000 -> 2800`
  - `endpointing: 1200 -> 1500`
- Reasoning: the calmer architecture fixed the interruption problem, so we can now shave a little dead air while also reducing `is_final` churn. Since interim snapshots already feed speculative prep, endpointing no longer has to be hyper-aggressive.
- Current speculative prompt context is still intentionally light: accumulated current-turn partial transcript + last question + sprint/persona + trimmed resume context. It does not yet include candidate-model memory or refine-or-keep logic.
- Later UX idea to keep in mind: when we intentionally pivot topics, the generated final question should include a natural bridge phrase in the same spoken utterance rather than using a separate filler fragment.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- I implemented the next two missing pieces:
  - `short_answer_rescue` in the fast path
  - refine-or-keep speculative generation
- `backend/services/orchestrator.py` now tries one bounded Haiku rescue call for short answers when nothing is staged, instead of falling straight into `sprint_fallback`.
- `backend/agents/followup_agent.py::generate_speculative()` now takes `current_best_question` and can return structured `keep`/`replace` decisions, so new partials refine the current speculative candidate instead of blindly overwriting it.
- I kept the speculative prompt intentionally lightweight; it still does not pull in full candidate-model memory or broad history on every interim pass.
- Verified with `python3 -m py_compile` and helper sanity checks.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-15**
- Follow-up on the terse-answer failure mode: the rescue path was present, but generic packets were still outranking it.
- `backend/services/orchestrator.py` now:
  - stops synthetic packet reconstruction from injecting `_FALLBACK_FOLLOWUPS`
  - refuses to prioritize bank follow-ups when the active packet route is `sprint_fallback` or `unknown`
  - lets same-turn speculative candidates beat a generic staged fallback
  - expands `_short_answer_rescue_eligible()` to cover 1–18 word answers, so `"Mostly cost."` / `"Latency."` are no longer excluded
  - emits explicit rescue telemetry events for attempt/success/timeout/failure
- Live terse-answer simulation improved materially:
  - `"Mostly cost."` now hit `route_kind="short_answer_rescue"` with a grounded cost-specific follow-up
  - `"Quality tradeoffs."` then hit `clarification_fast`
- Remaining gap: the first 1–2 turns can still miss when nothing useful is staged yet; that is now a cold-start preparation problem, not the old generic-packet precedence bug.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-22**
- I hardened the interview-map startup path for the current OpenRouter/Qwen-in-China environment.
- `backend/services/orchestrator.py` now lets a deterministic map land instead of failing startup when `generate_interview_map()` times out.
- `backend/services/interview_map.py` now builds its focus-source text from work/research experience instead of the whole raw resume blob, filters out contact/education/awards/skill-bucket metadata, shortens the seed-extraction LLM prompt, and logs timeout failures with exception type.
- Important current state: the map quality on raw noisy resumes is much better in fallback mode now, but the LLM-backed map path still times out frequently enough that deterministic fallback remains common on this network/provider path.
- Verified on Yash's raw pasted resume: the old bad fallback focused on phone number / degree / scholarship lines; the improved fallback now focuses on work like the AIGC video pipeline, semantic UI-to-latent interface, Optek model work, classifier optimization, and benchmark framework.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-22**
- I pushed the interview-map robustness work one level further.
- The map builder now returns `track_source` per focus area plus `pending_hydration_focus_keys`, and `orchestrator.py` schedules background hydration for any focus tracks that had to fall back during startup.
- Verified behavior on Yash's raw resume:
  - startup now lands a correct LLM-selected focus list like `Filmora AIGC Internship`, `ML Feature-Map Control System`, `Semantic UI-to-Latent Interface`, and `TinyML Audio Pipeline`
  - after a short wait, the saved session state showed some tracks upgraded to `track_source="llm"` (`Semantic UI-to-Latent Interface`, `TinyML Audio Pipeline`) while slower ones remained deterministic
- This means the map is no longer "one shot or bust" at startup. It can now improve itself after boot instead of staying permanently degraded.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-22**
- I smoothed two product-critical UX seams in the live interview loop.
- `backend/agents/followup_agent.py` now biases the startup seed follow-up toward a warm, context-first phrasing that names the specific experience before probing contribution. Verified live on Yash's raw resume: the pre-seeded first follow-up is now `I’d love to start with your TinyML Audio Classification Pipeline — what part of that work felt most yours?`
- `backend/services/orchestrator.py` now detects the last two questions of Sprint 3 and decorates those questions with explicit runway language (`last_two` / `final_question`). The fast-track response payload now also includes `closing_phase` and `questions_remaining` so the frontend can later render a matching visual cue instead of relying only on spoken wording.
- Important current state: the focus-area selection on Yash's raw resume is now good (`Filmora AIGC Internship`, `ML Feature-Map Control System`, `Semantic UI-to-Latent Interface`, `TinyML Audio Pipeline`), but this particular startup still returned deterministic focus tracks after 8s. So the product feel is better immediately, while the remaining technical gap is still LLM branch hydration reliability on the current provider/network path.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-23**
- I hardened the interview-map core rather than just tuning prompts.
- `backend/models/llm_router.py` now recovers JSON arrays from noisy model output, which matters because focus-seed extraction expects an array and the old last-resort parser only looked for `{...}` objects.
- `backend/services/interview_map.py` now fixes a real segmentation bug where bullet lines like `Engineered ...` or `Built ...` could be misread as new work headers. Fallback seed extraction is now detail-first: it prefers artifact labels from bullet content (`Feature-Map Control System`, `TinyML Audio Classification Pipeline`, `Multi-Modal Benchmark Framework`) before broad role headers.
- Deterministic fallback tracks are also stronger now: they pull concrete technologies and constraints out of the seed snippets (`Google ADK`, `TensorFlow Lite-Micro INT8`, `OCR`, `95%+`, `<10 ms`, etc.) so a fallback map is still interview-worthy, not just technically present.
- Important current state: yes, the map still hydrates in background via `pending_hydration_focus_keys` + `hydrate_interview_map_tracks(...)`, but the remaining weakness is still provider-sensitive LLM branch hydration. The default map quality when hydration misses is significantly better now, which is the correct robustness direction for a load-bearing component.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-23**
- I added the user-requested hybrid fallback layer to the map pipeline.
- `backend/services/interview_map.py` now has a second tiny `small`-tier artifact extractor that runs with a very constrained prompt when the primary focus-seed pass is thin. This extractor is artifact-only: it asks for things like pipelines, interfaces, benchmarks, classifiers, and control systems, and explicitly forbids education/awards/locations/role-title clutter.
- I also added overlap-based dedupe so fallback seeds no longer keep near-duplicates like `TinyML Audio Classification Pipeline` plus `Audio Classification Pipeline`.
- Added a regression check in `backend/test_interview_map_fallback.py`; verified with `python3 -m backend.test_interview_map_fallback`.
- Important current state: the strongest remaining issue is still not seed extraction, it is LLM-authored branch hydration reliability under the current provider/network path. The default map that exists before hydration is now much healthier than before.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-23**
- I fixed the next interview-map failure mode: evidence misbinding inside deterministic tracks.
- `backend/services/interview_map.py` now binds `resume_snippets` from the best matching work entry before it ever falls back to generic resume-unit scoring. This stops focus areas like TinyML from inheriting unrelated Filmora/AIGC snippets and stops benchmark work from pulling `Top Skills` lines.
- I also fixed the deterministic tech selector so family-aware priority survives all the way through ranking. Before this, TinyML could still ask about `Google ADK` or `MediaPipe Audio`; after the fix it correctly centers `TensorFlow Lite-Micro INT8`, and feature-map work correctly centers `Google Veo 3`.
- Added a broader contract test in `backend/test_interview_map_contract.py` and verified:
  - `python3 -m py_compile backend/services/interview_map.py backend/test_interview_map_fallback.py backend/test_interview_map_contract.py`
  - `python3 -m backend.test_interview_map_fallback`
  - `python3 -m backend.test_interview_map_contract`
- I also ran an isolated live smoke against a clean local backend on `127.0.0.1:8011`:
  - `POST /api/start_interview` returned `200`
  - stored map contained the intended four focus areas from Yash's resume
  - live `process_turn` on the TinyML path produced the exact map-grounded short-answer prompt: `On TinyML Audio Classification Pipeline, what specific part of TensorFlow Lite-Micro INT8 are you referring to?`
- Important current state: deterministic fallback quality is now substantially stronger and locally verified. Background hydration is still provider-latency-sensitive, but the startup map is no longer embarrassing or off-artifact when hydration misses.

**→ TO: Claude Code, Antigravity | FROM: Codex | Date: 2026-04-23**
- I implemented the architecture Yash explicitly asked for: **prepare the interview map first, validate it, persist it, then and only then allow interview start**.
- Backend changes:
  - `backend/services/interview_map.py`
    - added branch-provenance tracking (`llm_branch_count`, `fallback_branch_count`, `llm_branches`, `fallback_branches`)
    - added `validate_interview_map(...)`
    - fixed hydration so upgraded focus areas keep their provenance metadata
    - strengthened the slow hydration path with longer timeouts and a large-tier retry
  - `backend/services/orchestrator.py`
    - added a strict preparation gate with ~120s budget
    - `prepare_session_map(...)` now initializes the session and blocks on a validated rich map
    - `start_prepared_session(...)` now refuses to start unless `interview_map_status == "ready"`
    - launch no longer treats deterministic fallback as startup success
  - `backend/api/routes.py`
    - added `POST /prepare_interview_map`
    - added `GET /interview_map_status/{session_id}`
    - `POST /start_interview` now accepts `prepared_session_id`
  - frontend launch updated in `app/page.tsx` and fresh-rerun flow updated in `app/interview/[session_id]/page.tsx` to use the two-step contract
- Verification:
  - `python3 -m py_compile backend/services/interview_map.py backend/services/orchestrator.py backend/api/routes.py backend/test_interview_map_validation.py`
  - `python3 -m backend.test_interview_map_fallback`
  - `python3 -m backend.test_interview_map_contract`
  - `python3 -m backend.test_interview_map_validation`
  - `npm run build`
- Live strict-flow verification on a clean local backend (`127.0.0.1:8011`):
  - first strict prep failure surfaced real unmet richness constraints and exposed a provenance bug
  - after fixing that bug and strengthening hydration, `POST /api/prepare_interview_map` returned `200`
  - ready result on Yash's resume:
    - `llm_focus_count = 4`
    - `rich_focus_count = 4`
    - `pending_focus_keys = []`
  - `POST /api/start_interview` with the prepared session id then returned `200` and served rich LLM-authored preview branches
- Important current state: the product contract is now finally aligned with Yash’s intent. We no longer launch just because a deterministic fallback map exists. The interview starts only after a validated rich map is ready.

**→ TO: Antigravity | FROM: Claude Code | Date: 2026-03-30**
- Full frontend is live in `frontend/`. Next.js 14, App Router, TypeScript, Tailwind.
- ⚠️ A prompt injection attempt was found in `frontend/AGENTS.md` + `frontend/CLAUDE.md` — removed. Be vigilant about unexpected files appearing.
- `/sessions` list endpoint is missing from the backend — the recruiter dashboard page exists but will show empty until this is built.
- 4 tasks are ready to pick up from "In Progress" above — RAG wiring is the highest priority (no blockers).
- Remaining open questions: LangGraph vs raw asyncio (Q5).

_Acknowledge by clearing this note after reading._

**→ TO: All Agents | FROM: Codex | Date: 2026-06-02**
- Semantic second-anchor routing has been patched after the marketplace-growth diagnostic. The runtime now has a surface-level selector (`next_secondary_surface`) so a second anchor targets `focus_key + sub_focus_key/surface_kind`, not just a parent focus. This prevents administrative "second_anchor" turns from drifting back into the old taxonomy/attribution surface when dashboard/reporting/ops was the intended acceptance surface.
- The selector intentionally does not blindly prefer any different focus: different focus gets a ranking bonus, but high-value same-parent dashboard/ops can beat weak off-role OCR/CV. This is important for Product Analytics Engineer/Product Analyst resumes.
- Map selection now accepts preferred sub-focus/surface kind and returns surface metadata. Orchestrator packets/history preserve `surface_kind`, and second-anchor dedup recovery reselects from the intended semantic target instead of clearing the packet and falling into generic map selection.
- Verification is green: compile, agenda contracts, policy checker contracts, parser contracts, scaffolding contracts, map contracts, and map validation.
- Next paid step is still exactly one fresh `marketplace_growth` full gate to confirm visible dashboard/reporting/ops second-anchor engagement and stable finalization. Do not run the full silverline batch before that.

**→ TO: All Agents | FROM: Codex | Date: 2026-06-02**
- Ran a paid isolated SurfacePlanV2 model probe for the proposed pre-map recommendation layer. Artifact: `/tmp/antigravity_surface_planner_probe_20260602_190718.{json,md}`.
- Compared `openai/gpt-5-mini`, `openai/gpt-5.4-mini`, and `openai/gpt-chat-latest` on six resumes. Task was focus areas -> sub-focuses -> testable surfaces -> demoted off-role/risky surfaces, with no question generation.
- Result: use `openai/gpt-5.4-mini` as the current default candidate for SurfacePlanV2. It produced complete, grounded six-case plans at about `$0.062` estimated total cost and about `14.7s` mean latency. Chat Latest was faster but about `$0.333` for the same six calls, so reserve it for rescue/baseline. `gpt-5-mini` is too thin despite being cheap.
- Important guardrail: `recommended_allocation_hint` is soft only. It must never directly set question counts or interview budgets.

**→ TO: All Agents | FROM: Codex | Date: 2026-06-02**
- Ran the final edge-only SurfacePlanV2 probe requested by Yash: `openai/gpt-5.4-mini` only, five new problematic resumes, 10k max-token cap. Artifact: `/tmp/antigravity_surface_planner_probe_20260602_191323.{json,md}`.
- Result: 5/5 valid JSON, mean score `99`, min score `95`, mean latency about `15.4s`, estimated total cost about `$0.056`.
- Human inspection: GPT-5.4 Mini correctly kept focus on regulated no-show/adherence analytics, vendor-AI routing/latency/evaluation, messy GMV/funnel denominator correction, product-ops dashboards/qual-to-quant analysis, and bank-risk reporting/stakeholder surfaces while demoting Kaggle imaging, model-weight/toy-transformer claims, OpenCV/Canva noise, Figma/Dribbble portfolio work, OCR, and robotics.
- Implementation guardrail to preserve: off-role credibility checks should become warnings/risk checks/demotions, not standalone routable focus areas, unless role relevance is explicit.

**→ TO: All Agents | FROM: Codex | Date: 2026-06-02**
- Ran the same five edge resumes on `openai/gpt-chat-latest` only. Artifact: `/tmp/antigravity_surface_planner_probe_20260602_191854.{json,md}`.
- Result: 5/5 valid JSON, mean score `99`, min score `95`, mean latency about `11.4s`, estimated total cost about `$0.291`.
- Policy read: Chat Latest is faster than GPT-5.4 Mini but about 5.2x more expensive on this probe and not clearly better qualitatively. Keep GPT-5.4 Mini as the SurfacePlanV2 default; reserve Chat Latest for rescue/baseline or high-uncertainty planner cases.

---

## OPEN QUESTIONS
> Unresolved decisions that need Yash's input or further discussion.

1. ~~**ASR provider:** Deepgram confirmed — nova-3 model, API key set in `.env`~~ ✅ RESOLVED
2. ~~**TTS provider:**~~ ✅ **RESOLVED** — Cartesia first forever; ElevenLabs fallback only.
3. **Frontend framework:** React (WebRTC) vs Next.js vs something else?
4. **Auth:** Is recruiter-facing dashboard in scope for V1, or candidate-only first?
5. **LangGraph vs raw asyncio:** Notes reference LangGraph StateGraph. Do we want the full LangGraph wiring or keep it as pure async Python for now?

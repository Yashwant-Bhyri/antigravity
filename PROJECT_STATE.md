# PROJECT_STATE.md — The Religious Log Book

> This is the single source of truth for the **Project Trajectory**. 
> Every agent (Claude, Codex, Antigravity) MUST update this file after every major change. 
> We track not just what we did, but **WHY**, the **IMPACT**, and the **DRIVE** behind every architectural shift.

---

## 🧭 CORE TRAJECTORY
| Phase | Goal | Rationale | Status |
| :--- | :--- | :--- | :--- |
| **Phase 1: Interrogator** | Purely adversarial cognitive interrogation. | Test the failure boundaries of reasoning. | ✅ Complete |
| **Phase 2: Robust Validator** | Balance interrogation with curiosity and technical validation. | Foster meaningful technical exchange; reward intellectual honesty. | 🏃 In Progress |
| **Phase 3: Real-Time Flow** | Eliminate dead air and conversational lag. | Move from sequential 10s pauses to a 500ms "Two-Track" response system. | 🏃 In Progress |

---

## 📜 STEP-BY-STEP ACTIVITY LOG

### [2026-05-08] - Static Interview Left Rail + Separate Camera Panel
- **WHAT**: Updated `app/interview/[session_id]/page.tsx` so the live interview workspace is viewport-height on desktop, the left interrogator rail stays fixed/stable while the transcript scrolls inside its own panel, and the camera feed now renders in a dedicated rectangular "Camera Stream" panel instead of being overlaid behind the AI orb.
- **WHY**: During longer interviews, accumulated STT/transcript content made the whole page scroll, which caused the mode/orb/camera rail to drift away from its intended alignment. The camera was also visually blended under the listening/speaking/reasoning orb, making the two status signals compete.
- **IMPACT**: The main interview screen now keeps the left status UI anchored for the duration of the session, while the right transcript pane handles growth internally. The AI mode graphic and camera stream are visually separated, making the live interview state easier to read and less jumpy as the conversation gets long.

### [2026-04-23] — Deployed API-Origin Fix for Antigravity Frontend + ProvenHire Service Clarification
- **WHAT**: Fixed `lib/api.ts` so the Antigravity frontend now uses same-origin `/api` on deployed hosts instead of falling back to `http://localhost:8000/api` whenever `NEXT_PUBLIC_API_URL` is unset. Re-verified that the strict interview-map architecture remains backward-compatible for the ProvenHire integration path because `backend/api/routes.py:/start_interview` still accepts the legacy direct-start contract and internally routes through `prepare_session_map()` before starting the session.
- **WHY**: The previous frontend fallback was correct for local development but wrong for Vercel/runtime deployment. On a deployed domain, it caused the browser to call its own localhost and fail, making the Antigravity app appear broken even though the GitHub push had landed. At the same time, Yash clarified that ProvenHire’s `@ Antigravity LAB` path is the production-critical integration, so we needed to confirm whether the new strict map-first work had accidentally broken that service contract.
- **IMPACT**: Standalone Antigravity deployments will now hit the colocated FastAPI `/api` routes correctly by default, which removes the most likely “deployment updated but app still errors immediately” failure mode. ProvenHire’s backend adapter remains compatible with the robust interview-map flow because it still calls `/api/start_interview` directly and the service now performs strict map preparation before session start under the hood.

### [2026-04-22] — Frontend Visual System Overhaul + Design-Team UI Port
- **WHAT**: Reworked the live Antigravity Next.js frontend to adopt the design-team visual language across the real product surfaces. Added a reusable shared design layer in `components/design-system.tsx`, replaced the old global theme with a richer token/animation system in `app/globals.css`, upgraded `components/Waveform.tsx` to the new orb/waveform style, and redesigned `app/page.tsx`, `app/interview/[session_id]/page.tsx`, `app/report/[session_id]/page.tsx`, and `app/dashboard/page.tsx` around those primitives. Also fixed `startFreshInterview()` so fresh reruns preserve `target_role` and `years_experience` from the loaded session snapshot.
- **WHY**: The product had working flows but visually still looked like an early scaffold compared to the design-team direction. Yash explicitly wanted the Antigravity UI pushed toward a top-tier, more intentional visual identity, not just a light reskin. The right move was to port the design into the real app shell instead of leaving the design files as disconnected prototypes.
- **IMPACT**: Antigravity now has one coherent frontend language across candidate launch, live interview, report, and recruiter/dashboard views. The UI feels much closer to a deliberate product rather than a debug scaffold, while the underlying interview behavior remained intact. `npm run build` passed after the port, so the redesign is not just mock-level polish; it is wired into the actual runtime frontend.

### [2026-04-17] — Mentor Audit Pass: ASGI Fallback Boundary
- **WHAT**: Completed the short focused pass on `api/index.py` as the Vercel/serverless entry shim, including its relationship to `vercel.json` and `backend/main.py`. Logged the full mentor-style notes in `repo_optimization_journal.md`.
- **WHY**: After reviewing the real FastAPI boot file, the remaining deployment-facing question was what happens when that app fails to import or initialize. `api/index.py` is the last boundary between backend boot failure and the client-facing HTTP payload, so it deserved its own audit instead of being treated as an incidental helper.
- **IMPACT**: This pass mainly sharpened an already-known risk rather than adding a new one: the shim is structurally sound and intentionally minimal, but it still leaks traceback and `sys.path` details directly to clients on boot failure. The architecture is good; the failure payload is the part that needs tightening.

### [2026-04-17] — Mentor Audit Pass: App Boot and Startup Semantics
- **WHAT**: Completed the audit pass on `backend/main.py`, including its dotenv precedence, FastAPI lifespan behavior, startup warmups, CORS setup, and relationship to `api/index.py` plus `vercel.json`. Logged the full mentor-style write-up in `repo_optimization_journal.md`.
- **WHY**: After the route pass, the next important question was what the backend considers a successful boot. `backend/main.py` is where the app decides whether warm filler TTS, Postgres schema init, and question-bank load are mandatory or merely best-effort, so this file determines the difference between “healthy startup” and “silent degraded mode.”
- **IMPACT**: The repo now has a clearer articulation of the startup philosophy and its risks: config precedence is in a much better place, but the app still declares success too easily when important subsystems fail, and the lifespan has no teardown symmetry. This pass mostly sharpened architecture and operability understanding rather than adding a brand-new top-tier bug beyond what was already logged.

### [2026-04-17] — Mentor Audit Pass: Route Contract Layer
- **WHAT**: Completed the mentor-style pass on `backend/api/routes.py` and traced its main frontend consumers in `app/page.tsx`, `app/interview/[session_id]/page.tsx`, `app/report/[session_id]/page.tsx`, and `lib/audio.ts`. Logged the full route-layer analysis in `repo_optimization_journal.md`.
- **WHY**: After the orchestrator, interview-map, and session-state passes, the next right question was not “what does the backend do internally?” but “what does the backend promise publicly?” The route file is where orchestration behavior, storage durability, TTS behavior, and session lifecycle actually become product-facing contracts.
- **IMPACT**: This pass surfaced a new high-signal correctness issue: natural interview completion currently triggers one finalization path inside `handle_transcript()` and then a second `/end_interview` finalization from the frontend, which can rerun evaluation after `per_answer_scores` were already consumed. It also reinforced that `/process_turn` still has inconsistent missing-session semantics and that `/report` / `/sessions` expose the underlying durability split directly.

### [2026-04-17] — Mentor Audit Pass: Session State and Durability Layer
- **WHAT**: Completed the deep dive on `backend/state/session_manager.py` and traced its live use through orchestrator startup/turn handling/speculative writes plus the `/state` and `/report` API paths. Logged the full mentor-style analysis in `repo_optimization_journal.md`.
- **WHY**: After the orchestrator and interview-map passes, it was clear the next real bottleneck was not another agent but the storage model underneath them. The whole system now depends on many concurrent read-modify-write cycles against Redis session state, so we needed to inspect whether the state layer itself provides the guarantees the rest of the architecture assumes.
- **IMPACT**: This pass sharpened two key truths in the repo: first, the already-logged race condition is not incidental but structural because the state layer is a whole-blob replace API with no conflict primitives; second, completed reports are still TTL-bound because `/report/{session_id}` reads Redis state while Postgres only keeps a summary row. That report-expiry problem was important enough to log formally in `bug audit.md`.

### [2026-04-17] — Mentor Audit Pass: Interview Map Control Layer
- **WHAT**: Completed the mentor-style deep dive on `backend/services/interview_map.py`, including its seed extraction, snippet grounding, per-focus branch generation, runtime retrieval policy, and the simulation script in `backend/test_trajectory_map.py`. Also checked its live integration points in `backend/services/orchestrator.py`, `backend/api/routes.py`, and `backend/models/llm_router.py`.
- **WHY**: The interview map is no longer just a sidecar fallback helper. After the startup-contract changes, it now sits directly on the session-start critical path and also feeds the fast-path scheduler plus prompt-grounding packs for the live generators. That means we needed to evaluate it as a control-plane subsystem, not as a prompt experiment.
- **IMPACT**: The repo now has a clear written architectural judgment on this layer: the per-focus structure and raw-resume grounding are strong, but the subsystem is now startup-critical while still relying on heavyweight `large`-tier generation and brittle snippet-enrichment gates. The main next-step pressure is to make the map startup-safe and more deterministic before adding further behavior on top of it.

### [2026-04-17] — Mentor Audit Pass: Orchestrator Control Plane
- **WHAT**: Completed the line-by-line mentor-style audit of `backend/services/orchestrator.py`, including the new `backend/services/interview_map.py` dependency and the startup handshake through `/api/start_interview` plus the interview page's startup-state check. Logged the full walkthrough and architectural analysis in `repo_optimization_journal.md`.
- **WHY**: The orchestrator is now the real runtime brain of the product. It owns startup readiness, same-turn revision semantics, fast-path scheduling, background staging, speculative partial-transcript prep, sprint progression, and final interview closure. We needed one focused pass on the place where all agent outputs are actually turned into behavior.
- **IMPACT**: The audit surfaced the next highest-leverage correctness bugs before any more feature work: the legacy staged-shadow path can double-apply and wipe focus metadata, the turn-level in-flight guard can suppress the newest same-turn revision so it never gets full analysis, trajectory-map fast-path responses lose pivot/focus packet metadata when rebuilt, and `_REASONING_FALLBACK` still violates the reasoning agent's `0-3` score contract. No runtime code was changed in this pass; the value here is narrowing the next fix set with code-proof evidence.

### [2026-04-15] — Short-Answer Rescue Hardening + Generic Packet Demotion
- **WHAT**: Tightened `backend/services/orchestrator.py` so short terse answers are no longer trapped behind synthetic/generic follow-up packets. Synthetic `active_question_packet` reconstruction no longer injects `_FALLBACK_FOLLOWUPS`, bank follow-ups are no longer prioritized when the active packet route is `sprint_fallback` or `unknown`, same-turn speculative candidates can now outrank a generic staged fallback, and `_short_answer_rescue_eligible()` was widened from `4..18` words to `1..18`. Added explicit rescue telemetry events for attempt, success, timeout, and failure.
- **WHY**: The earlier rescue + refine/keep work was present in code, but live terse-answer behavior still felt bad because generic packets could still “own” the next turn and block rescue. The worst real-world answers were often one or two words (`"Mostly cost."`, `"Latency."`), and those were not even eligible for rescue under the prior threshold.
- **IMPACT**: Minimal answers now have a real path to a grounded follow-up instead of collapsing immediately into the generic sprint fallback loop. Live terse-answer simulation improved: `"Mostly cost."` now routed as `short_answer_rescue` with a cost-specific question, and the next terse answer recovered into `clarification_fast`. The remaining issue is now clearly narrowed to cold-start early-turn staging, not generic-packet precedence.

### [2026-04-15] — Full Interview Telemetry + Session Trace Capture
- **WHAT**: Added a new append-only telemetry sink in `backend/services/interview_telemetry.py` that writes per-session JSONL traces to `backend/runtime/interview_traces/{session_id}.jsonl`. Added `POST /api/telemetry` for browser/client events and `GET /api/telemetry/{session_id}` for summary + recent-event readback. Instrumented backend API routes, orchestrator fast-track/background-pipeline stages, TTS pre-generation/cache, and the frontend audio/interview loop (`lib/audio.ts`, `app/interview/[session_id]/page.tsx`) so live runs now record STT flush reasons, floor transitions, process-turn latency, route decisions, TTS provider/source/latency, playback events, same-turn reopen behavior, hold/revoke behavior, and warning/error/bottleneck events.
- **WHY**: Live testing was still surfacing rich qualitative failures ("no follow-ups", "cold pivots", "fragmented narration", "stalls"), but the codebase only had scattered `print` and `console.log` lines. That made it hard to distinguish frontend STT timing problems from backend route selection, cache misses, or slow background pipelines. We needed a single per-session trace that captures the entire interview lifecycle across browser + backend.
- **IMPACT**: The next live interview run is diagnosable end to end. We can now inspect one session trace and answer: which API calls happened, how long each stage took, whether TTS was prepped or live, why a flush occurred (`early_commit` / `utterance_end` / `hard_cap`), whether the turn reopened as a same-turn revision, what route kind the orchestrator selected, what the background pipeline was doing, and where stalls/fallbacks/errors occurred. This turns vague UX complaints into concrete, timestamped failure evidence.

### [2026-04-15] — ElevenLabs Live Backend Path Restored
- **WHAT**: Fixed `backend/main.py` so dotenv loading is resolved from the project root instead of the process working directory. The backend now loads `/Users/yash/antigravity/.env` first with override enabled, then loads `.env.local` only to fill missing values. Restarted the backend and re-verified the live TTS path end to end.
- **WHY**: Direct ElevenLabs SDK probes were succeeding from the same machine, but the live backend was still returning `x-tts-provider: cartesia`. That meant the account/provider itself was healthy and the mismatch had to be in the running backend config path. The old startup logic let cwd-relative / stale local env precedence drift away from the effective config used by the successful direct probe.
- **IMPACT**: The backend and direct probes now agree on provider credentials/config. Startup filler warming no longer falls back immediately, `/api/tts_health` reports `last_provider_used="elevenlabs"` with no active error, and live `/api/tts` is back to returning ElevenLabs MP3 audio instead of emergency Cartesia fallback.

### [2026-04-15] — Packetized Follow-Up Scheduling + Immediate Turn Memory
- **WHAT**: Refactored `backend/services/orchestrator.py` so the live interview loop now tracks an `active_question_packet` and a `prepped_next_packet` instead of relying on a loose mix of `prepped_next_question` plus opportunistic follow-up arrays. Each question packet carries the current thread’s question text, route kind, focus, and deterministic follow-up templates. `handle_transcript()` now prioritizes follow-ups from the current packet before consuming a parked next-topic packet. Also added immediate skeleton turn records to `history` on the fast path (`analysis_status="pending"`), and changed `_apply_staged_analysis()` to enrich those skeleton records in place by `turn_id` instead of skipping them as duplicates. Added lightweight TTS runtime visibility via `TTSService.status_snapshot()` and `/api/tts_health`.
- **WHY**: The core bug was architectural, not just prompt quality. The fast path was trying to both deepen the current topic and advance to the next topic, and the next-topic staging path kept starving or overwriting the deepening path. That made follow-ups disappear, continuity feel one turn late, and early turns collapse into generic fallback even when the background pipeline had already chosen the next topic. The history model also lagged behind the conversation because new memory only arrived when staged analysis was consumed later.
- **IMPACT**: The orchestrator now has an explicit notion of “current thread” versus “next topic,” which is the actual fix for early-turn follow-up starvation. Follow-ups are deterministic and can stay on-thread even when the next topic is already prepared. Turn memory lands immediately instead of one turn late, which should improve continuity and reduce `question_count/history` drift. TTS runtime state is also now inspectable in a live environment instead of opaque during failures.

### [2026-04-14] — Follow-Up Continuity + Anti-Tunneling + STT Turn-Calming Pass
- **WHAT**: Reworked follow-up sequencing in `backend/services/orchestrator.py` so queued bank follow-ups can beat generic staged sprint pivots, preserved up to two follow-up templates instead of a single one-shot slot, and stopped overwriting remaining follow-ups after a `bank_followup_fast` turn. Tightened the same-focus breadth guard so repeated high-severity non-recovering probes pivot sooner. Improved sprint question generation in `backend/agents/followup_agent.py` by querying the question bank with transition memory / topic anchor / weakness hints and by forbidding generic cold-start design prompts. On the frontend, slowed the STT commit path in `lib/audio.ts` and `app/interview/[session_id]/page.tsx`: early-commit now respects a real eligibility gate, Deepgram end-of-utterance timing is less eager, and clustered final chunks get a 700ms settle window before `processTurn`.
- **WHY**: Repeated live tests were still showing the same interaction failures despite earlier timing fixes: almost no real follow-ups, repeated drilling on the same concept, Sprint 3 questions feeling detached and generic, and natural narration getting split into many micro-turns / same-turn revisions. The previous architecture had structural causes for each of those behaviors, not just prompt-quality issues.
- **IMPACT**: The interview should now deepen more naturally before jumping topics, pivot away from dead-end contradiction loops sooner, enter Sprint 3 with stronger continuity, and stop interrupting hesitant speakers as aggressively. This is a user-experience hardening pass on the core live loop, not just a wording tweak.

### [2026-04-14] — Frontend API Contract Normalized + Follow-Up Output Safeguards
- **WHAT**: Added shared frontend API-base normalization in `lib/api.ts` and switched the landing page, interview page, report page, dashboard, and browser audio helper to use it. Restored landing-page enforcement of `target_role` and `years_experience`. Hardened `backend/agents/followup_agent.py` so raw/serialized/malformed LLM question output is validated and replaced with route-specific fallback questions when needed.
- **WHY**: The frontend had drifted into two incompatible API-base assumptions: the landing page normalized `NEXT_PUBLIC_API_URL` to `/api`, while the other pages used the raw env value directly. Separately, question-cleaning logic could still leak garbage like `Question:` or serialized payloads into the live interview if the LLM ignored formatting instructions. The landing page had also stopped enforcing the calibration inputs that the new role/YOE-relative interview flow depends on.
- **IMPACT**: Frontend fetch paths now share one API contract instead of depending on page-by-page assumptions. Interview question generation is safer under malformed LLM output and degrades to usable fallback prompts instead of blank/junk text. The role/YOE calibration path is once again consistently populated at interview start.

### [2026-04-14] — Revision-Safe Background Staging + TTS Provider Realignment
- **WHAT**: Added backend-managed `answer_version` tracking in `backend/services/orchestrator.py` for same-turn revisions. Staged analyses now carry version metadata, superseded staged items are dropped before canonical apply, and background runs self-discard if a newer revision exists for the same `turn_id`. Also restored ElevenLabs as the default TTS provider in `backend/services/tts_service.py`, with Cartesia preserved as explicit opt-in or fallback-only when ElevenLabs is unavailable.
- **WHY**: The prior same-turn revision logic still depended on timing assumptions: an older background pipeline could overwrite or be applied ahead of a fuller revised answer if it finished at the wrong time. Separately, the runtime had started silently switching to Cartesia whenever a Cartesia key existed, which conflicted with the project's settled TTS decision and made provider behavior environment-dependent.
- **IMPACT**: Same-turn revision handling is now version-safe instead of race-prone. Older analyses cannot mutate canonical history once a newer revision exists. Historical TTS note superseded on 2026-05-07: Cartesia is now the default contract.

### [2026-04-15] — Calmer STT Boundaries + Interim Snapshots for Speculative Prep
- **WHAT**: Reworked the frontend/backend partial-transcript contract so `lib/audio.ts` now sends throttled interim `/partial_transcript` snapshots, not just Deepgram `is_final` blocks. Each snapshot carries `turn_id`, `is_final`, and `snapshot_seq`. At the same time, the frontend STT commit policy was rolled back toward a calmer Deepgram-led model: `utterance_end_ms=3000`, custom early-commit and hard-cap commit logic removed, and only a long safety timeout retained as a defensive fallback. `backend/api/routes.py` and `backend/services/orchestrator.py` now accept the richer partial payload, reject stale snapshots by sequence, and use them only for speculative prep / entity accumulation. `app/interview/[session_id]/page.tsx` now treats normal UtteranceEnd-backed finals as immediately settled and keeps the old defensive TTS hold only for safety-timeout commits.
- **WHY**: Live telemetry showed the “low-latency” path was actually hurting the experience: one natural answer could turn into many revisions, the AI repeatedly prepared follow-ups while the user was still talking, and the backend saw far less live context than the UI implied because pure interim text never reached `/partial_transcript`. The user explicitly preferred better turn integrity and good follow-ups over shaving the last 1-2 seconds.
- **IMPACT**: The backend now has richer real-time context for speculative follow-up prep without being allowed to treat unstable interim text as canonical interview history. Turn commits should be much less fragment-prone, interruptions should be less immature, and the system is now aligned around a clearer contract: speculative thinking can happen continuously, but committed action still waits for a real utterance boundary.

### [2026-04-15] — Interview Map Must Exist Before Interview Start
- **WHAT**: Changed startup flow so `backend/services/orchestrator.py:start_session()` now awaits both `_seed_first_question()` and `_build_interview_map()` before returning the session id. After those complete, startup re-reads Redis and verifies that `interview_trajectory_map.focus_areas` is non-empty; otherwise it raises and `/api/start_interview` fails instead of silently starting a degraded interview. Also compacted focus keys in `backend/services/interview_map.py` so fallback or weak seed extraction no longer turns entire resume lines into giant `focus_key` strings. `frontend/app/interview/[session_id]/page.tsx` was updated to stop polling for a background map and instead treat a missing map as a startup error, because the backend contract is now “ready means ready.”
- **WHY**: The product requirement was explicit: do not start the interview until the map is fully generated and verified. The previous code still returned immediately and relied on frontend polling / hope, which left Turn 1 exposed to `sprint_fallback` whenever the map lagged. The ugly focus keys coming from fallback extraction were another sign that we were letting low-quality startup state leak into the runtime.
- **IMPACT**: Turn 1 no longer has to race a background map build. If startup succeeds, the map is present. If the map cannot be built, the session fails loudly instead of pretending the interview is ready. This trades startup latency for correctness, which is the intended product behavior for now.

### [2026-04-15] — Raw-Resume Focus Packs + Trajectory Retrieval Hardening
- **WHAT**: Reworked the interview-map / follow-up grounding path so raw resume text is the canonical source, not brittle parser artifacts. `backend/services/interview_map.py` now extracts exact supporting resume snippets for each focus area, uses those snippets directly in the map-generation prompt, rejects arbitrary first-focus fallback when retrieval overlap is weak, and fills missing generated map branches from deterministic per-branch fallbacks instead of dropping the whole focus track. `backend/services/orchestrator.py` now builds compact focus prompt packs from the active focus area and passes them into speculative generation, discrepancy/clarification/attack probes, bank-followup adaptation, sprint-question generation, and sprint openers. `backend/agents/followup_agent.py` now lets speculative generation compare the current best speculative question against a relevant interview-map candidate and requires clearer in-question pivot phrasing. `frontend/lib/audio.ts` was normalized back to `endpointing=1500` / `utterance_end_ms=2800`.
- **WHY**: Recent live sessions showed that generic or parser-polluted fallback context was still making the interview feel narrow, repetitive, or vaguely attached to the wrong experience. The user explicitly pushed for raw-resume-grounded prompting, exact snippet context, and stronger project/tech-stack transitions instead of trusting lossy parsed labels or floating generic follow-ups.
- **IMPACT**: Follow-up generation now has a much better grounding contract: speculative partial-STT calls stay lightweight but can still see the active focus label, 1-3 exact resume snippets, the current best speculative question, and a map-backed candidate question. Full-turn follow-up generation can stay on the right project more reliably, pivot language should be clearer in the generated question itself, and uncertain retrieval no longer pollutes prompts by silently jumping to an arbitrary resume area.

### [2026-04-15] — STT Timing Retune After First Stable Live Run
- **WHAT**: Tuned Deepgram timing in `lib/audio.ts` from `endpointing=1200 / utterance_end_ms=3000` to `endpointing=1500 / utterance_end_ms=2800`.
- **WHY**: Live run `061852df-d640-4a05-a962-4c1ce7fbc739` showed that the calmer commit architecture was doing its job: one natural answer now usually became one committed turn, with zero follow-up revocations while the user was still speaking. That gave us room to shave a bit of perceived dead air. Because interim snapshots now stream continuously to `/partial_transcript`, `endpointing` no longer needs to be ultra-low just to feed speculative prep; it can instead be tuned for calmer chunk quality.
- **IMPACT**: The system should hand off slightly faster after the user finishes, while also producing slightly fewer `is_final` fragments and speculative churn than the `1200` setting. This is an incremental timing optimization on top of the calmer architecture, not a return to the old early-commit behavior.

### [2026-04-15] — Runtime Config Source-of-Truth Hardening + Interview-Map Prompt Upgrade
- **WHAT**: Added `backend/config/env_runtime.py` to resolve aliased runtime config uniformly (`ELEVENLABS_API_KEY`/`TTS_API_KEY`, tier model envs, voice-id aliases). Updated `backend/main.py` so `.env` loads as base config and `.env.local` overrides it. Historical local override note superseded on 2026-05-07: Cartesia is primary even if `TTS_PROVIDER=elevenlabs` is present. Hardened `backend/services/interview_map.py` with a much stricter generation prompt, branch-specific intent, and validation that rejects generic or off-focus questions before they reach runtime.
- **WHY**: Key/model changes had been brittle because config precedence was backwards for local overrides: stale values in `.env` could silently beat a newer runtime override. Separately, the first cut of the interview-map prompt still allowed generic interviewer language and weak focus drift, which undermined the whole point of the resume-grounded trajectory bank.
- **IMPACT**: Rotating the ElevenLabs key or changing the active OpenRouter models now has a single reliable local override path instead of “change one thing and hope the server picked it up.” The live backend now restarts into the expected provider/model mix deterministically. On the question-generation side, the interview-map should produce sharper, more resume-anchored fallback branches and fall back to deterministic templates when the model returns mush.

### [2026-04-14] — "Pure Vercel" Deployment Shift
- **WHAT**: Stripped heavy AI dependencies (FAISS, SentenceTransformers) and configured `vercel.json`.
- **WHY**: To shrink the backend from ~1GB to <50MB, enabling direct deployment as Vercel Serverless Functions.
- **IMPACT**: Infrastructure simplified from a Docker-hybrid (Railway/Vercel) to a unified Vercel-only deploy.

### [2026-04-14] — Stabilization: The Memory Core
- **WHAT**: Implemented `generate_sprint_opener` and initialized Turn 1 pre-seeding logic.
- **WHY**: To fix the "Cold Start" problem and ensure sprint transitions feel like a continuation, not a reset.
- **IMPACT**: AI persona feels significantly more intelligent and grounded in previous turns.

### [2026-04-07] — The Two-Track "Fast/Slow" Response Strategy
- **WHAT**: Proposed and refined the "Adversarial Shadow" two-track system in `COLLAB.md`.
- **WHY**: Yash reported 5-10s of dead air causing a "mechanical" feel.
- **IMPACT**: Eliminates conversational lag. The AI responds in <500ms (Fast Track) while planning the next adversarial probe in the background (Slow Track).

### [2026-04-13] — Dynamic Sprint Openers
- **WHAT**: `generate_sprint_opener()` added to `FollowUpAgent`. `_maybe_advance_sprint()` in orchestrator is now async and calls it at every sprint transition with the full prior sprint history (including current answer as a synthetic entry). Falls back to static `SPRINT_OPENERS` if LLM fails.
- **WHY**: Live test `a82b7820` confirmed static sprint openers produce cold-start questions that ignore all prior context. Turn 6 (sprint 2 opener) asked "pick one idea at the core of what you've built" after 5 turns drilling latent-space steering and feature map engine — candidate answered with a fragment.
- **IMPACT**: Sprint transitions now carry context forward. Opener references specific things said in the previous sprint. Haiku call adds ~300ms to the sprint transition turn (acceptable — only fires once per sprint).

### [2026-04-13] — LATER_EDITS.md Created
- **WHAT**: Created `/Users/yash/antigravity/LATER_EDITS.md` — structured backlog of deferred improvements.
- **WHY**: Multiple items identified in testing that aren't urgent but must not be forgotten (CV warmup, utterance_end_ms tuning, filler loop cooldown, faiss model reload bug, project_map, confession pivot, distress detection, weakness_summary rendering, stale response invalidation).
- **IMPACT**: Single place to track deferred work. Prevents re-discovering the same issues each session.

### [2026-04-13] — Two-Track Architecture Deployed + Tested (a82b7820)
- **WHAT**: First live test of the full two-track implementation from the prior session.
- **WHAT WORKED**: Mid-sprint turns (3+) serving prepped adversarial probes correctly. Turn 5 context-aware question confirms bg pipeline staging works. WeaknessAgent correctly identified vague ML claims, latent-space steering unsubstantiated, attribution ambiguity.
- **WHAT FAILED**: Turn 1 always hits raw fallback (no prepped_q on first turn ever). Sprint 2 opener was static (fixed above). bg pipeline may have failed on Turn 1 (first-run issue — not confirmed).
- **REMAINING GAP**: Turn 1 cold start — pre-seeding prepped_next_question at start_session with a Haiku resume-based question. Pending product decision.

### [2026-04-07] — Honesty Detection Logic
- **WHAT**: Updated `WeaknessAgent` and `ReasoningBehaviorAgent` to detect "Admitted Gaps."
- **WHY**: The system was attacking candidates for being honest about their limits, which is a desirable engineering trait.
- **IMPACT**: High-severity attacks are now downgraded to curious probes if the candidate admits a gap.

---

## 🛠️ EXPERIMENT & IDEAS LEDGER
| Idea | Status | Rationale / Outcome |
| :--- | :--- | :--- |
| **Backend ASR Service** | 🪦 Buried | Replaced by client-side Deepgram SDK for lower latency and simpler architecture. |
| **RAG-based Questioning** | 🧊 Shelved (v2) | Moving to a pre-seeded bank for v1 stability. |
| **Mic Throttling (Ghost-VAD)** | 🏗️ Active | Prevents the AI from hearing itself and causing a recursion loop (The Softmax Incident). |
| **HandoverManager** | 🏗️ Active | Prevents "Split Answer" bugs where a mid-thought pause triggers a premature AI response. |

---

## 🚨 REGRESSIONS & COMPLEXITY LOG
- **[2026-04-07] The "Stable Softmax" Echo Loop**: 
  - *Symptom*: AI began interviewing itself recursively.
  - *Cause*: Acoustic echo picked up AI output as user input.
  - *Fix*: Implementing Mic Throttling in the frontend.
- **[2026-04-07] The "Answer Splitter" Bug**:
  - *Symptom*: 3s silence flushes a partial thought, causing disjointed responses.
  - *Cause*: Hard timeout on `UtteranceEnd`.
  - *Fix*: Handover logic to detect trailing fragments.

---

## 🔄 RESURRECTED IDEAS
| Idea | Initially Rejected | Why it returned? |
| :--- | :--- | :--- |
| **Fast Haiku Adaptations** | Latency concerns | Necessary to ground deepening questions in <500ms. |

---
### [2026-04-17] - Repo Optimization Journal Started
- **WHAT**: Added `repo_optimization_journal.md` as a long-form, mentor-style review log that complements `bug audit.md`. Started the first full deep dive on `backend/agents/weakness_agent.py`, including its downstream contract in `backend/services/orchestrator.py`, `backend/agents/followup_agent.py`, and `backend/models/llm_router.py`.
- **WHY**: We now need a slower, component-by-component pass that does more than list bugs. The goal is to understand what each agent is trying to do, what each block of code translates to in product behavior, where the architecture is elegant, where it drifts, and what the best version of that subsystem should become.
- **IMPACT**: The repo now has a persistent optimization journal we can build incrementally across sessions. `WeaknessAgent` is the first lock-in point, and future deep dives can continue in the same file without losing context or repeating earlier architectural reasoning.

### [2026-04-17] - FollowUpAgent Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full deep dive on `backend/agents/followup_agent.py`, including its cleanup/fallback layer, persona system prompts, attack-strategy bridge, sprint question generation, bank follow-up adaptation, sprint openers, seed-question generation, speculative question generation, and its live orchestrator call sites.
- **WHY**: `FollowUpAgent` is where diagnosis becomes candidate-facing product behavior. To improve the interview experience, we need to understand not only whether the system picked the right route, but whether the actual question it asks is sharp, fair, continuous, grounded, and fast.
- **IMPACT**: We now have a written map of `FollowUpAgent` as the system's question-rendering engine, not just a helper. The journal also locks in its main strengths (continuity, route-specific question styles, repair/fallback discipline, good latency tiering) and its biggest weaknesses (role sprawl, prompt-text corruption, silent degraded modes, limited observability, and an apparently unused `prefetch()` path). That gives us a much clearer basis for the next deep dive and eventual refactor work.

### [2026-04-17] - DiscrepancyAgent Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full deep dive on `backend/agents/discrepancy_agent.py`, including its prompt contract, fallback behavior, orchestrator priority path, interaction with `generate_discrepancy_challenge()`, candidate-memory effects, and its dependency on upstream resume structure.
- **WHY**: `DiscrepancyAgent` is the trust-calibration route in the interview loop. We needed to understand not just how it flags contradictions, but when it is fair to escalate, how it should treat ambiguity versus direct contradiction, and whether honest self-correction is being handled properly.
- **IMPACT**: We now have a clear written diagnosis of the discrepancy path's real strengths and blind spots. The journal locks in several high-value insights: the `suspected` state barely matters in live routing, discrepancy has no fairness softening comparable to weakness-routing honesty protection, the agent underuses structured resume claims, and its `none + description` output can accidentally influence `established_facts`. That gives us a much better basis for the next upstream deep dive into `ResumeAgent`.

### [2026-04-17] - ResumeAgent Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full deep dive on `backend/agents/resume_agent.py`, including its prompt schema, heuristic fallback parser, merge behavior, session-start integration, focus-inference dependencies, and its downstream effects on weakness calibration, question grounding, discrepancy checking, and final evaluation.
- **WHY**: The previous three agent reviews all converged on the same upstream dependency: the system's fairness and grounding depend on what it thinks the candidate actually claimed. We needed to inspect whether `parsed_resume` is a trustworthy candidate model or just a useful-but-fuzzy blob.
- **IMPACT**: We now have a clear map of which parts of the resume schema are truly load-bearing (`projects`, `claims`, `experiences`, `experience_tier`) and which parts are weaker or more aspirational (`experience`, GitHub links, shallow merge behavior). The journal also locks in the key strategic direction: `ResumeAgent` should evolve from a convenient parse blob into an explicit candidate-claim model with stronger normalization, confidence, and downstream consumption.

### [2026-04-17] - ReasoningBehaviorAgent Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full deep dive on `backend/agents/reasoning_behavior_agent.py`, including its prompt contract, orchestrator routing influence, honest-admission soft-cap logic, overprobed-topic dead-end handling, terminal-admission early-stop behavior, and its final-evaluation aggregation path.
- **WHY**: After reviewing claim ingestion, weakness diagnosis, question rendering, and discrepancy detection, the next high-leverage seam was the fairness governor that decides how the system should interpret candidate behavior under pressure. We needed to understand whether the runtime was truly rewarding honest self-correction and how much force this behavioral signal should have.
- **IMPACT**: We now have a clear written model of `ReasoningBehaviorAgent` as a live moderation layer rather than a report-only sidecar. The journal locks in several important issues: the agent is making subtle judgments from very thin context, its outputs are not normalized locally, `_REASONING_FALLBACK` is inconsistent with the prompt's `0-3` structure scale, and its fairness signal currently softens weakness routing more than discrepancy routing. That gives us a strong foundation for the next downstream deep dive into `EvaluationAgent`.

### [2026-04-17] - EvaluationAgent Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full deep dive on `backend/agents/evaluation_agent.py`, including its per-answer scoring path, full-interview synthesis path, calibration helper, coverage-note logic, orchestrator session-end integration, `/report` API contract, frontend report rendering, and Postgres persistence boundary.
- **WHY**: After reviewing the live-loop agents and fairness/moderation layer, the next convergence point was the final judgment engine. We needed to understand exactly how transcript history, weaknesses, reasoning signals, role calibration, and claim credibility concerns are being turned into the product's final recommendation and report schema.
- **IMPACT**: We now have a clear written diagnosis of `EvaluationAgent` as both the system's final judge and the effective author of the report payload. The journal locks in several important issues: per-answer and full-interview evaluation are unevenly calibrated, multi-pass averaging currently averages only the scalar score and not the sub-breakdown, output normalization is too trusting, and several summary paths are lossy. That gives us a strong base for the next remaining core-agent deep dive and then the eventual orchestrator pass.

### [2026-04-17] - ConceptAgent Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full deep dive on `backend/agents/concept_agent.py`, including its minimal prompt/contract, orchestrator fallback path, frontend Deepgram-entity replacement path, partial-transcript accumulation flow, and the limited downstream use of stored `concepts` in canonical history.
- **WHY**: After reviewing the rest of the core agents, we needed to understand whether `ConceptAgent` was still a real control-plane agent or whether the architecture had already evolved past it. That question matters because the current system increasingly relies on frontend-supplied entities and speculative partial-transcript logic instead of backend LLM concept extraction.
- **IMPACT**: We now have a clear written model of `ConceptAgent` as mostly a fallback extractor in the current runtime. The journal locks in several useful conclusions: the frontend/Deepgram path has largely taken over live concept/entity surfacing, `ConceptAgent.extract()` is locally fragile because it assumes dict output, and the stored `concepts` field currently has weaker downstream influence than the rest of the core-agent outputs. That clears the way for the next high-leverage pass on the orchestrator as the real runtime fusion layer.

### [2026-04-17] - LLMRouter Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full systems-level deep dive on `backend/models/llm_router.py`, including its tier map, env-resolution behavior, token-budget policy, JSON-recovery heuristics, transport lifecycle, and live influence across the agents plus the startup-critical interview-map path.
- **WHY**: After the agent and control-plane passes, it was clear that model behavior was being discussed as if it lived only in prompts. In reality, `LLMRouter` is the shared boundary where provider selection, reasoning-model cleanup, and structured-output recovery become backend-wide behavior, so it needed its own explicit architectural review.
- **IMPACT**: The repo now has a clearer written judgment that `LLMRouter` has quietly become control-plane infrastructure while still behaving like a thin helper. This pass also surfaced a new concrete bug: the router's last-resort parser rescues JSON objects but not JSON arrays, while interview-map seed extraction explicitly expects an array, so a nearly-correct wrapped response can still degrade startup into deterministic fallback parsing.

### [2026-04-17] - TTSService Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full systems-level deep dive on `backend/services/tts_service.py`, including provider policy enforcement, Cartesia fallback semantics, filler caching, pre-generated audio caching, `/tts` and `/tts_filler` route behavior, orchestrator pre-generation dispatch, and frontend playback usage in `lib/audio.ts` plus `app/interview/[session_id]/page.tsx`.
- **WHY**: After the router pass, the next important shared boundary was voice delivery. This service turns backend scheduling and provider behavior into the user's lived sense of latency, so we needed to inspect not just the file itself but whether the frontend and orchestrator are still honoring the filler-first/pre-generated-audio contract it was designed for.
- **IMPACT**: This pass surfaced two important cross-file issues. First, the main interview path is no longer truly filler-first: normal turns wait for `processTurn` plus `/tts`, and filler audio is now only used for silence nudges. Second, the single-slot `session_id` pre-generated audio cache is race-prone, so a stale background synthesis can overwrite the correct upcoming question and silently force a live TTS miss. The repo now has a much clearer written diagnosis of why TTS latency feels more inconsistent than the service design alone would suggest.

### [2026-04-17] - Interview Page Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full mentor-style systems pass on `app/interview/[session_id]/page.tsx`, including snapshot loading, resume/fresh-run gates, answer-draft accumulation, stale/revision guards, silence-hold/revoke behavior, TTS sequencing, and completion/navigation flow.
- **WHY**: After the TTS pass, it was clear the biggest remaining questions were no longer inside one backend service. The interview page is where speech accumulation, turn finalization, AI playback, session lifecycle, and product-level safety gates all become one user experience, so it needed to be treated as a control surface rather than just UI chrome.
- **IMPACT**: This pass confirmed that the page is doing several sophisticated things well, especially same-turn revision handling and stale-response invalidation, but it also surfaced a new correctness issue: candidate answers are written optimistically into the local transcript before `processTurn` succeeds, and on failure the page does not reconcile with backend state. The page also reinforced earlier findings around missing normal-turn filler-first behavior, redundant completion finalization, and fresh-run calibration drift.

### [2026-04-17] - Memory/Data Highway Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a systems-level pass on memory handling and the full working highway of the product: browser speech buffers, frontend control refs, Redis session state, backend process-local sidecars, speculative partial-transcript flow, committed-turn handoff, background agent fan-out, TTS pre-generation, end-session evaluation, and the real API/LLM call budgets across startup and live turns.
- **WHY**: The repo had reached the point where file-by-file review was no longer enough. We needed to understand how data actually moves and survives across the whole runtime: what is canonical versus speculative, which memories are durable versus local, where prompts are duplicated, how many model/API calls a real turn actually costs, and whether the written architecture still matches the live one.
- **IMPACT**: This pass locked in a much clearer systems truth: the runtime now depends on multiple load-bearing process-local sidecars even though the docs still describe Redis-only state and strict chain-isolated agents. It also surfaced a new high-severity bug for the register: critical live interview state still lives in Python-process memory, so multi-worker/non-sticky execution can lose partial entity accumulation, per-answer scores, speculative coordination, and pre-generated TTS hits.

### [2026-04-17] - `lib/audio.ts` Deep Dive Logged
- **WHAT**: Extended `repo_optimization_journal.md` with a full mentor-style deep dive on `lib/audio.ts`, covering Deepgram boot, floor-state transitions, barge-in detection, echo suppression, utterance buffering, speculative partial snapshots, committed flush semantics, TTS prefetch/playback, browser fallback speech, and the file's architectural relationship with the interview page and backend orchestrator.
- **WHY**: After the interview-page pass, the remaining hidden question was how much of the product's turn-boundary behavior actually lives one layer lower in the browser audio/ASR substrate. `lib/audio.ts` is the first runtime surface that decides what speech even reaches the backend, so it needed the same control-plane level of scrutiny as the orchestrator.
- **IMPACT**: This pass confirmed that `lib/audio.ts` is one of the strongest infrastructure files in the repo, but it also surfaced two important concerns for the ongoing audit: telemetry is now tightly coupled to the hot path, and the current lexical echo-suppression cooldown can plausibly clip legitimate answer openings when a candidate immediately repeats terms from the just-finished question.

### [2026-04-22] - Interview Map Startup Hardening + Resume Focus Sanitization
- **WHAT**: Hardened the startup-critical interview-map path in `backend/services/interview_map.py` and `backend/services/orchestrator.py`. Added a deterministic startup fallback map when the LLM-backed map generation times out, introduced work-entry-focused resume sanitization for focus extraction, filtered contact/education/awards/skills metadata out of map seeds, shortened the LLM seed-extraction input and token budget, improved timeout logging, and tightened fallback label generation so degraded-mode maps now center on actual technical work rather than top-of-resume metadata.
- **WHY**: Live startup on OpenRouter/Qwen in mainland China was repeatedly degrading into `TimeoutError` during interview-map generation. When that happened, the old deterministic fallback would happily build focus areas from phone numbers, scholarships, degree lines, and skill buckets. That made the map exist, but not be product-usable.
- **IMPACT**: Antigravity now boots reliably even when the map LLM path times out, and the fallback map is materially more interviewable on messy real resumes. On Yash's raw resume input, the fallback focus areas now land on work like the AIGC video pipeline, semantic UI-to-latent interface, Optek model work, classifier optimization, and benchmark framework instead of contact/education noise. The core remaining issue is no longer catastrophic map corruption; it is that the LLM-backed map path still exceeds the startup budget often enough that deterministic fallback remains common in this network/provider environment.

### [2026-04-22] - Interview Map Partial LLM Hydration After Startup
- **WHAT**: Extended the interview-map runtime so startup can return a high-quality map with LLM-selected focus areas and then continue hydrating any fallback-written focus tracks in the background. `backend/services/interview_map.py` now tags each focus area with `track_source`, returns `pending_hydration_focus_keys`, and exposes `hydrate_interview_map_tracks(...)`. `backend/services/orchestrator.py` now schedules `_hydrate_interview_map()` after startup whenever some focus branches are still deterministic.
- **WHY**: Even after shrinking prompts and sanitizing resume input, the per-focus branch-writing calls were still the slowest part of map generation on the current OpenRouter/Qwen route. We needed a design that preserves fast reliable startup without abandoning the higher-quality LLM-authored track structure.
- **IMPACT**: The system now behaves more like a resilient control plane: startup immediately lands a correct LLM-selected focus list, and the map can keep upgrading itself after boot instead of freezing in a degraded state. On Yash's raw resume, the latest verified session (`bbb0d497-dfee-46cd-86de-148d10281983`) started with four good focus areas and, after a short wait, had already upgraded `Semantic UI-to-Latent Interface` and `TinyML Audio Pipeline` to `track_source="llm"` while the slower `Filmora` and `ML Feature-Map Control System` branches remained queued for future hydration.

### [2026-04-22] - Warmer Seed Follow-Up + Explicit Closing Runway
- **WHAT**: Updated `backend/agents/followup_agent.py` so the session-start seed follow-up now explicitly prefers a warm, context-first phrasing that names the specific experience before probing ownership or implementation. Updated `backend/services/orchestrator.py` to detect the last two questions of Sprint 3, decorate those questions with explicit runway language, and surface `closing_phase` / `questions_remaining` metadata in the fast-track response contract.
- **WHY**: The interview was technically functional, but the first seeded follow-up still entered too sharply and the ending could feel abrupt because the user was not told they were entering the final stretch. Those two moments are disproportionately important for the product feel, especially in a voice interview where tone and pacing matter as much as question quality.
- **IMPACT**: Live startup on Yash's raw resume now pre-seeds with a much more inviting opener: `I’d love to start with your TinyML Audio Classification Pipeline — what part of that work felt most yours?` instead of a colder ownership probe. The runtime also now has first-class closing semantics: when Sprint 3 reaches the final two questions, the served question is wrapped with an explicit “last two questions” / “final question” framing so the interview lands deliberately instead of ending without runway.

### [2026-04-23] - Interview Map Core Hardening: Parsing + Detail-First Fallback Seeds
- **WHAT**: Hardened the map pipeline in two core places. `backend/models/llm_router.py` now recovers JSON arrays embedded in model output instead of only objects, which is important because focus-seed extraction expects an array. `backend/services/interview_map.py` now prioritizes detail-level artifact extraction from work bullets over coarse role headers, fixes the work-entry segmentation bug that treated bullet lines like new headers, and produces stronger deterministic fallback tracks keyed to real technologies like `Google ADK`, `TensorFlow Lite-Micro INT8`, `OCR`, and `feature-map control system`.
- **WHY**: The biggest remaining risk was not just “the LLM is slow.” It was that when the LLM path missed, the fallback map could still drift into low-quality labels or generic tracks, and when the LLM path succeeded partially, valid array-shaped seed output could be discarded by the shared router parser. That made the interview map too brittle for a core control-plane component.
- **IMPACT**: The map pipeline is now materially safer. Fallback focus seeds on resume-like input now prefer artifacts such as `Agent Based AIGC Video Generation And Editing Pipeline`, `Feature-Map Control System`, `TinyML Audio Classification Pipeline`, and `Multi-Modal Benchmark Framework` instead of role/location clutter. Deterministic tracks are also more interview-worthy because they now anchor questions to real technologies and constraints from the candidate's own snippets rather than broad generic template language. Remaining gap: LLM-authored branch hydration is still provider-latency-sensitive, but the default map quality when hydration misses is now substantially stronger.

### [2026-04-23] - Hybrid Fallback Seeding + Map Regression Tests
- **WHAT**: Added a second ultra-focused small-model artifact extractor in `backend/services/interview_map.py` that runs when primary focus-seed extraction is thin, then normalizes and merges those artifact labels with deterministic fallback seeds. Also added label-overlap dedupe so fallback extraction stops emitting near-duplicates like `TinyML Audio Classification Pipeline` and `Audio Classification Pipeline` together. Added a lightweight regression test at `backend/test_interview_map_fallback.py`.
- **WHY**: Even after the earlier hardening, the fallback path could still land on broad or redundant labels because it had to choose between one general LLM seed pass and pure manual extraction. The user explicitly asked for a small, fast LLM call if it helps the fallback mechanism, and that is the right trade-off here: a tiny artifact extractor is cheaper and safer than relying on broad free-form generation later.
- **IMPACT**: The fallback path is now hybrid rather than brittle. When the main focus-seed pass underperforms, the system can still recover artifact-quality labels like `Feature-Map Control System` and `TinyML Audio Classification Pipeline` before dropping fully to manual parsing. The new regression test proves the current fallback extractor keeps those artifact labels and avoids the redundant degraded variants.

### [2026-04-23] - Interview Map Evidence Binding + Live Resume Verification
- **WHAT**: Tightened deterministic snippet binding and tech ranking in `backend/services/interview_map.py`. `_extract_resume_snippets()` now binds each focus area to the best matching work entry before scoring generic resume lines, and `_extract_focus_signals()` now preserves family-aware tech priorities instead of losing them in a later generic re-sort. Added a broader deterministic quality contract test at `backend/test_interview_map_contract.py`.
- **WHY**: The biggest remaining map bug was no longer focus extraction; it was evidence misbinding inside deterministic tracks. TinyML branches could ask about `Google ADK` or `MediaPipe Audio` instead of `TensorFlow Lite-Micro INT8`, and unrelated lines like `Top Skills` could still leak into some focus areas. That made fallback tracks look real while still probing the wrong implementation surface.
- **IMPACT**: Deterministic fallback now stays on the correct artifact and asks about the correct mechanism. On Yash's resume shape, the map now consistently lands on:
  - `Agent Based AIGC Video Generation And Editing Pipeline`
  - `Feature-Map Control System`
  - `TinyML Audio Classification Pipeline`
  - `Multi-Modal Benchmark Framework`
  TinyML short-answer rescue now correctly references `TensorFlow Lite-Micro INT8`, and feature-map probes now correctly reference `Google Veo 3`. Verification completed with:
  - `python3 -m py_compile backend/services/interview_map.py backend/test_interview_map_fallback.py backend/test_interview_map_contract.py`
  - `python3 -m backend.test_interview_map_fallback`
  - `python3 -m backend.test_interview_map_contract`
  - isolated live smoke on `127.0.0.1:8011`, where `POST /api/start_interview` returned `200`, state stored the corrected four-focus map, and live `process_turn` on the TinyML path returned the exact map-grounded short-answer prompt about `TensorFlow Lite-Micro INT8`.

### [2026-04-23] - Strict Interview-Map Preparation Gate + Ready-Only Launch
- **WHAT**: Reworked the startup architecture so interview-map preparation is now its own explicit phase. Added `validate_interview_map(...)` in `backend/services/interview_map.py`, branch-provenance metadata (`llm_branch_count`, `fallback_branch_count`, `llm_branches`, `fallback_branches`), stronger slow-path hydration retries, and a new strict two-step contract:
  - `POST /prepare_interview_map`
  - `POST /start_interview` with `prepared_session_id`
  Frontend launch in `app/page.tsx` and fresh-rerun launch in `app/interview/[session_id]/page.tsx` now follow that two-step flow instead of calling `start_interview` directly.
- **WHY**: The product requirement changed from “don’t start without any usable map” to “don’t start until a rich, validated AI-authored questioning map exists.” The old contract still let deterministic fallback count as startup success, which was technically robust but not aligned with the intended quality bar.
- **IMPACT**: Antigravity can now spend up to roughly two minutes preparing the questioning map, and interview launch is blocked unless the map validator says it is ready. The validator now requires:
  - enough focus areas
  - no metadata-noise labels
  - full branch coverage
  - no pending hydration
  - all focus areas marked `track_source="llm"`
  - a minimum LLM branch ratio plus required core LLM-authored branches
  Verification completed with:
  - `python3 -m py_compile backend/services/interview_map.py backend/services/orchestrator.py backend/api/routes.py backend/test_interview_map_validation.py`
  - `python3 -m backend.test_interview_map_fallback`
  - `python3 -m backend.test_interview_map_contract`
  - `python3 -m backend.test_interview_map_validation`
  - `npm run build`
  - isolated live smoke on `127.0.0.1:8011`
    - initial strict prep failure surfaced real unmet richness constraints, which exposed a hydration provenance bug
    - after fixing provenance carry-through and strengthening the slow hydration path, `POST /prepare_interview_map` succeeded with `map_status="ready"`
    - live result on Yash’s resume: `llm_focus_count=4`, `rich_focus_count=4`, no pending hydration
    - `POST /start_interview` with the prepared session id then returned `200` and served rich LLM-authored preview branches

### [2026-05-04] - ProvenHire Full-Page Handoff Runtime + Async Finalization
- **WHAT**: Added the Antigravity side of the ProvenHire handoff integration. New `/launch?token=...` frontend route consumes ProvenHire launch tokens through `POST /api/provenhire_handoff/consume`, starts or resumes a prepared Antigravity session, stores `external_handoff` metadata in session state, and returns candidates to ProvenHire after completion. Added signed ProvenHire callback helpers with retry/backoff for started/complete/failed events. Reworked final-turn handling so `process_turn` marks the live interview complete and starts report scoring in a background task instead of awaiting the full evaluation before returning.
- **WHY**: ProvenHire’s embedded proxy path was carrying too much live-interview risk: final turns could block while the full LLM evaluation ran, every turn and TTS call had extra proxy failure points, and ProvenHire needed a cleaner way to let the standalone Antigravity experience own the interview while still receiving durable completion/report data.
- **IMPACT**: Final candidate turns now return immediately with `complete: true`, `report_ready: false`, and `finalization_status="running"`, while `/api/report/{session_id}` only reports `complete: true` once final evaluation exists. Standalone handoff mode can now notify ProvenHire when the report is ready, retry transient webhook failures, and preserve a manual `/handoff-sync` backstop on the ProvenHire return page. Verified with `python3 -m py_compile` and `npm run build`.
### [2026-05-07] - Final Product Decision Hardening After Layer-8 Audit
- **WHAT**: Hard-wired Yash's final product calls into runtime and shared docs. `backend/services/tts_service.py` is Cartesia-first whenever Cartesia credentials exist, with ElevenLabs as fallback only. Removed live context-aware filler selection so fillers stay in the simple default pool for this release. Preserved the application-transfer / coverage-map redesign as the core direction, but changed final evaluation so coverage produces an advisory signal instead of overriding the LLM's contextual hire recommendation and confidence. Fixed the early communication-mode detector so it waits for two committed answers, updated map contract tests for the dimension-schema map, and normalized frontend/dashboard handling for historical extended verdict enums.
- **WHY**: The implementation had embedded several choices that needed product-owner approval. Yash confirmed Cartesia is the permanent TTS primary, context-aware fillers are future scope, application transfer is the elite redesign path, RAG removal is correct, disengagement/save-face routing is correct, and deterministic coverage should not be the final hiring authority yet.
- **IMPACT**: The code now matches the final product stance: Cartesia-first everywhere, no premature context-aware filler behavior, app-transfer remains load-bearing, coverage informs but does not overrule final LLM judgment, and early stress-test utterances no longer over-trigger communication-mode changes from a single answer.
